{
	"name": "@promptgate/evals",
	"version": "1.0.0",
	"type": "module",
	"bin": {
		"pg-eval": "./src/cli.ts"
	},
	"scripts": {
		"build": "rm -rf dist && tsc -p tsconfig.json --outDir dist --rootDir src && find dist -type f -name '*.test.js' -delete && find dist -type f -name '*.test.d.ts' -delete"
	},
	"dependencies": {
		"@promptgate/shared": "workspace:*",
		"ajv": "^8.20.0",
		"yaml": "^2.8.1",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@types/node": "^22.20.1"
	}
}
