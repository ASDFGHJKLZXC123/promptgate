{
	"name": "@promptgate/shared",
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"build": "rm -rf dist && tsc -p tsconfig.json --outDir dist --rootDir src && find dist -type f -name '*.test.js' -delete"
	},
	"dependencies": {
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"vitest": "^4.1.10"
	}
}
