{
	"name": "@promptgate/gateway",
	"version": "1.0.0",
	"type": "module",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "rm -rf dist && tsc -p tsconfig.json --outDir dist --rootDir src && find dist -type f -name '*.test.js' -delete && cp -R src/db/migrations dist/db/migrations",
		"dev": "tsx watch src/index.ts"
	},
	"dependencies": {
		"better-sqlite3": "^12.11.1",
		"fastify": "^5.10.0",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@types/better-sqlite3": "^7.6.13",
		"@types/node": "^22.20.1",
		"tsx": "^4.23.1",
		"vitest": "^4.1.10"
	}
}
