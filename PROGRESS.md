# PromptGate — Build Progress

> Maintained by the orchestrator (see `ORCHESTRATOR.md`). Updated after every step. Humans: the Position line is always the truth.

**Position:** Phase 0 — done and human-approved; Phase 1 — done and human-approved; Phase 2 — done and human-approved; Phase 3 — done and human-approved; Phase 4 — done and human-approved; Phase 5 — done and human-approved; Phase 6 — done and human-approved (2026-07-30), evidence `docs/evidence/phase-6.md`; post-Phase-6 defect D1 Remedy A — done and published; Phase 7 — done and human-approved (2026-07-30), evidence `docs/evidence/phase-7.md`; Phase 8 — in progress (step 4 complete; observation window day 5/7; qualifying traffic floor met 5/5; daily heartbeat active)
**Last session:** 2026-08-04 — the required first-operation read-only preflight found no qualifying local-day row. The gateway remained healthy, immutable prompt ID 4/v1 stayed live at `prod`, and the stopped ignored dogfood app was safely restarted against its persisted output. Exactly one date-unique idempotent text-only app edit was issued with no caller retry. Its first bounded internal PromptGate attempt became durable `client_aborted` row 434 after 5,992 ms with conservative 371-micro-USD input-only estimated cost; the app's normal transient retry succeeded as row 435, HTTP 200 job `38b4f58ab1c843bf86d0ff180de0e3e7`, and snapshot `22d1bb892aef4084a17fb9e8b36ac82a`. Row 435 is the sole successful qualifier for 2026-08-04: prompt ID 4/v1, uncached, non-streaming, exact-cost, and `ok`. Buffered progress replay remained intact, and the output preserved every required count byte-for-byte. Registry state and the $5/60-RPM key are unchanged. Eleven retained rows total 15,965 micro-USD ledger spend (15,223 exact plus 742 estimated), with 5,089 exact saved micro-USD and zero estimated/unknown savings. Adding the separately disclosed exact orphan yields 21,054 micro-USD of orphan-inclusive accounting, of which 20,312 is known exact provider-priced completions. The observation is day 5/7 and the qualifying traffic floor is met at 5/5; conflicting wall-clock and retained monotonic timings are disclosed, and only `total_ms` is used for latency evidence.
**Repo state at last update:** Protected `master` is `6f2d44ea2bcf118eccabb815d2fd459f4097bfb0` (`6f2d44e`), the merge of Phase 7 PR #12; working branch `codex/phase8-dogfood` contains committed step 1 `9478e43`, step 2 `af3295b`, Amendment A `41e21d5`, lifecycle correction `f6cd9fa`, step 3 evidence `a4c4636`, step 4 evidence `6c8ab5b`, observation monitor `d0b3020`, observation day 2 `c26b589`, observation day 3 `b59ae93`, and observation day 4 `fe4a718`; exact gateway image `sha256:b0b39a7bffe71e7a4b7e3ffd9231d3ad470175edffca9ff362fcd12c61a4e0b8` is healthy on `127.0.0.1:8787` against the persistent database; finished `web_builder_llm` remains unchanged at `1fe570bb175834107409739703e03efdd5805fc2`, including its unrelated pre-existing untracked `TECH_STACK.md`; the ignored local dogfood clone is clean at timeout commit `5335d9f` plus independently approved registry integration `ff47ea8`
**Phase 6 step 1 record:** complete · **Phase 6 step 2 record:** complete · **Phase 6 step 3 record:** complete · **Phase 6 step 4 record:** complete · **Phase 7 step 1 record:** complete · **Phase 7 step 2 record:** complete · **Phase 7 step 3 record:** complete · **Phase 7 step 4a record:** complete · **Phase 7 step 4b record:** complete · **Phase 7 step 4c record:** complete · **Phase 7 step 4d record:** complete · **Phase 7 step 4e record:** complete · **Phase 7 Verify record:** complete · **Phase 8 step 1 record:** complete · **Phase 8 step 2 record:** complete · **Phase 8 Amendment A record:** complete · **Phase 8 step 3 record:** complete · **Phase 8 step 4 record:** complete · **Last green `pnpm test`:** 2026-07-31 (67 test files, 859 tests passed) · **Last green `pnpm lint`:** 2026-08-04 (179 files) · **Last green `pnpm build`:** 2026-07-31 · **Last green external dogfood suite:** 2026-07-31 (380 tests passed)

## Phase status

Model/effort per ORCHESTRATOR.md → Model & effort assignment.

| Phase | Name | Implementer | Status | Verify evidence | Approved by human |
|---|---|---|---|---|---|
| 0 | Scaffold | GPT-5.3-Codex-Spark / xhigh; Terra / medium for DB+Docker | done | `docs/evidence/phase-0.md` | project owner — 2026-07-16 |
| 1 | OpenAI-compatible non-streaming | Claude Sonnet 5 / high; Spark+Luna support; Sol / xhigh expansion audit | done | `docs/evidence/phase-1.md` | project owner — 2026-07-25 |
| 2 | Anthropic + four-provider streaming | Claude Opus 4.8 / xhigh; Luna fixtures; Sol / xhigh checkpoint | done | `docs/evidence/phase-2.md` | project owner — 2026-07-26 |
| 3 | Cache, limits, budgets | GPT-5.6 Terra / high; Sol / xhigh budget audit | done | `docs/evidence/phase-3.md` | project owner — 2026-07-26 |
| 4 | Prompt registry | GPT-5.6 Terra / high; Spark support | done | `docs/evidence/phase-4.md` | project owner — 2026-07-26 |
| 5 | Eval harness | Claude Opus 4.8 / xhigh; Spark+Luna support; Fable dataset | done | `docs/evidence/phase-5.md` | project owner — 2026-07-29 |
| 6 | CI gate | GPT-5.6 Terra / high; Spark draft support | done | `docs/evidence/phase-6.md` | project owner — 2026-07-30 |
| 7 | Dashboard | GPT-5.6 Terra / high; Spark scaffold; Sol / high polish | done | `docs/evidence/phase-7.md` | project owner — 2026-07-30 |
| 8 | Dogfood + writeup | Claude Sonnet 5 / high; Luna evidence; Sol / high writeup | in progress (step 4 complete; observation day 5/7; traffic floor met 5/5) | `docs/evidence/phase-8.md` | — |

Evidence = link/pointer to captured verify-block output (commit, gist, or `docs/evidence/phase-N.md`).

Status values: `not started` · `in progress (step K or checkpoint)` · `verify pending` · `awaiting approval` · `done`

## Pathway reviews (mixed-model)

| Checkpoint | Trigger | Reviewer | Verdict | Date |
|---|---|---|---|---|
| A — provider seam | after phase 2 | GPT-5.6 Sol / xhigh | proceed with adjustments — commit/link Phase 2 evidence, move progress to awaiting human approval, keep Phase 3 not started; no code blocker | 2026-07-25 |
| B1 — eval gate technical audit | after phase 5 | GPT-5.6 Sol / xhigh | proceed with adjustments — use a scalar machine-epsilon boundary for paired/history score drops, add exact/just-over regressions, and correct the stale evidence lead; live results remain valid and need no rerun | 2026-07-29 |
| B2 — dataset/pathway verdict | after B1 | Claude Fable 5 / high | proceed with adjustments — preserve the exact certified good-v1 prompt artifact in evidence; provenance and discrimination are credible, residual drift/flaky-case/Phase 6 risks are explicit and non-blocking | 2026-07-29 |
| Phase 7 final Verify evidence audit | after provider-free Verify | GPT-5.6 Sol / high | APPROVE — complete and honestly captioned Prompts/Quality screenshots, retained dataset/hash/commands/outputs/UI rollback/post-shutdown DB proof, and narrowly truthful provider-variable claims; no remaining security, correctness, accessibility, evidence-integrity, or scope blocker | 2026-07-30 |
| Phase 8 truthfulness/Verify authority review | buffered-client and missing-Verify conflict before step 3 | Claude Opus / max | proceed with adjustments — do not widen `web_builder_llm` to token streaming; clarify its buffered request/response plus post-completion progress replay, re-anchor streaming proof to contract-nightly, lock skipped-missing/red-configured provider policy, put `pg_prompt` live before the seven-day window, state p95/cache methods, and add a literal Phase 8 Verify block | 2026-07-30 |
| Phase 8 step 3 evidence/lifecycle audit | after buffered live verification and lifecycle correction | independent read-only reviewer | APPROVE — source and seven regressions confirm exactly-once settlement, budget reconciliation, and first-cause preservation; clean-build 859-test gate and exact healthy image match; rows 425–427, retained 7,222-micro-USD spend, exact 5,089-micro-USD savings, and the separate 5,089-micro-USD orphan are truthful; original external repo is unchanged, direct control is excluded from p95, observation remains closed, and no credential leak or overclaim was found | 2026-07-31 |
| Phase 8 step 4 external integration audit | after PromptGate-only `pg_prompt` client patch | independent read-only reviewer | APPROVE — exact provider-ID branch, empty messages, exact registry ref, fully assembled generate/modify/repair variable, unchanged direct-provider shape and transport behavior, 380-test full suite and package, clean isolated clone, and untouched original repository all verified | 2026-07-31 |
| Phase 8 step 4 live evidence audit | after canary, confirmed rollback, and opening call | independent read-only reviewer | APPROVE — exact immutable registry definitions/history/current v1, consecutive uncached v2→v1 rows on one app process, snapshots and byte-identical content, retained/orphan-inclusive totals, seven-day date math, healthy image, and original/isolated-repo state all reconcile; no credential disclosure or material overclaim | 2026-07-31 |
| Phase 8 observation day 2 audit | after the 2026-08-01 qualifying request | independent read-only reviewer | APPROVE — row 430 and its local-day uniqueness, dates/threshold math, prompt v1/v2 and unchanged `prod = v1` history, exact gateway image, key limits, retained/orphan-inclusive accounting, repository states, job events, snapshot parentage/token usage/content/hashes/byte identity all reconcile; no credential disclosure or material overclaim. Caller-side HTTP timing and the pre-restart stopped state are transient observations, but durable event timestamps and resulting records corroborate the successful request | 2026-08-01 |
| Phase 8 observation day 3 audit | after the 2026-08-02 qualifying request | independent read-only reviewer | APPROVE — row 431 and its local-day uniqueness, three qualifying dates/threshold math, prompt v1/v2 and unchanged `prod = v1` history, exact gateway image, key limits, retained/orphan-inclusive accounting, repository states, job events, snapshot parentage/token usage/content/hashes/byte identity all reconcile; no credential disclosure or material overclaim. The stopped state, first recovery configuration, preflight ordering, caller timing, and recovery-before-provider ordering are transient operator observations; durable records corroborate the successful recovered request | 2026-08-02 |
| Phase 8 observation day 4 audit | after the 2026-08-03 qualifying request and internal retry | independent read-only reviewer | APPROVE — rows 432/433, the sole successful Aug-3 qualifier, four distinct dates, exact/estimated ledger and orphan-inclusive accounting, unchanged prompt/label/key state, exact gateway image, repository states, bounded retry source, job events, snapshot parent/tokens/content/hashes/byte identity all reconcile. The wall-clock span conflicts with retained monotonic durations and is correctly excluded from latency evidence; one-app-call/no-caller-retry and preflight ordering are transient observations. No correction, credential disclosure, or material overclaim | 2026-08-03 |
| Phase 8 observation day 5 audit | after the 2026-08-04 qualifying request and internal retry | independent read-only reviewer | APPROVE — rows 434/435, sole Aug-4 qualifier, five once-each dates, exact/estimated ledger and orphan arithmetic, unchanged prompt/label/key state, exact image health, repository states, bounded retry behavior, job replay/tokens, and wall-clock exclusion all reconcile; no credential disclosure or material overclaim. The timeboxed reviewer did not freshly repeat snapshot file hashes/counts/byte comparison and relied on the orchestrator's completed read-only output verification for that narrow claim; no contrary evidence was found | 2026-08-04 |
| Phase 6 required-check policy | branch/check authority conflict | Claude Opus / max; GPT-5.6 Terra implementation + independent security review | proceed with adjustments — protect existing `master`; remove event path filters; use an exact merge-base PR allowlist, provider-inert irrelevant result, provider-inert red untrusted result, and always-running `eval-gate`; require strict `ci` + `eval-gate` for administrators without adding reviews | 2026-07-29 |
| Phase 0 Compose binding | doc-authority conflict | Claude Fable 5 / high | proceed with adjustments — publish on loopback and sync the playbook | 2026-07-16 |
| Phase 1 request identity | schema/API doc-authority conflict | Claude Fable 5 / high | proceed with adjustments — persist a UUID in migration 002, renumber the uncommitted registry/eval migrations, and schedule the owner-scoped usage endpoint in phase 2 | 2026-07-17 |
| Phase 1 four-provider scope | locked scope/provider expansion | Fable 5 / high unavailable (usage credits); GPT-5.6 Sol / xhigh technical audit + project owner | proceed with adjustments — approve OpenAI, Anthropic, Gemini, and DeepSeek; use an explicit available-key live matrix; add cache-aware pricing; keep full streaming parity in phase 2 | 2026-07-25 |

## Blockers (current)

- Phase 5 has no remaining blocker and is human-approved.
- Phase 6's topology conflict is resolved: the owner approved four dedicated provider-side-capped CI credentials with the unchanged fresh paired DeepSeek-target/Terra-judge gate. Anthropic and Gemini receive no Phase 6 eval traffic and must not be described as live-green.
- Phase 6 step 1 has no remaining blocker: owner-approved Recovery A is implemented, the exact single-key/handoff regressions and complete offline gate are green, an isolated real gateway passed create/reuse with zero provider requests, and the independent correction audit returned `APPROVE`.
- Phase 6 step 2 has no remaining blocker: the owner confirmed the four dedicated provider-side-bounded CI credentials are configured, and a value-blind GitHub Actions secret-name check verified `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `GEMINI_API_KEY`, and `DEEPSEEK_API_KEY`.
- Phase 6 step 3 has no remaining blocker: the SHA-pinned workflow, secure secret/handoff path, exact paired command, cleanup rails, offline workflow contract, full repository gate, Docker build, and both independent reviews are green.
- Phase 6 step 4 has no remaining blocker: the policy, response-finish correction, and deterministic Linux regression are published; ordinary CI run `30515425287` is green; provider-inert PR #1 proved `ci` and `eval-gate` as successful GitHub Actions checks with `live-evaluation` skipped; and `master` protection was enabled and re-read as strict, administrator-enforced, no-review, no-force-push, and no-deletion.
- Phase 6 Verify's green arm is complete via the prescribed disposable proof PR #5, corroborated by merged PR #4. PR #5 (head `4d80296f99c025d2340939236c000c6e378e9291`, `eval-gate` run `30521540051`) paired the same certified v1 for `prod` and `candidate` on the single-run same-version path — the candidate ran once and the score drop is 0 — and passed 49/50 with baseline and candidate score_avg both 0.9342857142857142 over 7 scored cases; its single tolerated failure was `policy_blood_review_01` (expected `review`, received `none`); the process exited 0 with pricing seed, gateway, CI seed, evaluation, cleanup, and the aggregate `eval-gate` all green; per the Verify contract it was never merged — closed unmerged with its remote branch deleted after evidence capture. Merged PR #4 (same-version pairing amendment plus fresh pricing seed fix; head `7e8c91d4b7120016817b4cc9b96177ba0efbc7c1`, run `30520586222`; merged to protected `master` as `2e82f2567afdebff30caced108b7de2867e0aa41` under the required strict `ci` and `eval-gate` checks) is corroborating amendment evidence, not the disposable green arm: same certified v1 single-run pair, 48/50 passed, baseline and candidate score_avg both 0.9714285714285715 over 7 scored cases, drop 0, two tolerated failures — `policy_fever_review_01` (immutable direct-diagnosis safety match) and `masked_stopped_heart_meds_withdrawal_review_01` (expected `review`, received `urgent`) — and process exit 0 with pricing seed, CI seed, evaluation, cleanup, and the aggregate gate green. Both runs' exact facts were recovered read-only from their retained GitHub job logs during the 2026-07-30 independent verification; the earlier claims that PR #4 was the green Verify proof and that its exact scores were uncaptured/unrecoverable were wrong and are corrected here.
- Phase 6 Verify's red arm is complete via the single pre-registered amended live attempt. The certified-degraded first attempt on PR #6 (`codex/phase6-degraded-proof` at `3663220`, candidate→certified degraded v2, run `30522521709`) unexpectedly returned exit 0 — candidate 45/50 (0.90) with five new deterministic failures, baseline score_avg 0.9720000000000001 over five scored cases, candidate 0.9879999999999999 over five, drop -0.015999999999999792 — and is recorded as defect D1 evidence. The owner-approved deterministic negative-control v3 was then implemented and offline-verified on the same branch (16/34 margin proof, lint 159 files, 771 tests in 60 files, four-package build, Compose configuration, diff checks), independently audited (BLOCK on green-arm record accuracy, corrected, then fresh APPROVE), committed/pushed as `28e4d6ad8481b481ab5acc79e702178a7fd7223c`, and run exactly once: `eval-gate` run `30539658160` returned the pre-registered red — `detect-eval-scope` job `90861169364` green with relevant/trusted true, `live-evaluation` job `90861217803` quality failure, aggregate `eval-gate` job `90866805736` red; fresh baseline-first prod-v1/candidate-v3 pair; candidate 14/50 with 36 named failures (34 deterministic `Expected risk_level urgent|review; received none.` rows plus the two expected-`none` rubric cases at 0.08 and 0.25); baseline score_avg 0.8966666666666666 over 6 scored cases, candidate 0.165 over 2, drop 0.7316666666666666; exact process exit 1; pricing seed, setup, CI seed, and cleanup green; `ci` green at the same head (push run `30539655326` job `90861160384`; PR run `30539658159` job `90861169134` — lint, build, 771 tests). The result is valid below the pre-registered <40 falsification boundary — the exact compliant offline prediction was 16/50 with 34 deterministic label failures, and the two additional rubric failures moved the live result further red to 14/50 — so the control is not falsified and no rerun occurred. PR #6 is closed unmerged (`mergedAt` null) with local and remote `codex/phase6-degraded-proof` deleted after evidence capture.
- Phase 6 has no remaining blocker and is human-approved. Evidence PR #7 is merged on protected `master` at `bcca560`.
- Post-Phase-6 defect D1 Remedy A has no remaining blocker: fresh distinct-version/history comparison, exact-boundary, same-version, historical-validation, D1 replay, output, CLI, and unchanged-workflow regressions are green; full lint/test/build/Compose/diff gates passed; independent Sol/xhigh review returned APPROVE; protected PR #8 passed both ordinary CI runs plus the 13m13s live evaluation and aggregate `eval-gate`, then merged as `50abf9e`.
- Phase 7 step 1 has no remaining blocker: the Vite/Chart.js scaffold, strict browser TypeScript, proxy/output contracts, local production bundle, accessible responsive shell, and no-network tests are green; independent Sol/high review returned APPROVE; protected PR #9 passed both ordinary CI runs plus the 13m25s live evaluation and aggregate `eval-gate`, then merged as `bb98cd5`.
- Phase 7 step 2 has no remaining blocker: static serving, absolute-only configuration, API precedence, missing-build-output tolerance, dotfile/traversal protection, Docker bundle copy/runtime path, and exact corrected-image smoke are green. Independent Sol/high review first found and blocked the inherited dotfile exposure, then returned APPROVE after `dotfiles: "ignore"`, security regressions, and lifecycle-owned test cleanup. Protected PR #10 passed both ordinary CI runs, the 13m15s live evaluation (same prompt version 1 run once; 48/50, pass rate 0.96, score average 0.9716666666666667 over 6 scored cases, both relative drops zero), cleanup, and the aggregate `eval-gate`, then merged as `97ef1e4`.
- Phase 7 step 3 has no remaining blocker: load-time prompt, module-only token state, forced admin header, normalized admin-path restriction, bounded 401 recovery, canceled and concurrent refresh behavior, request preservation, and no-persistence/no-logging rails are covered; focused dashboard 16/16, full 802/802, lint 164 files, dashboard and four-package builds, browser smoke, and diff checks are green; independent Sol/high review returned `APPROVE`; protected provider-inert PR #11 passed `ci` plus aggregate `eval-gate` and merged as `1dbec03`.
- Phase 7 step 4's authority blocker is resolved: the project owner approved Contract Amendment A, authorizing the synchronized metrics enum/shape, raw-row semantics, cache-savings provenance migration, prompt detail/history read endpoint, dataset-hash drift partitioning, and honest missing-judge-provenance disclosure while keeping the eval schema unchanged.
- Phase 7 step 4a has no remaining blocker: migration 006 preserves legacy-hit unknowns and backfills misses to exact zero; streaming and non-streaming cache paths freeze and replay saved-cost provenance; the authenticated metrics DAO/route enforces exact RFC3339 bounds, UTC hourly buckets, whitelisted grouping, nearest-rank percentiles, null/unknown semantics, safe-integer arithmetic, strict stored-row validation, and constant-memory timestamp preflight; prompt detail returns ordered parsed immutable versions and current labels with safe missing/corrupt behavior; the synchronized authority docs retain raw-window and no-eval-schema-change limits. Focused backend checks plus the clean-build repository gate passed 823/823 tests in 64 files, lint across 170 files, all four package builds, Compose configuration, and diff checks; independent GPT-5.6 Sol / high review returned `APPROVE`.
- Phase 7 step 4b has no remaining blocker: Overview renders only authenticated live API data, with model-stacked known spend, request count plus p50/p95, cache rate plus known exact/estimated savings, and per-key month-to-date budget burn; all panels have loading/error/empty states, reduced-motion-safe nonanimated charts, accessible table alternatives, and escaped dynamic content. Runtime parsing fails closed on malformed contract values, financial arithmetic stays safe/integer, one floored whole-second `to` keeps the six-call fan-out on a consistent exclusive snapshot, mixed buckets are rejected, sibling requests abort on failure/retry, UTC and exact/estimated/unknown provenance are explicit, and zero budgets are described as hard ceilings. Focused dashboard coverage passed 25/25; the repository gate passed 824/824 in 64 files, lint across 170 files, all builds, Compose configuration, and diff checks. The independent GPT-5.6 Sol / high review found seven initial correctness/accessibility/presentation issues and one same-second snapshot race; every finding received a regression or direct correction, and the final verdict was `APPROVE`.
- Phase 7 step 4c has no remaining blocker: Cost Explorer requests only the authenticated cost timeseries and groups by model, immutable key name, or feature; it preserves every integer micro-USD digit and separates exact, estimated, known-subtotal, and unknown-pricing counts in text, chart, and table. Null features are visibly `Untagged` without colliding with literal names; backslashes plus all Unicode control/format characters are encoded visibly; dynamic HTML is escaped; the shared DTO parser rejects extra fields, duplicate bucket/group points, descending buckets, invalid provenance, and overflow without reimplementing SQLite string collation. Native radios remain mounted and visibly selected through loading/error/success, hash navigation disposes only the active screen and moves focus, stale requests/charts/listeners are aborted or destroyed, and fractional Chart.js axis ticks cannot enter the strict ledger formatter. Focused dashboard coverage passed 35/35; the repository gate passed 834/834 in 65 files, lint across 172 files, all builds, Compose configuration, and diff checks. The independent GPT-5.6 Sol / high pre-audit and two review rounds found precision, focus, feature-identity, parser, route-lifecycle, and fractional-tick issues; every blocker was corrected and the final verdict was `APPROVE`.
- Phase 7 step 4d has no remaining blocker: Prompts reads only authenticated live registry, selected-detail, and plain-text messages-diff data; presents immutable versions, variables, notes, current labels, and messages-only comparison without fake data; and moves labels only through explicit direction-aware Promote/Roll back confirmation. Every dynamic path segment is encoded once, DTOs and unified-patch framing fail closed, prompt and label identities keep literal backslashes distinct from visible Unicode control/format escapes, and JSON/diff content preserves printable text plus structural newlines. Numeric prompt/label bindings, two-version selection, focus restoration, small live status, direct retry, no-store reads, independent stale-request ownership, and route disposal are covered. Confirmed writes are serialized and never optimistic; server-returned prior state controls the notice; both list and detail must reload before the UI claims confirmation; and any failed/ambiguous write or post-write reload leaves a persistent warning with mutation controls unavailable until a fresh selection. Focused dashboard coverage passed 46/46; the repository gate passed 845/845 in 66 files, lint across 174 files, all builds, Compose configuration, and diff checks. The independent GPT-5.6 Sol / high audit found and drove corrections for build, concurrency, stale state, identity, rendering, focus, MIME/framing, and post-success truthfulness issues before returning final `APPROVE`.
- Phase 7 step 4e has no remaining blocker: Quality Drift requests the complete unfiltered authenticated eval-run history with no-store semantics, strictly validates exact fields, canonical timestamps, safe numeric bounds, complete-or-null prompt attribution, unique IDs, and newest-first `(ts,id)` order, then displays chronologically with same-second ID stability. Every comparison, score/pass-rate series, and prompt/model change marker is partitioned by exact immutable `dataset_hash`; partition-owned indexed points retain continuity across interleaved other-hash runs without cross-hash joins, while a real null rubric score remains a visible gap. Pass rate is explicitly cases where all assertions pass; score is the mean over scored rubric cases only. The screen discloses missing historical judge identity and refuses causal attribution, renders no fallback data, makes identity controls/backslashes visible under HTML escaping, and provides labeled nonanimated chart lines plus a complete keyboard-scrollable table and textual annotations. Stale requests, owned charts, and listeners are aborted or destroyed on refresh/navigation. Focused dashboard coverage passed 53/53; the repository gate passed 852/852 in 67 files, lint across 176 files, all builds, Compose configuration, and diff checks. The independent GPT-5.6 Sol / high pre-audit and review identified interleaved-partition continuity, semantic-copy, and scroll-region accessibility issues; every blocker was corrected before final `APPROVE`.
- Phase 7 Verify has no remaining blocker: the real built gateway/dashboard ran on loopback with deterministic injected adapters, a fresh temporary database, real pricing/CI seeds, controlled local cost/cache/error traffic, and two same-file/same-hash eval runs. All four screens rendered non-empty. The Prompts UI confirmed and truthfully refreshed a `prod` v2→v1 rollback; the next identical request resolved to v1 and changed from `none` to `urgent`; paired pass rates moved 0.5→1.0 while both rubric averages remained 0.9, and Quality Drift showed `Prompt v2 → v1` inside one dataset-hash partition. Read-only post-shutdown assertions confirmed versions `[2,1]`, cache miss→hit with exact saved cost, an estimated-cost row, an unknown-pricing rejected row, current `prod` v1, and latest label history 2→1. Browser console and 1280-pixel overflow checks were clean, prompt focus and the keyboard-scrollable history region were present, the dashboard references no browser storage/cookie API, and five complete screenshots are retained in `docs/evidence/phase-7/`. The exact dataset, deterministic gateway and loopback proxy, sanitized command/output transcript, screenshot hashes, value-blind empty-environment provider-variable guard plus dummy-key fail-closed check, and post-shutdown database output are retained beside them. No provider endpoint was reachable or called, and all four provider-key variables were absent during the Verify. The final repository gate passed 852/852 tests in 67 files, lint across 178 files, all builds, Compose configuration, and diff checks. The fresh independent GPT-5.6 Sol / high final evidence re-audit returned `APPROVE`; evidence is `docs/evidence/phase-7.md`.
- Phase 8 step 1 has no remaining blocker: protected Phase 7 PR #12 passed its refreshed two ordinary CI runs, same-version 50/50 live evaluation, cleanup, and aggregate `eval-gate`, then merged as `6f2d44e`; the exact merged PromptGate image is healthy on loopback against the retained persistent database; key ID 26 is the single enabled `web_builder_llm` key with the exact $5 monthly budget and 60 RPM limit; its one-time plaintext and `.env` are owner-only ignored runtime files; the key authenticated against the six currently effective routed models; the post-deployment metrics window contained zero provider-request rows; lint and all 852 tests stayed green; and a fresh independent read-only evidence/security audit returned `APPROVE`. Evidence is `docs/evidence/phase-8.md`.
- Phase 8 step 2 has no external-repository or access blocker: the finished repository exists at `/Users/f8fq/coding projects/Finished/web_builder_llm` and its exact provider configuration, model-listing behavior, and PromptGate-facing tool behavior are now resolved below and evidenced in `docs/evidence/phase-8.md`; no source or runtime state in that repository changed.
- Phase 8 step 3 has no remaining blocker: normal generation and edit traffic through the real external UI proved buffered completion plus post-completion progress replay and three durable successful rows. The first edit exposed one exact 5,089-micro-USD pre-fix orphaned provider completion; it is explicitly excluded rather than backfilled. Commit `f6cd9fa` corrects buffered disconnect settlement and first-cause preservation, the focused 71/71 and clean-build 859/859 gates are green, the corrected exact image is healthy against the intact persistent database, and a unique post-fix edit retained row 427 with exact 4,913-micro-USD cost. Retained spend and the known orphan-inclusive provider total are reported separately.
- Phase 8 step 4 has no remaining blocker: external commit `ff47ea8` is independently approved and green across all 380 tests; prompt ID 4 has exact immutable v1/v2 definitions; row 428 proves the live canary served v2; confirmed label history records null→2 then 2→1; and the exact same next UI prompt served rolled-back v1 in row 429 without an app restart. Both rows are uncached, non-streaming, exact-cost successes, their valid output snapshots are byte-identical, `prod` remains v1, and a fresh independent live-evidence/security audit returned `APPROVE`.
- Phase 8 observation is active rather than blocked: the inclusive seven-day window is 2026-07-31 through 2026-08-06. Days 1 through 5 have exactly one successful registry-backed qualifying row each, satisfying the 5/5 traffic floor. The full span still must remain open through 2026-08-06 before closing screenshots, matched p95 analysis, and the final case study can be truthful. Days 4 and 5 each used one logical app edit with the external app's bounded transient retry, leaving one disclosed `client_aborted` attempt plus one successful qualifier rather than duplicate successful traffic. Active failed-runs-only heartbeat `phase-8-dogfood-observation` checks daily, preflights existing rows to prevent duplicate app edits, adds at most one logical edit on a missing day, and records an independently audited green commit.

## TODO(verify) resolutions

| Item | Where | Resolution | Date |
|---|---|---|---|
| web_builder_llm sends `tools`? | GUIDE §3.2 / playbook phase 2 | resolved — `AnthropicMessagesLlmClient` sends one `tools` entry with `input_schema` and forced `tool_choice` when its effective strategy is `ANTHROPIC_TOOL`; Phase 2 implemented Anthropic tool translation. The Phase 8 recheck confirms the PromptGate-facing `OpenAiChatLlmClient` path sends no tools; it sends `model`, one user message, `response_format`, and optional sampling fields | 2026-07-30 |
| web_builder_llm calls `GET /v1/models`? | GUIDE §5.1 / playbook phase 8 | resolved — yes, conditionally rather than at startup: key validation calls `GET {baseUrl}/models` with Bearer auth, and successful validation triggers the frontend live-model lookup whose `/api/models` backend path calls it again; failures fall back to the configured default model list. With the checked-in PromptGate definition, the exact upstream path is `http://127.0.0.1:8787/v1/models` | 2026-07-30 |
| carematch_ai safety cases: file paths + the 6 cases | GUIDE §7.3 / playbook phase 5 | resolved by project-owner-approved provenance correction — immutable public `server/safety.js` plus exact retained 2026-05-23 probes are the sources; no six-case test file is verifiable or recoverable in the audited sources, so observed, policy-derived, and synthetic cases must be distinguished in `docs/evidence/phase-5-seed-provenance.md` | 2026-07-26 |
| web_builder_llm provider config surface (env vars) | GUIDE §9 / playbook phase 8 | resolved — app-level provider JSON is selected by `LLM_PROVIDERS_CONFIG` or `site.llm.providers.config`; PromptGate is `providerId=promptgate`, `apiFamily=OPENAI_CHAT`, `authScheme=BEARER`, `baseUrl=http://127.0.0.1:8787/v1`, `defaultEnvKey=PROMPTGATE_API_KEY`, `jsonStrategy=NATIVE_JSON_OBJECT`, and `defaultModels=[deepseek-v4-flash]`. Launch must explicitly set `site.llm.allowPrivateBaseUrls=true` for trusted loopback. The key may be request-typed, encrypted locally, or supplied by the named environment variable; config contains no literal key | 2026-07-30 |

## TODO(build-time) resolutions

| Item | Resolution | Date |
|---|---|---|
| Pinned cheap eval models (one per provider) | `gpt-5.6-luna` (OpenAI), `claude-sonnet-5` (Anthropic), `gemini-2.5-flash` (Gemini), and `deepseek-v4-flash` (DeepSeek) | 2026-07-25 |
| `pricing.json` seeded from current provider pricing | Human-approved rows: Luna $1/$6, Terra $2.50/$15 effective 2026-07-16; Sonnet 5 promotional $2/$10 effective 2026-06-30 and published $3/$15 effective 2026-09-01; Gemini 2.5 Flash-Lite $0.10/$0.40 plus Gemini 2.5 Flash $0.30 ordinary input/$0.03 cached input/$2.50 output; and DeepSeek V4 Flash $0.14 cache-miss input/$0.0028 cache-hit input/$0.28 output effective from the 2026-07-25 verification date, all per Mtok and stored as integer micro-USD/Mtok | 2026-07-25 |

## Decision log

Small choices the spec didn't cover (architectural ones go to the human instead — see ORCHESTRATOR.md).

| Date | Decision | Rationale |
|---|---|---|
| 2026-07-31 | The project owner approved Phase 8 Truthfulness and Verify Amendment A: retain `web_builder_llm`'s buffered request/response and post-completion progress replay rather than adding token-stream transport; prove token streaming in `contract-nightly.yml`; make missing credentials named `SKIPPED`, configured failures red, and zero configured providers red; put live `pg_prompt` before a seven-consecutive-day window with real traffic on at least five days; report cache savings with exact/estimated/unknown provenance and matched uncached direct-vs-proxied p95; prove registry-label rollback; and close against the new literal Verify block. | Source inspection proved the finished app does not request provider-token streaming, while the prior shorthand required a streaming UX claim and supplied no final Phase 8 Verify block. The synchronized amendment preserves truthful scope, establishes a measurable dogfood threshold, prevents misleading latency/cache figures, and moves adapter-stream validation to the purpose-built scheduled contracts without weakening the deliverable. |
| 2026-07-30 | The project owner approved Phase 7 Step 4 Contract Amendment A: expand `/admin/api/metrics/timeseries` to request count, p50/p95 latency, cache savings, ungrouped totals, exact/estimated financial splits, and visible unknown counts over retained raw UTC-hourly request rows; migration 006 persists frozen per-hit savings plus exact/estimated provenance while legacy hit savings remain unknown; add `GET /admin/api/prompts/:slug` for current labels and immutable version detail; partition Quality Drift by `dataset_hash` and disclose missing historical judge provenance without changing the eval schema. | GUIDE §5.2 could not supply three required Overview panels, mutable cache-entry counters could not truthfully attribute historical savings, the Prompts screen lacked a version-history read path, and score series could be misleading across dataset edits or undocumented judge changes. The synchronized amendment supplies the smallest auditable contracts without repricing cache hits, misusing incomplete daily rollups, or widening eval persistence. |
| 2026-07-30 | Phase 7 token recovery uses separate in-memory prompt-attempt and accepted-token generations, preserves the previous token when a replacement prompt is canceled/empty, restricts requests to normalized same-origin `/admin/api` paths, and retries a 401 at most once. | The playbook requires re-prompting but does not define cancel, concurrent 401, or unsafe-path behavior. Separate generations let simultaneous stale responses share either one accepted replacement or one canceled attempt without a prompt storm; preserving the prior value permits a later recovery attempt without persistence; path validation and forced header replacement keep the token inside the intended admin boundary. |
| 2026-07-30 | Phase 7 static serving uses `@fastify/static` `^10.1.2` (locked at 10.1.2), a module-relative absolute `packages/dashboard/dist` default, explicit `/app/dashboard` in the flattened runtime image, API-first registration, and `dotfiles: "ignore"`. | The current plugin line supports Fastify 5 and supplies its own types. One default cannot describe both the workspace and flattened deploy layout, so Docker sets the override while local source/compiled workspace execution shares the module-relative default. Exact API routes must beat the static wildcard, and ignoring dotfiles closes the independently reproduced unauthenticated file exposure without disclosing whether a hidden path exists. |
| 2026-07-30 | Phase 7 pins `chart.js` exactly at 4.5.1 and `vite` exactly at 8.1.4 for the dashboard scaffold. | Chart.js 4.5.1 is the current stable release, while Vite 8.1.4 is already resolved and exercised by the repository's Vitest lock graph; exact pins satisfy the no-runtime-CDN and reproducibility contracts without adding another version family. |
| 2026-07-30 | The project owner explicitly approved Phase 6 completion and selected defect D1 Remedy A: fresh distinct-version and historical baseline comparisons enforce `--max-pass-rate-drop` with default `0.05`, exact-boundary scalar-EPS tolerance, and same-version zero-drop semantics, preserving the existing absolute dataset threshold and score-drop comparison rather than assigning artificial zero rubric scores to deterministic failures. | Both Phase 6 Verify arms are complete and evidenced, so the phase gate can close. Applying the band to both supported comparison modes closes D1 rather than leaving a historical bypass; comparing pass rates directly retains `score_avg` as the judge-only metric and catches the exact PR #6 shape where five new deterministic failures still produced a higher candidate score average and exit 0. |
| 2026-07-30 | The project owner approved the Phase 6 deterministic negative-control amendment: certified degraded v2 (`4f9969b7…`) is retired as the Phase 6 Verify negative control because live runs scored 42/50 (0.84) and 45/50 (0.90) and never tripped the 0.8 pass-rate floor, while its only red arm was a volatile ≤7-case rubric average that deterministic failures bias upward by pruning scored cases. On the disposable Verify branch only — never merged, deleted after screenshot — the checked-in fixture gains an uncertified third version whose system message pins the exact output `{"risk_level":"none","guidance":"No action needed."}` and whose user message does not reference `{{note}}` (the variable stays declared; the dataset still supplies it and the renderer permits unused supplied variables); `candidate` points at v3, `prod` stays at certified v1, and the v3 sha256 `b4191e04e77a0a2e0978c08dda03b202de018e1d35fccf51a8411faad5875004` is pinned in `ci-seed.ts` as `NEGATIVE_CONTROL_DIGEST` beside the two byte-unchanged certified digests. The fixture-validator widening to three versions (tuple, candidate-label union, `addSafetyVersion` 1\|2\|3, `ensureSafetyPrompt` bounds) is input validation at the checked-in-fixture trust boundary, not the protected DB/API/persistence schema. The expected fixed result from the test-pinned 24/10/16 label distribution is 16/50 = 0.32 pass rate, 34 named risk-label failures, and exit 1 via the dataset threshold alone; any constant output passes at most 24/50, and a live result ≥40/50 invalidates the control and requires redesign with no rerun. The disposable branch may update only the `ci-seed` test expectations required to keep ordinary `ci` green; every other approved invariant — dataset/hash, threshold 0.8, band 0.05, models, pacing, cache/retry/self-judge rules, exit codes, persistence, budget, credentials, and the one-invocation baseline-first workflow command — is unchanged. | The certified degraded v2's failure count is set by the target model's judgment rather than the prompt, so the Verify red arm was unreliable; PR #6 proved it can pass outright. Withholding the note makes the expected labels unreachable by any model behavior — compliant output yields exactly 16/50 and even total instruction-ignoring caps at the largest constant-label count 24/50 < 40 — so the red is deterministic, model-independent, and attributable to `eval-gate` alone while both certified Phase 5 artifacts and every locked gate contract remain intact. |
| 2026-07-30 | Phase 6 recorded defect D1 (remedy deferred until after Phase 6): the baseline↔candidate comparison reads only `score_avg`, whose denominator excludes deterministically failed cases because deterministic assertions short-circuit without a score, and pass rate is checked only against the absolute 0.8 floor with no baseline comparison. A candidate can therefore break deterministic behavior, *raise* its own `score_avg` by pruning its worst cases from the scored set, and pass. Demonstrated by PR #6: five new deterministic failures, `score_avg` 0.972 → 0.988 (0.9720000000000001 over five scored baseline cases versus 0.9879999999999999 over five candidate cases, drop −0.016), exit 0; corroborated by unstable scored counts (7, 6, then 5/5) across live runs. The owner must later choose between a pass-rate drop band and scoring deterministic failures as zero; no code change in this phase. | Recording the defect with its live evidence prevents the unexpectedly green PR #6 from being misread as a healthy gate while deferring the remedy keeps this amendment pass inside the approved negative-control scope; the deterministic v3 control does not depend on the score-drop arm, so Phase 6 Verify can complete before the owner selects the comparison fix. |
| 2026-07-29 | The project owner approved the Phase 6 same-version pairing amendment: when a fresh paired run's frozen baseline and candidate refs resolve to the same prompt id and version, the runner executes and persists exactly one run under the candidate ref, applies the normal dataset threshold, defines the score drop as zero, and prints an explicit line naming both refs and the shared version; every fresh pair also prints baseline/candidate score averages, both scored counts, and the computed drop after the unchanged per-case table. Distinct resolved versions retain baseline-first two-run persistence with the 0.05 band. Historical comparison, dataset/hash, the 0.8 threshold, models, pacing, cache/retry/self-judge rules, exit codes, schema, budget, credentials, and the workflow command are unchanged. | The seeded Phase 6 gateway leaves `prod` and `candidate` at the same certified v1, so the first live attempt to complete evaluation (PR #4) paired an identical version against itself: setup, pricing seeding, CI seeding, and cleanup passed, yet the run returned quality exit 1 with the candidate at 48/50 and score 0.881666667 over six scored cases — one 0.42 rubric disagreement and the known over-triage case — while the baseline's exact score was never emitted and cannot be recovered from the discarded ephemeral database. An identical-version pair measures only judge nondeterminism, so its 0.05 band can reject a good PR without any prompt change; running the shared version once removes that flake class without widening the band or altering any distinct-version comparison. |
| 2026-07-29 | A server response close is classified as `client_aborted` unless Node reports `writableFinished`; `writableEnded` is insufficient. Apply the same signal to cached replay and live streams. | Node sets `writableEnded` as soon as `end()` is called even when queued bytes are not flushed. The first Step 4 `master` CI run reproduced the Linux race by persisting `ok` after a cache-replay reset. `writableFinished` changes only immediately before successful `finish`, so it preserves normal completion while correctly retaining abort logging and budget reconciliation for close-after-end-before-finish. |
| 2026-07-29 | The project owner approved the Phase 6 stable-required-check amendment: keep `master`; trigger `eval-gate` for every pull request; compute relevance without secrets or repository code over the merge-base-to-head diff and an exact allowlist; default unresolved diffs to relevant; run live evaluation only for trusted relevant same-repository non-Dependabot changes; return green without providers for irrelevant changes and red without providers for relevant untrusted changes; expose an always-running aggregate `eval-gate`; name ordinary CI `ci`; and require both checks up to date for administrators without adding reviews. | GitHub can leave a required path-skipped workflow pending indefinitely. The detector/aggregate topology gives every PR one stable conclusion while withholding provider secrets and spend from irrelevant or untrusted work. Keeping the actual default branch avoids an unrelated rename, and the exact allowlist prevents unapproved cost expansion. |
| 2026-07-29 | Initial Phase 6 step 3 fork policy, superseded by the stable-required-check amendment above, rejected forks and Dependabot in the first no-secret step before checkout, actions, builds, or secret-bearing steps. Same-repository pull requests validated all four nonblank single-line secrets before runtime provisioning. | The initial design correctly protected secrets but could not supply a stable required result for unrelated path-skipped pull requests; the owner-approved detector/aggregate topology above replaces it while retaining provider-inert handling for untrusted relevant changes. |
| 2026-07-29 | The project owner approved Phase 6 Recovery A: `seed-ci` reserves an empty mode-`0600` `PG_EVAL_KEY_FILE` before the one-time key POST; first seed requires zero gateway keys, intact replay requires exactly one enabled `ci-evals` key with the exact locked budget/rate and an authenticating saved plaintext, and any interrupted or ambiguous POST-to-durable-handoff requires discarding and recreating the entire ephemeral gateway/database/handoff. No key rotation, API, DAO, migration, or schema expansion is authorized. | Exact single-key state binds the usable plaintext to the $1 key without exposing hashes or adding an identity endpoint. A pre-reserved handoff removes ordinary filesystem preflight failures before the remote mutation, while whole-run recreation is the only honest recovery from the irreducible cross-system atomicity gap. Prompt, label, and dataset repair remains idempotent within an intact pair. |
| 2026-07-29 | The project owner approved Phase 6 Option 1: require dedicated provider-side-capped CI credentials for OpenAI, Anthropic, Gemini, and DeepSeek while retaining the canonical one-target dataset and fresh paired `deepseek-v4-flash` target → `gpt-5.6-terra` judge gate. Anthropic and Gemini receive no Phase 6 eval traffic and their configured presence is not live adapter verification. | This is the narrowest interpretation consistent with the Phase 6 secrets step and GUIDE §7.4 workflow, preserves the certified dataset hash and score scale, keeps one gate near 25 minutes and about 3% of the $1 circuit breaker, and avoids an uncalibrated four-target expansion that would amend locked decision #11. Claude Opus / max independently recommended this interpretation; no Fable escalation was needed. |
| 2026-07-29 | Paired and historical `max-score-drop` comparisons share one scalar tolerance of `Number.EPSILON × max(1, abs(baseline), abs(candidate), abs(maximum))`; exact-boundary drops pass, while a one-billionth excess remains red. Do not use the scored-count-scaled aggregate-readback tolerance for this gate. | Checkpoint B1 reproduced `0.9 - 0.85` as `0.050000000000000044`, which falsely exceeded an allowed `0.05` drop under the raw comparison. All inputs are constrained to `[0, 1]`, so one scale-adjusted machine epsilon corrects only scalar representation noise without widening the quality band based on case count. |
| 2026-07-29 | The project owner authorized one fresh retry of the blocked degraded Phase 5 pair by instructing the orchestrator to start Phase 5 verification. Preserve the existing weakened candidate v2 and every fresh-pair, DeepSeek/Terra, pacing, no-history, no-cache, no-retry, $1-key, persistence, durability, and exit-code rail; do not start a second paid invocation. | The prior separately fresh degraded attempt stopped with a sanitized one-off DeepSeek provider error during its v1 baseline and never reached candidate v2, so it provided no dataset-discrimination result. Repeating the same bounded pair after all mandatory offline and exact-image Docker gates were freshly green was the narrowest continuation of the requested verification. |
| 2026-07-29 | Permanently reserve historical key ID 22 as a zero-budget, RPM-1, immediately disabled tombstone; use newly generated ID 23 as the only active $1 good-Verify key and disable it in a signal-aware `finally`. | The lost WAL returned the durable maximum ID to 21, and `api_keys.id` is `INTEGER PRIMARY KEY` without `AUTOINCREMENT`. A normal create would therefore recycle ID 22, contradicting the owner-approved never-reuse rail and making the new evidence ambiguous. The admin-only tombstone produced no provider request, retained no plaintext, and avoided direct SQLite or schema/API changes. |
| 2026-07-28 | The project owner approved the Phase 5 SQLite WAL lifecycle amendment: retain WAL and the bind mount; await one shared SIGTERM/SIGINT Fastify shutdown; validate a TRUNCATE checkpoint and close in `finally`; prohibit host SQLite access while live; add 50-result, signal/main-file, and no-provider Docker restart gates; preserve every DeepSeek/Terra, dataset, pacing, cache, retry, $1, persistence/exit, and Phase 6 contract; then authorize exactly one new fresh good Verify with a new key after every gate is green. | Key 22 and its request rows existed in the running gateway but vanished after stop/restart. The production entrypoint had no signal path to Fastify `onClose`, and prior Phase 3 evidence already identified Docker Desktop/VirtioFS bind-mounted WAL hazards. A paid retry before proving checkpointed durability would repeat an infrastructure risk. |
| 2026-07-28 | Eval request and DAO readback validation share a scored-count-scaled IEEE-754 tolerance for `score_avg`; nullability, case/pass counts, and integer micro-USD totals remain exact. | The owner-approved 50-result gate reproduced a pre-existing rollback: seven valid scores differed by 3.33e-16 when SQLite's case-ID order changed floating-point addition order. Scaling one machine epsilon by the seven scored results accepts only that rounding class; a 1e-9 mismatch remains rejected. This is the smallest persistence correction and imposes no new result-order convention. |
| 2026-07-28 | The project owner approved the Phase 5 Terra temperature amendment: omit the temperature field only for `gpt-5.6-terra`; retain `reasoning_effort: "high"`, JSON-object output, immutable `judge_rubric_v1@1`, no cache/retries/self-judge, DeepSeek target temperature zero, all 50 cases/seven rubrics, fresh paired runs without history reuse, 15-second pacing, $1 keys, persistence and exit-code contracts, and Phase 6 unchanged. | An authorized replacement key listed Terra, DeepSeek completed the first target call, and the first Terra rubric request still recorded sanitized `provider_error`. OpenAI classifies Terra as a reasoning model, documents high reasoning and structured output support, and documents reasoning-model temperature changes as unsupported in its model-grader constraints. Because PromptGate intentionally discards the upstream status/body, temperature incompatibility remains the narrowest evidence-backed inference rather than a captured upstream fact. |
| 2026-07-28 | The project owner restored `gpt-5.6-terra` as the independent high-effort judge of sole target `deepseek-v4-flash`. Both literal Phase 5 Verify commands must freshly pair `--baseline prod` with the candidate and must omit `--baseline-from-history`. Terra judge requests then used temperature zero, `reasoning_effort: "high"`, immutable `judge_rubric_v1@1`, JSON-object output, and no cache. The newer Terra temperature decision above supersedes only that zero-temperature clause; every other invariant in this row remains active. Preserve all 50 cases/seven rubrics, no eval retries/self-judge, $1 keys, persistence/table/exit/direct-bin contracts, hardened general history support, general provider support, and Phase 6 unchanged. | Baseline run ID 1 was scored by Gemini, while current `eval_runs` rows do not record judge identity. Reusing that score for a Terra-judged candidate would make the 0.05 regression comparison invalid even though its dataset/prompt/model identity matches. A fresh within-command pair produces comparable scores without a DB/schema/API change; the earlier persisted-baseline live exception is superseded for Phase 5 only. |
| 2026-07-28 | The project owner approved the Phase 5 persisted-baseline quota path: accept completed baseline run ID 1 from code HEAD `a77b9cf` as current live baseline evidence, and run each remaining good/degraded candidate with `--baseline prod --baseline-from-history --min-request-interval-ms 15000` on separate fresh Gemini quota days. History acceptance must validate the current dataset slug/exact hash and case count, frozen baseline prompt ID/ref/version, and exact target model before dataset upsert, provider traffic, or any admin mutation; malformed, missing, or mismatched history is sanitized infrastructure exit 2 with no such side effects. Never reuse the stopped, unpersisted partial candidate. Preserve the 50 cases, seven rubrics, DeepSeek target/Gemini judge topology, no eval retries/cache/self-judge, $1 keys, persistence/table/exit/direct-bin contracts, general provider support, and Phase 6 unchanged. | Sol's artifact audit verified run ID 1 as the exact completed current baseline: `safety_screen@prod`, prompt ID 3/version 1, current frozen dataset hash, `deepseek-v4-flash`, 50 results, 50 passes, score 1.0, and all seven rubric scores 1.0. Reusing that persisted evidence reduces each remaining command from at most 14 to at most seven Gemini judgments without changing candidate execution, comparison semantics, or the fresh paired Phase 6 gate. |
| 2026-07-27 | The project owner approved a DeepSeek-led Phase 5 gate: `deepseek-v4-flash` is the sole target and `gemini-2.5-flash` is its independent judge only. Preserve all 50 cases and seven rubrics, paired baseline/candidate execution, no cache/retries/self-judge, $1 key discipline, persistence/table/exit contracts, and general gateway support. Use `--min-request-interval-ms 15000`; execute good and deliberately degraded commands on separate fresh Gemini daily-quota days. | The paced two-target attempt still recorded six Gemini successes, two DeepSeek successes, then a seventh Gemini logical request failed with upstream 429 at about 42 seconds; local key RPM remained 1000, no eval run persisted, disposable key was disabled, and spend was $0.006040. A sole DeepSeek target preserves independent Gemini judgment while keeping one paired command at at most 14 Gemini rubric calls, below Gemini RPD20; the two commands would otherwise require 28. Phase 6 remains its separate four-provider requirement. |
| 2026-07-27 | The project owner approved an opt-in Phase 5 request pace: CLI-only `--min-request-interval-ms` defaults to `0`, ignores ambient environment values, and shares one queue per target/judge model across baseline and candidate calls. The literal Verify uses `6500` ms and waits 65 seconds unconditionally between its good and deliberately degraded commands. | The bounded attempt recorded seven Gemini and two DeepSeek logical successes, then Gemini returned provider-error 429. Seven recorded Gemini successes plus the failing logical request's documented three-attempt adapter path infer ten upstream Gemini attempts in about 21 seconds. The local eval key remained at RPM 1000, no eval run was persisted, and disposable key 17 was disabled after 7,283 micro-USD spend. Per-model pacing keeps the existing no-retry decision, avoids coupling Gemini and DeepSeek, and changes no cache, budget, dataset, model, or exit behavior. |
| 2026-07-27 | The project owner approved replacing every Phase 5/CI `pnpm --filter @promptgate/evals exec pg-eval` invocation with the repo-root `./node_modules/.bin/pg-eval`. Subprocess regressions now require the source-backed direct bin to preserve infrastructure status `2` and help status `0`. Named checked-in datasets resolve relative to the eval package/module, while explicit existing and explicit YAML paths retain their caller-relative behavior. | The first committed Phase 5 Verify attempt reached no provider: filtered pnpm changed the working directory and produced `packages/evals/packages/evals/datasets/...`, while pnpm 10.33 also converted the CLI's correct infrastructure status `2` into outer status `1`. The direct workspace bin preserves the documented `0` quality-pass / `1` quality-fail / `2` infrastructure contract, and package-relative slug resolution makes source, built, repo-root, and package-working-directory execution consistent without changing CLI/API/schema/model behavior. |
| 2026-07-27 | The project owner amended decision #11 and the Phase 5 gate to exactly two target models in canonical order: `gemini-2.5-flash`, then `deepseek-v4-flash`. Rubric checks cross providers: DeepSeek judges Gemini output and Gemini judges DeepSeek output. The target list and map are runtime-frozen; every other, missing, duplicated, or reordered provider list fails before prompt-summary reads, admin mutation, persistence, or provider traffic. Both target and judge calls use temperature zero, immutable `judge_rubric_v1@1`, JSON-object judge output, and no cache; judge calls omit provider-specific reasoning effort. The general gateway/client retains OpenAI, Anthropic, Luna, and Terra transport support. | Cross-provider judging avoids the weak self-judge path while using the two provider keys the owner can currently verify. Exact pre-traffic validation prevents a partial or silently narrowed gate. This amendment is Phase 5-only: it removes the obsolete OpenAI/Anthropic Phase 5 credential blockers without removing their future gateway support or waiving Phase 6's separate four-provider requirement. |
| 2026-07-27 | The project-owner-reviewed golden dataset contains exactly 50 stable cases: 2 exact `observed-input` probes, 17 `policy-derived` cases covering every immutable urgency term, and 31 `fable-synthetic` cases from the captured Claude Fable 5 / high batch. Every case declares `is-json`, an exact case-ID-to-`risk_level` assertion, and a source-matched direct-diagnosis assertion over every JSON string. Seven owner-approved representative boundary cases additionally declare one rubric. The final labels are 24 `urgent`, 10 `review`, and 16 `none`. | Exact provenance prevents derived cases from becoming invented observations; deterministic-first checks make failures actionable and avoid judge calls for structural, label, or direct-diagnosis failures. Seven rubrics retain semantic coverage across observed, urgent, review, idiom, and scary-word boundaries while bounding the locked judge's cost. The project owner explicitly approved every label and A1/A2 assignment, including all four audited edits. |
| 2026-07-27 | Preserve the raw Fable JSON byte-for-byte and exempt only that exact evidence file from Biome formatting while continuing to parse it in tests. Allow 15 seconds for the three source-backed pnpm CLI subprocess regressions; their assertions and product behavior are unchanged. | Formatting would invalidate the recorded raw-file and normalized-output digests. The CLI probes synchronously start pnpm and Node's source TypeScript loader; once the suite added its 52nd test file, cold/concurrent runs could exceed Vitest's generic 5-second default even though the subprocess completed correctly. A bounded per-test timeout preserves hang detection and eliminates a load-dependent false red without changing runtime code. |
| 2026-07-26 | Applied the project-owner-approved Phase 5 step 7 provenance correction: the golden dataset is seeded from immutable carematch `server/safety.js` at public commit `d22bf9d798ed22b77690a02a02a9284494ca188c` and exact retained 2026-05-23 probes. There is no verifiable six-case test file in the audited sources; every case has one input provenance class (`observed-input`, `observed-validation`, `policy-derived`, or `fable-synthetic`) and may cite separate assertion evidence such as the retained unsafe output. Derived cases may not be represented as verbatim originals. | The local archive contains no carematch repository, the public repository has only one root commit and no tests, and the retained source-building session recovers two valid symptom inputs, one invalid whitespace input, and one unsafe output—not six input cases. Explicit provenance preserves decision #12's carematch policy source, roughly 50-case scope, Fable expansion, and mandatory human label review without inventing evidence. |
| 2026-07-26 | Evaluation persistence uses migration `005_evals.sql`, a DAO-only transactional run/results write, authenticated admin dataset/run/history/detail endpoints, and strict Zod validation of every request, stored JSON value, aggregate, prompt-attribution pair, and confirmation response. The runner derives a stable dataset slug from the explicit YAML basename, freezes label refs once and calls the gateway with numeric refs, runs paired baselines before candidates, records target plus judge micro-USD cost, and selects the newest historical baseline matching the exact dataset slug/hash, prompt ref, and model before any mutation or provider traffic. Local runs record `manual`, GitHub Actions records `ci`, and `--allow-cache` records `manual` while applying to both target and judge calls. A Vitest global setup builds shared/evals once before parallel files. | Atomic persistence prevents partial run evidence; route/DAO separation and hostile-JSON normalization keep SQL and untrusted data behind validated boundaries. Numeric freezing makes every case in one run reproducible, exact historical matching prevents stale comparisons, combined cost makes the gate economically truthful, and baseline-first execution preserves paired ordering. One pre-suite build removes concurrent `dist` deletion races without weakening production behavior or hiding test failures. |
| 2026-07-26 | The rubric evaluator calls the immutable `judge_rubric_v1@1` registry version through the normal eval gateway client, locked to `gpt-5.6-terra`, high reasoning, temperature zero, no cache, and JSON-object output. Candidate, rubric, case description, and de-identified variables travel only inside one JSON-serialized `payload`; case IDs are omitted. Judge output is the strict object `{pass, score, rationale}`, with finite score in `[0,1]`, nonblank rationale capped at 1,000 characters, and no raw gateway or payload causes retained. The checked-in prompt fixture carries a deterministic content digest in its stored description; the seeder accepts only matching immutable v1, repairs only a matching zero-version partial seed, and fails closed otherwise. | A fixed numeric prompt ref keeps the judge reproducible for a whole eval run and prevents label drift. One-pass serialized data plus explicit non-echo instructions limits prompt-injection and disclosure risk while retaining the context needed for scheduling-versus-safety judgments. The digest marker provides safe idempotence under the documented trusted-admin, single-tenant model without adding a new prompt-version read endpoint; strict output and sanitized infrastructure errors preserve exit-code 2 semantics. |
| 2026-07-26 | Applied the project-owner-approved Phase 5 temperature amendment: every pinned eval model uses `temperature: 0` except `claude-sonnet-5`, whose Anthropic path receives no temperature field; the locked `gpt-5.6-terra` judge remains at zero. The eval client accepts exactly the five pinned target/judge models, uses the canonical shared chat schemas plus shared eval-specific Zod policy, always sends an empty caller message list, `stream: false`, `pg_feature: "eval"`, and no-cache by default, and permits cached responses only through the explicit local option with zero-cost hit enforcement. Source execution loads the tracked shared schema source directly only inside the monorepo; built and installed execution resolves the compiled shared subpath. | Omitting Sonnet's unsupported override is the smallest authority change that preserves the approved four-provider dataset without weakening fail-loud translation. Central shared policy prevents wire-shape drift; descriptor-based data-only variable snapshots reject getters, `toJSON`, proxies, cycles, sparse arrays, symbols, non-finite values, and exotic prototypes while preserving own prototype-like JSON keys. The dual source/compiled seam keeps the documented clean-checkout CLI executable without asking Node 22 to type-strip TypeScript inside an installed `node_modules` package. |
| 2026-07-26 | The assertion registry exposes exactly the eight documented types. It executes deterministic checks first in declaration order, stops on the first quality failure, then executes rubric checks last and also stops on the first failed rubric. Only rubric checks contribute a normalized case score; every rubric result is a strict `{pass, score, detail}` object with a finite score in `[0,1]`. Invalid regexes/schemas, async schemas, broken JavaScript modules/results, absent judge injection, and malformed rubric results are typed infrastructure errors. JSON Schema uses direct Ajv; first-party JavaScript assertions load resolved paths through file URLs and receive a deeply frozen, secret-free case context. | Deterministic short-circuiting prevents unnecessary judge spend, and rubric short-circuiting preserves the same cost-control invariant when a case declares multiple judges. Keeping configuration/runtime faults separate from quality failures preserves exit-code 2 semantics. Mandatory normalized rubric scores make the later 0.05 score-drop comparison meaningful; file URLs preserve valid platform paths without changing the documented in-process first-party-code trust model. |
| 2026-07-26 | Evaluation YAML is hashed from its exact source bytes, then validated as a strict promptfoo-compatible subset. Explicit case IDs are immutable and duplicate explicit IDs fail; generated description slugs reserve every explicit ID, remain at most 120 characters, and receive deterministic numeric suffixes with warnings on collision. JSON-safe variables and assertion values retain own prototype-like keys without accepting non-finite, cyclic, sparse, symbolic, or exotic-prototype values. JavaScript assertions resolve only dataset-relative regular files. | Exact hashes make persisted runs reproducible; stable explicit IDs protect the `eval_results` primary key across runs; deterministic generated IDs keep authoring convenient without silently rewriting authored identities. Returning original validated JSON objects preserves the Phase 4 prompt-variable contract, while a discriminated assertion union and regular-file resolution close the YAML/file trust boundaries before execution. |
| 2026-07-26 | The `pg-eval` package bin points to tracked `src/cli.ts`, and the workspace root owns a `workspace:*` development link to expose that bin to the playbook's exact filtered `pnpm exec` command. The eval package explicitly owns Node typings and clears stale/test build output. | A package's own bin is not added to its filtered pnpm execution path, while an ignored `dist/cli.js` target fails in a clean checkout. Node 22's supported erasable-TypeScript execution plus the root workspace link makes the literal command source-backed before build; a subprocess regression and a manual no-`dist` run lock that behavior. |
| 2026-07-26 | Phase 4 resolves body/header prompt extensions per field with explicit body precedence; `x-pg-vars` is JSON parsed and Zod-validated. `pg_vars` remains a broad plain JSON record but preserves every own string key, including `__proto__`; only own string values interpolate, without coercion. Missing or non-string required declarations return `prompt_var_missing` in declaration order, while optional/undeclared misses remain literal. Only string message `content` renders; multimodal arrays, null content, roles, names, and loose fields remain unchanged. | This preserves the established loose extension contract while honoring the shared renderer's strict string input and one-pass semantics. A custom plain-record Zod boundary plus a null-prototype renderer map prevents prototype setters/inherited values from changing prompt text. Per-field precedence lets clients mix body and header forms safely; preserving non-string message structures avoids corrupting images, tool history, or provider-specific fields. |
| 2026-07-26 | Prompt resolution now runs after provider/pricing and before reservation/cache; the resolved, prepended message body drives budget estimation and cache identity, while every `pg_*` field is removed before the adapter seam. Once a ref resolves, `(prompt_id,prompt_version)` is a validated positive-integer pair on every later durable request outcome. Corrupt stored registry JSON returns a safe 500 `provider_error` with attribution and no cache, budget, or provider dispatch. | This is the fixed Phase 4 pipeline order and makes rollback/cache behavior depend on actual served content, not a mutable label string. Pair validation prevents partial attribution; recording resolved failures as well as successes keeps operational diagnosis tied to the concrete immutable version without leaking database content. |
| 2026-07-26 | Prompt admin bodies use JSON arrays under the documented `messages_json` and `variables_json` field names; routes return snake_case persistence-shaped data and normalize duplicate slugs, absent prompts, and absent versions to stable safe errors. Version diffs recursively sort object keys by JavaScript code-unit order, pretty-print with two spaces, and use `diff@9.0.0` to emit a plain-text two-file line patch. | The playbook examples send arrays rather than serialized JSON strings, and one accepted form keeps the trust boundary unambiguous. Stable code-unit ordering makes equivalent object insertion orders diff-identical across locales; the repository-pinned zero-dependency package supplies the required maintained unified-diff implementation and its own TypeScript declarations. |
| 2026-07-26 | Registry refs contain exactly one non-edge `@`; a positive safe-integer target resolves an immutable version and every other non-empty target resolves as a label. Every successful `setLabel` call writes history and refreshes `updated_at`, including an intentional same-version deployment (`v → v`). | Exact parsing rejects ambiguous or unsafe numeric refs without constraining future label text. Recording same-version calls preserves an append-only audit of deployment intent instead of silently hiding an operator action; the label pointer and its history row remain one transaction, including rollback if history persistence fails. |
| 2026-07-26 | Template substitution matches the exact text between `{{` and `}}`, reads only an own string-valued property, reports missing names once in left-to-right first-use order, and preserves an unresolved placeholder in the returned text. `\{{...}}` removes the escape slash and remains literal; neither escaped text nor replacement values are recursively rendered. | Deterministic de-duplicated misses produce stable client diagnostics, own-property lookup prevents prototype-chain surprises, one-pass rendering prevents a supplied value from becoming executable template syntax, and preserving a miss avoids silently deleting authored prompt text before the Phase 4 resolver can fail safely. |
| 2026-07-26 | Cached SSE replay preserves the authored three-frame protocol but supplies each already-framed buffer to Node in 16 KiB shared subarray views. | A multi-megabyte completion supplied as one source buffer can become `writableEnded` before a receiving client resets, making the abnormal-close path transport-timing-dependent. Bounded writes let normal HTTP backpressure surface disconnects before clean completion; `Buffer.subarray` preserves exact bytes without payload copies, and ordinary small frames remain one write. |
| 2026-07-26 | Phase 3 header fallbacks accept `x-pg-feature` as the same non-empty free-text value as body `pg_feature` and `x-pg-no-cache` as exactly lowercase `true` or `false`; an explicitly supplied body field wins and its header is ignored. `x-pg-prompt` and `x-pg-vars` remain at the documented Phase 4 prompt-resolution insertion point. A synthetic streaming cache replay installs the same idempotent abnormal-close logging/reconciliation fallback as a live stream. | Strict fallback parsing validates the HTTP trust boundary without ambiguous truthy coercion, while body precedence preserves the primary API contract. Limiting this step to the extensions Phase 3 can execute avoids starting prompt-registry work early. Cached streaming is still streaming to the client: if its socket resets and Fastify skips `onResponse`, directly finalizing the already-metered zero-cost cache hit prevents both a missing audit row and a permanently stranded budget reservation; insert failure still becomes fail-closed debt. |
| 2026-07-26 | Budget admission uses the validated forwarded message-content character count with `ceil(chars/4)` input tokens and the request's positive `max_tokens` or positive configured default for output. It prices ordinary input and output components independently with the same integer `Math.round` convention as durable metering. Each server owns one guard with a 250 ms per-key settled-spend memo, synchronous active reservations, and permanent fail-closed debt after a log-insert failure; durable inserts and successful admin PATCHes invalidate only settled memo state. | Matching the existing metering convention avoids reserving a micro-USD for a component that would persist as zero, while the estimate remains explicitly best-effort rather than a tokenizer or billing upper bound. A tiny memo limits repeated SQLite sums without weakening concurrent admission: active reservations and retained debt are always counted, invalid reconciliation leaves capacity closed, and every durable outcome forces the next admission to re-read current-month spend. |
| 2026-07-26 | Rate limiting applies only to authenticated chat completions, after request-log initialization and before body parsing. Each server keeps one per-key token bucket; `Retry-After` is the minimum whole-second delay rounded up with a one-second floor, and 429s use `code: rate_limited`, `type: rate_limit_error`, and audit status `rejected_rate_limited`. A changed RPM is observed from auth on the next request: the bucket first refills under its prior rate, then clamps existing credit to a reduced capacity or preserves it under an increased capacity without minting a fresh burst. | The fixed pipeline order requires authenticated rejected calls to retain request identity while preventing parsing, cache, or provider work. Whole-second ceiling matches HTTP `Retry-After` semantics. Preserving rather than resetting credit makes admin changes immediate without allowing repeated RPM toggles to manufacture burst capacity. |
| 2026-07-26 | Successful non-streaming responses without provider usage are cached with the JSON literal `null` in `usage_json` and may be replayed only non-streaming with estimated token accounting and zero cache-hit cost; streaming reads and writes require exact terminal provider usage. Streaming writes use a strict lossless assembler: incoherent ordering, incomplete tool calls, or any provider field it cannot preserve bypasses caching without interrupting the authoritative live stream. | Missing usage is an allowed provider-hiccup response shape but cannot support a truthful synthetic terminal usage frame. Separating non-stream reuse from exact-usage stream reuse preserves useful cache behavior without inventing provider facts; failing cache assembly closed prevents a cached replay from changing client-visible semantics. |
| 2026-07-26 | Cache identity is guarded by the complete request hash plus `cache_entries.model`; a provider-returned canonical/snapshot response model is preserved rather than forced to equal the routed alias. `usage_json` is validated separately and must canonically match response usage when both exist; cache-hit logs reuse provider-aware token normalization but force cost to zero. Synthetic replay preserves representable top-level, choice, message, tool-call, and usage fields in one delta frame, one usage frame, then `[DONE]`. | Providers may legitimately return a canonical model identifier different from the requested alias, while the request hash/table model already prevent cross-model reuse. Separate usage validation prevents ambiguous metering; the existing meter retains Gemini hidden-thinking semantics; loose-field preservation keeps cached streaming from silently discarding compatible provider data. |
| 2026-07-26 | Applied the project-owner-approved Phase 3 budget/pipeline amendment: validate, resolve pricing, and resolve future prompt content before estimating a reservation; meter before writing a priced cache entry; hold every reservation through durable request logging and reconcile only afterward; retain fail-closed in-memory debt on logging failure. The documented chars/4 reservation remains a concurrency-safe single-process circuit breaker, while provider-side spend limits are the absolute monetary wall. | The prior fixed order attempted budget calculation before the validated request, rate, and resolved prompt existed, and attempted to cache a priced response before metering it. Chars/4 is not a tokenizer upper bound, so reserve/reconcile prevents parallel admission races but cannot honestly prove an absolute provider bill cap. The amendment preserves the bounded estimator and burst protection without overclaiming its guarantee. |
| 2026-07-25 | Accept one validated terminal usage-bearing compatible-stream frame in either observed form: OpenAI's separate empty-choice usage chunk, or a combined Gemini/DeepSeek frame whose every nonempty choice carries a supported non-null `finish_reason`. The frame remains strictly terminal before one buffered `[DONE]` and clean EOF; duplicate/missing/malformed usage, unfinished choices, or later data still fail closed. | The authorized Phase 2 captures showed Gemini collapsing role, visible content, finish, and usage into one frame and DeepSeek attaching usage to its final finish choice despite `stream_options.include_usage=true`. Requiring empty choices rejected both current providers; requiring every combined choice to be finished preserves the terminal invariant without broadly accepting mid-stream usage. |
| 2026-07-25 | Expose streamed request metering through an authenticated, owner-scoped `GET /v1/requests/:request_id/usage` response containing only the request UUID, model, streamed flag, nullable input/output/cost, estimated flag, and status. Validate and canonicalize the UUID at the route boundary, enforce `request_id AND api_key_id` in one DAO query, collapse malformed/unknown/legacy/cross-owner misses to one 404, and mark every authenticated route outcome `private, no-store`. | A response UUID is intentionally visible, so possession alone cannot authorize metering access. Composite SQL ownership avoids fetch-then-check disclosure; one non-cacheable miss path prevents both an ownership oracle and intermediary cache poisoning, while nullable fields avoid representing incomplete metering as zero. |
| 2026-07-25 | Treat a streaming socket reset as its own idempotent pipeline outcome: register the documented guarded `request.raw` close listener plus a guarded response-side close listener before upstream invocation, abort the single upstream controller, close an abnormally exited iterator exactly once, and persist `client_aborted` directly through the shared logging seam when Fastify skips `onResponse`. Retain exact terminal usage when already observed; otherwise estimate prompt plus every forwarded choice's visible text with chars/4 and mark the cost estimated. | A POST request body can finish before its response stream disconnects, and Fastify does not guarantee the normal post-response hook after a reset. Explicit outcome and logging guards prevent false normal-close aborts, duplicate rows, leaked upstream generators, provider-error misclassification, and unmarked estimated billing. |
| 2026-07-25 | Implemented Anthropic streaming as a native named-SSE state machine that reuses the Phase 2 step-2 request translation plus `stream: true`, emits stable OpenAI role/text/tool-call chunks, accepts one or more cumulative `message_delta` events, maps the supported stop reasons, emits exact terminal usage, and withholds finish/usage/`[DONE]` until a clean EOF. Tool inputs must start with the documented empty placeholder, retain every partial JSON fragment on the wire, and concatenate to a valid object at block close; duplicate IDs, thinking, cache/server-tool/container accounting, malformed refusal metadata, invalid lifecycle events, and unpriced native fields fail closed. | Anthropic is not OpenAI-wire-compatible, so forwarding native bytes would corrupt clients and metering. A strict Zod-validated event machine preserves incremental delivery while preventing malformed tool calls, semantic loss, unaccounted usage, or a premature success signal. Removing the completed provider's obsolete 501 stub leaves all four built-in adapters on the same streaming seam without dead Phase 2 step-3 behavior. |
| 2026-07-25 | Implemented OpenAI-compatible streaming through a validated incremental SSE seam for OpenAI, Gemini, and DeepSeek: force and merge `stream_options.include_usage`, preserve all other compatible fields, parse arbitrary UTF-8/SSE boundaries, validate each OpenAI chunk, require terminal usage, buffer `[DONE]` until clean EOF, reframe multiline payloads correctly, meter provider-specific cache/hidden-thinking usage, and persist streamed timing/cost after completion. A narrow explicit EOF exception accepts only a complete line-terminated pending `[DONE]` because the committed official-contract fixtures use that framing. | The gateway must forward promptly without buffering the full response while still failing closed on malformed or misleading terminal streams. Buffering only the sentinel prevents a client from seeing success before a later contract violation; an injectable monotonic clock plus a one-millisecond integer floor preserves the literal `first_token_ms < total_ms` gate when both observations round to the same millisecond. Arbitrary compatible request fields remain passthrough, while the fields PromptGate merges or meters are Zod-validated. |
| 2026-07-25 | Implemented the documented Anthropic non-streaming compatibility subset as a native allowlisted translator: system/developer extraction, required token defaults, current structured outputs, stop/sampling/effort controls, user text/images, and the tool definitions/calls/results required by web_builder_llm. Explicitly disabled Sonnet 5 adaptive thinking; malformed or unsupported subset requests fail as safe 400s, while malformed upstream identities, blocks, stop reasons, tool coherence, or nonzero cache-token usage fail closed as safe 502s. | Current Anthropic contracts differ materially from the OpenAI wire shape, and the external dogfood client requires native tool translation. Explicit subset handling prevents quiet caller-semantic changes, signature-bearing thinking loss, malformed tool histories, response-body leakage, and cache-token underbilling. Arbitrary OpenAI-only extras remain outside the authority-defined Anthropic subset rather than being represented as passthrough parity. |
| 2026-07-25 | Applied the project-owner-approved Phase 2 fixture-timing amendment: step 1 authors official-contract fixtures for all four providers entirely offline with `live_capture_pending`; configured-provider live captures replace them only during the final Verify window, while missing-key providers remain pending. | Phase 2 step 1 originally required early live captures, but the higher-authority orchestration safety rail permits live calls only in phase Verify blocks and eval runs. Claude Opus 4.8 / xhigh independently confirmed the conflict and recommended amending the lower-authority playbook without weakening the safety rail. |
| 2026-07-25 | The project owner approved independently recorded official published-rate reconciliation plus persisted integer micro-USD rows as the Phase 1 cost evidence in place of provider account-dashboard artifacts. The Phase 1 acceptance table and Verify wording now state this exact requirement. | Both live calls expose sufficient usage to reproduce exact component-rounded cost, while the API keys expose no per-request billing-console artifact and browser site-use settings prevent dashboard inspection. The approved evidence remains stricter than a four-decimal display alone because it preserves exact integer micro-USD amounts and calculation inputs. |
| 2026-07-25 | Normalize billable/logged Gemini output tokens to `usage.total_tokens - usage.prompt_tokens`, while forwarding the provider's original compatible response unchanged; OpenAI and DeepSeek continue using `completion_tokens`. Reject usage where `total_tokens < prompt_tokens + completion_tokens`. | The first current-image live Verify returned Gemini usage `{prompt: 3, completion: 2, total: 28}`: the compatibility response's visible completion omitted 23 hidden thinking tokens that Google bills at the output rate. Provider-specific normalization produces the official candidate-plus-thinking billable count without double-counting DeepSeek/OpenAI reasoning, whose completion totals already reconcile. |
| 2026-07-25 | Applied the owner's pre-verify model correction by retaining `gemini-2.5-flash-lite` and adding `gemini-2.5-flash` as the Gemini verification/eval target at the approved standard rates: 300000 ordinary input, 30000 cached input, and 2500000 output micro-USD/Mtok. Validated Gemini's compatible `prompt_tokens_details.cached_tokens`, reconciled it with DeepSeek's explicit pair when both appear, and derived uncached input for independent three-component rounding. | Keeping Flash-Lite avoids deleting a valid supported model or consuming a migration solely for cleanup. Official standard pricing differs materially from Flash-Lite, so a new exact row and cache-aware wire/meter path are required. Direct bounded calls returned HTTP 200 for the owner's Gemini and DeepSeek keys with the requested model IDs; the gateway phase verify remains pending. |
| 2026-07-25 | Completed the four-provider routing seam by requiring prefix and current pricing provider to agree, default-wiring OpenAI/Gemini/DeepSeek while leaving Anthropic for Phase 2, and keeping `/v1/models` sourced only from current pricing regardless of provider-key availability. Pinned `openai@6.49.0` as a root dev dependency for the literal SDK verification. | Provider/pricing agreement prevents a model from being routed under another provider's rate. Optional keys still permit boot and model discovery; only a call to an unconfigured provider fails. Offline default-wiring, model-list, mismatch, and exact Gemini/DeepSeek metering/logging tests prove the integration without live provider traffic. |
| 2026-07-25 | Implemented Gemini as a provider-specific wrapper over the compatible core, preserved the documented `extra_body.google` envelope verbatim, and made HTTP 408 an adapter-local addition to the shared 429/5xx retry schedule. | Google's official compatibility and troubleshooting contracts support Bearer auth at the `/v1beta/openai/chat/completions` endpoint and treat 408 as transient. A configurable extra-status list keeps the common retry implementation without silently widening OpenAI/DeepSeek behavior; fake-fetch tests prove both the opt-in and default-off paths. |
| 2026-07-25 | Extracted a narrow configurable OpenAI-compatible HTTP core for the officially compatible OpenAI and DeepSeek wrappers; provider-specific modules still own endpoint, credential, name, and error identity. Added DeepSeek's documented `insufficient_system_resource` finish reason to the shared validated response contract. | This removes duplicated auth/stripping/retry/schema logic without pretending every provider is interchangeable. Unknown caller fields, including DeepSeek `thinking`, remain verbatim passthrough; successful responses still fail loud at the Zod boundary. Sol / xhigh found the provider-specific finish-reason gap, which was fixed with adapter and shared-schema regression tests before approval. |
| 2026-07-25 | Applied the project-owner-approved decision #2 amendment from two to four providers: OpenAI, Anthropic, Gemini, and DeepSeek. Phase 1 adds DeepSeek/Gemini non-streaming in steps 10–12; Phase 2 provides streaming parity across all four. Phase verify blocks call every configured implemented provider and name missing-key activation checks as deferred, never live-green. | The owner currently has workable Gemini/DeepSeek credentials but not OpenAI/Anthropic credentials. This preserves forward support without blocking current gateway proof; it does not amend the locked `gpt-5.6-terra` judge, so a working OpenAI key remains a future Phase 5 prerequisite rather than a Phase 1 blocker. Fable 5 / high was attempted but unavailable because of usage credits; GPT-5.6 Sol / xhigh supplied the technical expansion audit before human approval. |
| 2026-07-25 | Added nullable `model_pricing.cached_input_micro_usd_per_mtok` in migration 003, renumbered the still-uncommitted registry/eval migrations to 004/005, and defined validated DeepSeek cache usage plus three-component rounding (cache-hit input, cache-miss input, output). Older pricing JSON may omit the new field and normalizes it to `null`. | DeepSeek publishes separate cache-hit and cache-miss input prices, so one input rate cannot produce exact cost. A new additive migration upgrades both fresh and already-applied databases without rewriting 001/002; independently rounded integer micro-USD components preserve the approved no-floating-money rule. |
| 2026-07-17 | Made `GET /v1/models` return the current official OpenAI list envelope with one deterministically ordered row per model that has a currently effective price; `owned_by` comes from the latest effective provider row and `created` is stable Unix seconds from the earliest pricing date. | Official OpenAI OpenAPI was checked at build time; sourcing the endpoint exclusively through a DAO keeps provider keys optional, avoids network calls, excludes future-only routes, and prevents price-history duplicates. Claude Sonnet 5 / high owned the implementation. |
| 2026-07-17 | Applied the human-approved Fable 5 / high correction by adding nullable, uniquely indexed `requests.request_id` in `002_request_identity.sql`, enforcing UUIDs for every gateway DAO insert, renumbering the then-uncommitted registry/eval migrations (subsequently moved to 004/005 by the 2026-07-25 pricing migration), and scheduling `GET /v1/requests/:request_id/usage` in phase 2. | The response UUID must be persisted for the future owner-scoped usage lookup; a new migration converges already-applied and fresh databases without editing 001, while nullable legacy rows remain compatible and unaddressable. |
| 2026-07-17 | Made the non-streaming chat route an explicit authenticated Zod-validated pipeline with injected adapters, date-effective exact/estimated metering, route-level 1 MiB body hardening, a 120-second abort timeout, safe OpenAI error envelopes, and caught `onResponse` logging. | The route must remain fully offline-testable, keep optional provider keys from blocking boot, never leak upstream bodies or secrets, and send responses before synchronous SQLite logging; standard `invalid_request_error`/`provider_error` codes avoid expanding the documented taxonomy before phase 2. Claude Sonnet 5 / high owned the implementation and returned `APPROVE` after the orchestrator hardening pass. |
| 2026-07-16 | Implemented the OpenAI adapter against the current official `POST /v1/chat/completions` OpenAPI contract with JSON and Bearer provider auth, kept the provider key optional until adapter invocation, and Zod-validated successful upstream responses. | Official OpenAI documentation was checked at build time without a live call; dependency injection keeps all tests offline and prevents ambient developer credentials from entering test behavior. Claude Sonnet 5 / high owned and self-reviewed the implementation. |
| 2026-07-16 | Made shared `stripPgFields()` remove every `pg_`-prefixed key while retaining all other known and unknown fields; made the reusable provider retry helper retry only 429/5xx twice with equal jitter over 250ms/1s bases, body release, and abort cleanup. | Prefix stripping stays correct as PromptGate extensions grow and preserves OpenAI passthrough compatibility; the bounded retry behavior exactly matches §3.6 without retrying ordinary client errors or network/abort rejections. |
| 2026-07-16 | Defined `SseChunk` as a serialized OpenAI SSE data payload plus an explicit terminal flag, made provider resolution return a discriminated success/error result including HTTP 400, and required a currently effective pricing row through a DAO. | The phase 2 seam can stream JSON or `[DONE]` without prematurely defining another wire schema; step 7 can forward `unknown_model` without reconstructing either its envelope or status; no request can route without a rate usable for metering. |
| 2026-07-16 | Made `@promptgate/shared` a real workspace dependency with declarations and conditional development/compiled exports, while excluding emitted test bodies and declarations from its deployable `dist`. | Tests run before builds on clean CI checkouts, whereas the production Node image must resolve only compiled JavaScript and published declarations. A clean deploy and runtime import verified both paths. |
| 2026-07-16 | Installed client authentication through an encapsulated `/v1` Fastify plugin, attached a typed context containing only active-key metadata, and centralized all gateway error envelopes in `src/errors.ts`. | The plugin seam ensures future `/v1` handlers inherit auth without URL-condition middleware, while a safe context and one formatter prevent stored hashes or inconsistent error shapes from leaking downstream. Claude Sonnet 5 / high owned and self-reviewed the implementation. |
| 2026-07-16 | Scoped admin authentication and OpenAI-envelope error handling through the Fastify `/admin` plugin, returned only `{plaintext_key}` from create, listed non-secret metadata plus month-to-date spend, and returned non-secret metadata from patch. | Prefix encapsulation keeps admin policy off `/healthz`; the response shapes fulfill §5.2 while ensuring plaintext and stored hashes never appear after creation. Claude Sonnet 5 / high independently reviewed the completed auth/key path and returned `APPROVE` with no required corrections. |
| 2026-07-16 | Seeded only `gpt-5.6-luna`, locked judge `gpt-5.6-terra`, and `claude-sonnet-5`, with a future date-effective Sonnet 5 row for the published post-promotion price. | The human explicitly approved the current official-source proposal; limiting the table to routed models preserves the unknown-model guard and date-effective metering semantics. |
| 2026-07-16 | Made shared OpenAI wire schemas validate only fields PromptGate touches while preserving unknown provider fields with Zod loose objects; included the current `developer` role and official reasoning-effort values. | The gateway is a compatibility boundary: malformed fields it consumes must fail early, while untouched fields must survive OpenAI passthrough and future API additions. |
| 2026-07-16 | Pinned official `actions/checkout@v6.0.3` and `actions/setup-node@v6.5.0` to their verified full commit SHAs, with read-only contents permission and no pnpm cache bootstrap. | Phase 0 CI needs an immutable, least-privilege toolchain on the supported Node 24 action runtime; omitting setup-node pnpm caching avoids requiring the pnpm executable before Corepack activates the repository-pinned version. |
| 2026-07-16 | Applied the human-approved Fable 5 / high `proceed with adjustments` verdict by changing Phase 0 Compose publication to `127.0.0.1:8787:8787` and synchronizing the playbook. | The higher-authority security specification requires loopback for single-host use, and every documented local verify command remains compatible. |
| 2026-07-16 | Deployed only the gateway production `dist` payload from a multi-stage build, retained native build tools only in the builder, and ignored persisted data plus local `.env` variants. | The runtime needs the native SQLite binding and migration assets without compilers, source/tests, database files, or editor-created secret backups entering commits or images. |
| 2026-07-15 | Made gateway startup create the configured database parent, limited Vitest discovery to source tests, and made gateway builds clear stale `dist` output. | The default `./data/promptgate.db` must boot in a fresh workspace, and generated test copies or stale artifacts must not be executed or packaged as current output. |
| 2026-07-15 | Allowlisted lifecycle scripts only for `better-sqlite3` and `esbuild`, and made the gateway build replace-copy SQL migrations into `dist`. | The native SQLite binding and TS runtime tooling must install in clean local/Docker environments, and compiled startup needs the numbered SQL assets without stale nested copies. |
| 2026-07-15 | Scoped Node 22 types to the gateway package and enabled Biome's Git ignore integration. | Strict compilation needs the `process` type at the owning package boundary, while generated `dist/` files must stay outside lint input. |
| 2026-07-15 | Added root TypeScript with Node 22 types and real `tsc` build scripts for all four package stubs. | The required root `pnpm build` command must compile strict TypeScript for Docker/CI instead of succeeding through no-op package scripts. |
| 2026-07-15 | Installed root Biome/Vitest dev tooling in step 1 and configured Vitest `passWithNoTests` while the suite is empty. | The required `pnpm lint` and `pnpm test` pre-commit checks must be runnable from the first commit; this preserves step 6 as the first real test. |
| 2026-07-15 | Model allocation amended to a mixed OpenAI/Anthropic plan: Spark for bounded scaffolding, Luna for repeatable support work, Terra for routine phase ownership and the runtime judge, Sol for technical checkpoints/polish, Sonnet (never below high) for phases 1 and 8, Opus for phases 2 and 5, and Fable for dataset/pathway judgment. | Human-approved model-policy change. GPT-5.6 Terra / high replaces the previous runtime judge; no Haiku model is permitted. Cross-provider review is preferred for quiet-failure code. |
| 2026-07-15 | Plan review corrections applied (external agent review, human-approved). Highlights: budget → reserve-then-reconcile hard cap, integer micro-USD money, cache key = full forwarded body, evals run `pg_no_cache: true`, one eval run per model + `dataset_hash` + label freezing, paired CI baseline, pg-eval gets admin credential + dataset upsert endpoint, `x-pg-cost-usd` non-streaming only + `GET /v1/requests/:id/usage`, `response_format` → Anthropic `output_config.format` translation, promptfoo `defaultTest.threshold` path, FK pragma + composite label FK, `requests_daily` schema, repo bootstraps in place, provider keys optional at boot, node-native healthcheck, `--filter @promptgate/*`, CI secrets → container via `.env` + SHA-pinned actions + provider-side spend caps, contract-nightly deferred to phase 8, completion evidence → README (idea file stays clean), new guide §12 security/privacy. | Review verdicts accepted after independent fact-check (promptfoo threshold path, Anthropic structured outputs both confirmed against docs); two points accepted with qualification (streaming-usage wording was already correct in playbook; CI secret risk partly mitigated by GitHub's fork-PR secret withholding). |

## Backlog (noted, not acted on)

- Phase 6 CI-flake triage must retain the Phase 5 reference numbers: same-prompt v1 baseline scores differed by about `0.0214` (roughly 43% of the `0.05` score-drop band), while deliberately weakened v2 still passed `0.84` of cases and was caught by its decisive `0.20714285714285707` score drop. Any threshold change is owner-gated and changes the dataset hash.
- Treat `idiom_cant_take_it_persistent_heaviness_review_01` as a known review/urgent-boundary flake: good candidate run 3 overtriaged it, while the other v1 runs passed it. Its owner-reviewed `review` label remains unchanged.
- Consider persisting judge identity on eval runs in an approval-gated future schema change; fresh pairing keeps Phase 6 technically unblocked, but missing judge identity already invalidated one historical baseline comparison.

## Session log

| Date | Covered | Ended at |
|---|---|---|
| 2026-08-04 | Phase 8 observation day 5 — the required first-operation in-container read-only preflight found no local-day `web_builder_llm` success for prompt ID 4/v1. The exact gateway image remained healthy with restart count zero, `prod` remained immutable v1, label history and the $5/60-RPM key were unchanged, both repositories retained their approved states, and the stopped app was safely restarted against its persisted output. Exactly one normal date-unique idempotent text-only app edit was issued with no caller retry. Its first bounded internal attempt became durable row 434 (`client_aborted`, streamed 0, 2,647/0 tokens, conservative estimated 371 micro-USD, 5,992 ms); the app's normal transient retry returned HTTP 200 as job `38b4f58ab1c843bf86d0ff180de0e3e7`, sole successful qualifying row 435, and snapshot `22d1bb892aef4084a17fb9e8b36ac82a` (prompt ID 4/v1, streamed 0, uncached, 3,182/3,339 tokens, exact 959 micro-USD, `ok`, 16,567 ms). Buffered post-completion events were intact. The snapshot preserved exactly one footer sentence, one `Walk-ins welcome.`, and three FAQs; HTML/CSS remained byte-identical to the parent. Eleven retained rows total 15,965 micro-USD ledger spend (15,223 exact plus 742 estimated), with 5,089 exact saved micro-USD and no estimated/unknown savings. Adding the separately disclosed 5,089 exact orphan yields 21,054 micro-USD of orphan-inclusive accounting, including 20,312 micro-USD of known exact provider-priced completions. Conflicting wall-clock and monotonic timings are disclosed; retained `total_ms` is used for latency evidence. The window is day 5/7 and its 5/5 distinct-traffic-day floor is satisfied, but the full span through 2026-08-06 remains. A fresh independent read-only audit returned `APPROVE`; its timebox prevented a fresh repeat of only the snapshot hash/count/byte comparison, so that narrow claim remains supported by the orchestrator's recorded read-only verification. | Continue duplicate-safe daily observation through 2026-08-06, then run closing Phase 8 analysis |
| 2026-08-03 | Phase 8 observation day 4 — the required first-operation in-container read-only preflight found no local-day `web_builder_llm` success for prompt ID 4/v1. The exact gateway image remained healthy with restart count zero, `prod` remained immutable v1, label history and the $5/60-RPM key were unchanged, both repositories retained their approved states, and the stopped app was safely restarted against its persisted output. Exactly one normal date-unique idempotent text-only app edit was issued with no caller retry. Its first bounded internal attempt became durable row 432 (`client_aborted`, streamed 0, 2,653/0 tokens, conservative estimated 371 micro-USD, 15,030 ms); the app's normal transient retry returned HTTP 200 as job `ffd6f58297e74d64853e25cd5f188950`, sole successful qualifying row 433, and snapshot `ac5e5e21dec54ac28474020c5660b840` (prompt ID 4/v1, streamed 0, uncached, 3,186/3,405 tokens, exact 978 micro-USD, `ok`, 18,954 ms). Buffered post-completion events were intact. The snapshot preserved exactly one footer sentence, one `Walk-ins welcome.`, and three FAQs; HTML/CSS remained byte-identical to the parent. Nine retained rows total 14,635 micro-USD ledger spend (14,264 exact plus 371 estimated), with 5,089 exact saved micro-USD and no estimated/unknown savings. Adding the separately disclosed 5,089 exact orphan yields 19,724 micro-USD of orphan-inclusive accounting, including 19,353 micro-USD of known exact provider-priced completions. Conflicting wall-clock and monotonic timings are disclosed; retained `total_ms` is used for latency evidence. The window is day 4/7 with traffic on 4/5 required distinct days. A fresh independent read-only audit reconciled all durable claims and returned `APPROVE`, recording only the transient caller/preflight observations as non-durable. | Continue the daily observation; at least one more distinct traffic day and the full span through 2026-08-06 remain |
| 2026-08-02 | Phase 8 observation day 3 — the required first-operation in-container read-only preflight found no local-day `web_builder_llm` success for prompt ID 4/v1. The exact gateway image remained healthy with restart count zero, `prod` remained immutable v1, label history and the $5/60-RPM key were unchanged, and both repositories retained their approved states. The dogfood app was stopped; its first recovery launch omitted the persisted output path, but a read-only project lookup caught the empty store before provider traffic. After a controlled stop and correct relaunch, exactly one normal date-unique idempotent text-only edit was issued through the app endpoint. HTTP 200 job `a44aa0cad2ee459abd5813978774505d` produced sole snapshot `5d674218246c426d9d154ac9356ce571` and post-completion `START`/`STATUS`/`TOKEN_COUNT`/`DONE` evidence. Durable row 431 is the day's sole qualifier: DeepSeek v4 Flash, prompt ID 4/v1, uncached, non-streaming, 3,179/3,374 tokens, exact 1,022-micro-USD cost, `ok`, and 19,534 ms. The snapshot preserved exactly one footer sentence, one `Walk-ins welcome.`, and three FAQs; HTML/CSS remained byte-identical to the parent. Seven retained rows now total 13,286 micro-USD spend with 5,089 exact saved micro-USD and no estimated/unknown savings; adding the separately disclosed 5,089-micro-USD orphan yields 18,375 micro-USD of known provider-priced completions. The window is day 3/7 with traffic on 3/5 required distinct days. A fresh independent read-only audit reconciled every durable claim and returned `APPROVE`; it recorded the recovery/preflight/timing sequence as a nonblocking transient-operator evidence limitation. | Continue the daily observation; at least two more distinct traffic days and the full span through 2026-08-06 remain |
| 2026-08-01 | Phase 8 observation day 2 — the read-only preflight found no local-day `web_builder_llm` success for prompt ID 4/v1, while the exact gateway image remained healthy with restart count zero and `prod` remained immutable v1. The ignored dogfood clone was stopped, so it was safely restarted from clean external commit `ff47ea8` using ignored owner-only credentials without printing them. Exactly one normal date-unique text-only edit was issued through the app endpoint. HTTP 200 job `bc16a6d80b79426293fad8bc1de4cb08` produced the sole new snapshot `eb1bf72cd15a47c39630a92c509e4d18` and post-completion `START`/`STATUS`/`TOKEN_COUNT`/`DONE` evidence. Durable row 430 is the day's sole qualifier: DeepSeek v4 Flash, prompt ID 4/v1, uncached, non-streaming, 3,171/3,114 tokens, exact 1,158-micro-USD cost, `ok`, and 17,188 ms. The snapshot preserved exactly one footer sentence, one `Walk-ins welcome.`, and three FAQs; HTML/CSS remained byte-identical to the parent. Registry versions/history and `prod = v1` are unchanged. Six retained rows now total 12,264 micro-USD spend with 5,089 exact saved micro-USD and no estimated/unknown savings; adding the separately disclosed 5,089-micro-USD orphan yields 17,353 micro-USD of known provider-priced completions. The window is day 2/7 with traffic on 2/5 required distinct days. A fresh independent read-only audit reconciled every durable claim and returned `APPROVE`, with only the nonblocking note that caller-side timing and the pre-restart stopped state were transient observations corroborated by durable event/result records. | Continue the daily observation; at least three more distinct traffic days and the full span through 2026-08-06 remain |
| 2026-07-31 | Phase 8 observation continuity — after Step 4's independently approved rolled-back v1 call opened the exact 2026-07-31 through 2026-08-06 window, created active thread heartbeat `phase-8-dogfood-observation` for one daily 09:00 local check through the closing date. Each wake first performs an in-container read-only search for an already-qualifying prompt-ID-4/version-1 `ok` row on that local day; if one exists it sends no traffic. Otherwise it safely verifies or recovers the loopback services, sends at most one date-unique idempotent text-only edit through the normal external app path, requires a durable non-streaming v1 success, preserves the $5/60-RPM key and `prod = v1`, updates evidence/progress, obtains independent read-only review, and commits the green day. It never alters registry versions, labels, the original finished repository, or the project idea; notification policy is failures/manual blockers only, with the owner's Opus-max then Fable-max escalation order retained. | Continue the daily observation; close only after the seven-day span and at least five distinct qualifying days are durably proven |
| 2026-07-31 | Phase 8 step 4 live registry prompt and rollback — in the ignored dogfood clone, external commit `ff47ea8fc8af17933b0ec5a2f0742c6f942324d3` makes only provider ID `promptgate` send `messages: []`, exact `pg_prompt: web_builder_request@prod`, and the complete already-assembled generate/edit/repair text as `pg_vars.request_prompt`; every other OpenAI-compatible provider retains its one user message and no `pg_*` fields, while response format, sampling, auth, 135-second dogfood timeout, retry, and buffered transport are unchanged. Focused request-shape tests passed 33/33, the full external suite passed 380/380 with package success, and a fresh independent read-only audit returned `APPROVE`; the original finished repository remains unchanged at `1fe570b` with only pre-existing untracked `TECH_STACK.md`. Before app restart, the admin API created prompt ID 4 `web_builder_request`: immutable v1 is the direct-equivalent user-variable envelope and immutable v2 adds one bounded preservation/accessibility system message; the diff showed only that message. Label history first recorded null→2. After one patched-app restart, a real UI edit with the exact Phase 8 rollback-proof prompt remained buffered, succeeded as snapshot `e7c6baee0890440dae42584af5a21544`, and retained row 428 with prompt 4/v2, cache miss, non-streaming, 3,195/7,593 tokens, exact 2,573-micro-USD cost, `ok`, and 41,173 ms. The API then confirmed `prod` 2→1 and history row 9. Without app restart or code/client change, the exact same next app prompt succeeded as snapshot `1b84dc94357c428886a862779aeaad9d` and retained row 429 with prompt 4/v1, cache miss, non-streaming, 3,167/3,476 tokens, exact 1,311-micro-USD cost, `ok`, and 20,834 ms. Both snapshots contain exactly one required footer sentence, one visible `Walk-ins welcome.`, three FAQs, and byte-identical HTML/CSS hashes; both replayed progress only after completion. `prod` remains v1. Key 26 remains enabled at $5/60 RPM; five retained rows total 11,106 micro-USD spend and 5,089 exact saved micro-USD, while the separately disclosed orphan makes known provider-priced completions 16,195 micro-USD. The successful rolled-back v1 call opened the inclusive 2026-07-31 through 2026-08-06 window, with day 1 real traffic complete. A fresh independent live-evidence/security audit reconciled exact registry state, rows, one-process ordering, snapshot content/hashes, accounting, date math, image health, and both repository states, found no secret leak or overclaim, and returned `APPROVE`. | Commit the step-4 evidence; continue the observation window with real traffic on at least four additional distinct days |
| 2026-07-31 | Phase 8 step 3 buffered dogfood verification and lifecycle recovery — ran the real built PromptGate deployment through the finished app's existing UI without changing the original repository at `1fe570bb175834107409739703e03efdd5805fc2`. An ignored local clone made only the independently approved timeout configurability correction (`5335d9f`, default 90 seconds, dogfood override 135 seconds; external suite 377/377). The first generation created the Copper Spoke project, replayed `START`/`STATUS`/`TOKEN_COUNT`/`DONE` only after buffered completion, and retained exact-cost row 425. The first FAQ edit reached the provider but the external 90-second wait ended before PromptGate's 120-second upstream timeout; its successful exact 5,089-micro-USD provider response entered cache, while Fastify skipped `onResponse` after the client disconnect and PromptGate retained no row or spend. No row was fabricated or backfilled. Repeating under the corrected client timeout returned the cached page with exactly three FAQs and retained row 426 with exact 5,089-micro-USD savings. The separately committed PromptGate correction `f6cd9fa335ebfed300018b5d8de9c156917e2a74` now aborts upstream on buffered client disconnect, writes exactly one durable `client_aborted` outcome with conservative prompt-only metering when needed, reconciles budget only after durable logging, releases successful/cache-hit lifecycle state, and preserves earlier timeout/provider-error first causes, including adjacent pre-header streaming cleanup. Seven real-loopback regressions passed in the 71/71 focused suite; lint checked 179 files; a clean build followed by the exact full test command passed all 859 tests in 67 files; package builds, Compose, and diff checks passed. Docker Desktop failed during inspection and the old container exited 255; controlled engine recovery plus the old image's admin API proved the persistent database and exact $5 key intact before rebuild. Exact corrected image `sha256:b0b39a7bffe71e7a4b7e3ffd9231d3ad470175edffca9ff362fcd12c61a4e0b8` is healthy with restart count zero. A unique post-fix edit visibly added `Walk-ins welcome.`, preserved exactly three FAQs, replayed progress only after completion, and retained row 427 with 3,157/15,968 tokens, exact 4,913-micro-USD cost, cache miss, `ok`, and 85,962 ms. The database contains exactly three durable dogfood rows, retained spend 7,222 micro-USD, exact cache savings 5,089 micro-USD, and zero estimated/unknown saved-cost rows; known provider-priced completions including the explicit pre-fix orphan total 12,311 micro-USD. A separate direct `deepseek/deepseek-chat` generation proved the same buffered UX but is excluded from p95 because it is not model-matched. A fresh independent read-only audit verified the source, 859-test gate, exact healthy image, safe key metadata, rows and accounting, separate orphan disclosure, untouched original repository, closed observation window, and absence of credential leakage or overclaim, then returned `APPROVE`. | Commit the step-3 evidence, then implement the registry-backed step-4 canary and rollback proof |
| 2026-07-31 | Phase 8 Truthfulness and Verify Amendment A approval — recorded the project owner's explicit approval and synchronized the playbook, guide, progress authority, and Phase 8 evidence. Normal external dogfood verification now covers the actual buffered provider completion and post-completion progress replay; token-stream proof belongs to the scheduled contracts. Locked named missing-credential skips, configured/zero-provider red behavior, live registry use before the seven-day window, the five-of-seven real-traffic floor, cache/p95 methods, confirmed registry-label rollback, and the literal Verify block. Resolved stale guide TODO/risk language, made excluded unknown legacy cache-hit counts explicit, and recast the pre-amendment evidence historically. Lint checked 179 files, all 852 tests in 67 files passed, Compose configuration and diff checks passed, and the corrected independent read-only re-audit returned `APPROVE`. The project idea remains untouched. | Commit the approved authority change, then execute Phase 8 step 3 |
| 2026-07-30 | Phase 8 step 2 — read the exact finished `web_builder_llm` source at clean tracked commit `1fe570bb175834107409739703e03efdd5805fc2` while preserving its unrelated untracked `TECH_STACK.md`. Resolved the provider surface to app-level JSON selected by `LLM_PROVIDERS_CONFIG`/`site.llm.providers.config`; the checked-in `promptgate` definition uses `OPENAI_CHAT`, Bearer auth, loopback `http://127.0.0.1:8787/v1`, env-name-only `PROMPTGATE_API_KEY`, `NATIVE_JSON_OBJECT`, default `deepseek-v4-flash`, true provider streaming capability, and false vision/image capabilities. Because the repository correctly blocks loopback/private base URLs by default, the trusted local launch must explicitly set `site.llm.allowPrivateBaseUrls=true`. Proved `/v1/models` is used conditionally: OpenAI-compatible key validation sends one authenticated GET, and the successful frontend path then asks its `/api/models` controller for a live list, which sends a second authenticated GET and falls back to configured defaults if listing fails. Proved the PromptGate-facing OpenAI Chat body contains no tools and uses `model`, one user message, `response_format: json_object`, and optional sampling fields; the separate Anthropic tool path remains the already-resolved Phase 2 behavior. The same inspection found that provider generation is currently buffered via `BodyHandlers.ofString()` with no `stream: true`, while the app's documented SSE replays progress only after synchronous completion; therefore the existing playbook/guide streaming wording cannot be claimed literally. It also found Phase 8 has no literal Verify block despite the orchestrator's every-phase requirement. Per the project owner's escalation instruction, Claude Opus / max reviewed the supplied authority/source facts and returned `PROCEED WITH ADJUSTMENTS`: retain the buffered external transport, move token-stream proof to the already-planned nightly contracts, lock missing providers as named skips but configured failures and zero configured providers as red, put `pg_prompt` live before the observation window, state cache/p95 methods, and add a literal final Verify block. No external repository, provider, secret, Docker, browser, database, or GitHub state changed. | Commit the step-2 resolution; request the owner-approved Phase 8 truthfulness/Verify amendment before step 3 |
| 2026-07-30 | Phase 8 step 1 — after approval-only head `408c655` passed both refreshed ordinary CI runs, a 13m13s same-version live evaluation, cleanup, and aggregate `eval-gate`, marked PR #12 ready and merged it as `6f2d44ea2bcf118eccabb815d2fd459f4097bfb0`; synchronized protected `master`, deleted the merged local Phase 7 branch, created `codex/phase8-dogfood`, and reconfirmed all 852 tests in 67 files. Tightened `.env` to mode `0600`, built and deployed the exact merged Compose image `sha256:1070c675c80190765e3bc03ff778592bfb47ae89d3e1a5267fc9da34b737ccea` with the existing persistent database, and reached healthy loopback state. Reserved an empty ignored mode-`0600` handoff before the one-time admin POST, then created key ID 26 as `web_builder_llm` with exact monthly budget 5,000,000 micro-USD and 60 RPM, durably wrote its 51-byte plaintext only to `data/web_builder_llm.key`, authenticated it against `/v1/models` without printing it, and re-read the safe metadata: enabled, zero month-to-date spend, six effective models. Authenticated request-count metrics from the Phase 7 merge through `2026-07-31T05:12:59Z` returned no points and total zero, proving the deployment/key checks made no provider request. Final lint checked 178 files, all 852 tests in 67 files passed, Compose configuration and diff checks passed, and the healthy container retained restart count zero. A fresh independent read-only audit verified the branch/diff, exact image/container/mount, ignored owner-only credential files, safe key metadata, zero-row metrics through `05:15:42Z`, and absence of a tracked or logged `pg-*` key literal, then returned `APPROVE`. No product/source/contract change, external dogfood-repository mutation, browser action, or provider call occurred. | Commit the green step-1 evidence/progress record, then resolve the two Phase 8 `TODO(verify)` items from the available finished `web_builder_llm` repository |
| 2026-07-30 | Phase 7 completion approval — the project owner explicitly approved Phase 7 after the retained provider-free evidence, final independent Sol/high `APPROVE`, and the initial protected PR #12 checks were all green at exact head `346c23379757586616abcbfc4dfe95642b82b7f0`. The two ordinary CI runs passed; the same-version live gate ran one candidate evaluation, baseline and candidate each passed 50/50 with pass-rate drop 0 against the approved 0.05 limit and identical score average `0.9771428571428571` over seven scored cases; cleanup and aggregate `eval-gate` passed. This approval-only record changes no product code, contract, test, dataset, evidence artifact, provider configuration, runtime data, or Phase 8 scope. | Commit and push the approval record, require the refreshed provider-inert protected checks to pass, mark PR #12 ready, merge it, synchronize `master`, then begin Phase 8 |
| 2026-07-30 | Phase 7 provider-free end-to-end Verify — ran the real built gateway/dashboard on loopback against a fresh disposable SQLite database with injected deterministic DeepSeek/OpenAI-compatible adapters and no reachable provider endpoint. Real pricing plus CI seeds created immutable `safety_screen` v1/v2 and a disposable capped gateway key; one retained two-case file produced v2 `1/2` and v1 `2/2` with the identical `c8e0904…d0e` SHA-256/durable dataset hash, target model, prompt ref, and fixed rubric behavior. Controlled traffic retained exact miss→hit cache cost/savings, estimated cost, unknown rejected pricing, and exact v2/v1 rollback costs. The production Prompts UI confirmation moved `prod` v2→v1, truthfully refreshed its success state, and changed the next identical chest-pain response from `none` to `urgent`; Quality Drift rendered pass rate 50%→100%, stable 90% score, and the same-hash `Prompt v2 → v1` annotation. Overview, Cost Explorer, Prompts, and Quality all rendered authenticated live data; browser console/body overflow/focus/table-region assertions passed. Corrected the first evidence draft after an independent audit found right-edge screenshot cropping, over-aggressive temporary-artifact cleanup, and an overbroad credential-read claim: recaptured five complete screenshots including both sides of the scrollable Quality table; retained the exact dataset, deterministic gateway/proxy harnesses, sanitized commands/exact eval/traffic/UI outputs, screenshot hashes, and post-shutdown read-only integrity/foreign-key/feature/label assertions; launched under `env -i`, proved all four provider-key variables absent, and proved the checked-in harness rejects a dummy present variable without reading/printing its value. All loopback services stopped, no WAL/SHM remained, and the disposable database/key directory was moved to Trash recoverably. Final lint checked 178 files, all 852 tests in 67 files passed, all four packages built, Compose configuration and diff checks passed, and a fresh independent GPT-5.6 Sol / high re-audit returned `APPROVE`. No provider call, persistent project-data mutation, GitHub mutation, or Phase 8 work occurred. | Commit the verified evidence with `PROGRESS.md`, publish the single draft Phase 7 PR, and monitor required checks; do not merge or start Phase 8 before explicit human Phase 7 approval |
| 2026-07-30 | Phase 7 step 4e Quality Drift — replaced the final placeholder with the authenticated complete `/admin/api/evals/runs` history, intentionally omitting the endpoint's global limit so older dataset-hash partitions cannot be silently truncated. The exact DTO parser rejects extra fields, malformed/impossible canonical SQLite seconds, unsafe case/cost/duration values, score values outside `[0,1]`, malformed hashes/SHAs/triggers, incomplete prompt attribution, duplicate run IDs, and any response not newest-first by timestamp then ID. Display order reverses deterministically by `(ts,id)` and partitions exact immutable hashes before any chart series, line, comparison, or annotation. Partition-owned `{x: globalIndex, y}` points connect consecutive same-hash runs across interleaved other-hash events without ever connecting hashes, while an actual null rubric score stays in the owned series as a non-spanned `Unavailable` gap. Solid score and dashed pass-rate lines are nonanimated; a custom owned Chart.js plugin paints same-hash adjacent prompt-version/model markers; the complete table and separate text list expose those markers without canvas dependence. Copy states that pass rate means all assertions passed and score averages scored rubric cases only, discloses that historical judge identity was not persisted, and treats recorded target changes as context rather than causation. Dynamic identities keep literal backslashes distinct from visibly encoded Unicode controls/bidi characters before HTML escaping. One H1, a labeled chart, small live status, direct retry, honest empty state, and a labeled keyboard-scrollable table cover access; request/chart/listener generations own refresh and route disposal. The first independent Sol/high review returned BLOCK on interleaved-series continuity, inaccurate semantic copy, and the unfocusable table region; all received direct regressions, plus route/render-before-token and chart/listener lifecycle coverage, before final APPROVE. Focused dashboard passed 53/53; the full repository passed 852/852 tests in 67 files, lint across 176 files, all four builds, Compose configuration, and diff checks. GPT-5.6 Terra / high implemented the bounded screen; no provider call, secret read, live eval, external network, browser mutation, or persistent runtime-data mutation occurred. | Commit the verified Quality Drift substep, then execute provider-free Phase 7 Verify across all four screens |
| 2026-07-30 | Phase 7 step 4d Prompts — added the third ordered screen as authenticated live prompt-registry list/detail, immutable-version history, plain-text unified messages diff, and deployment-label mutation flows. Prompt selection is numeric-ID-bound; label controls use internal numeric indices while path identities are encoded exactly once; strict parsers reject extra fields, malformed canonical timestamps, duplicate prompt IDs/slugs or labels, invalid version references/order, unexpected label-move responses, and invalid diff MIME/header framing. Identifier rendering doubles literal backslashes and visibly encodes all Unicode `Cc`/`Cf` characters so distinct slugs/labels never appear identical, while descriptions, notes, JSON, and diff content preserve printable backslashes and JSON formatter newlines under HTML escaping. Native version checkboxes select at most two immutable versions, focus survives prompt/version rerenders, diff errors retain the messages-only disclosure and direct retry, no-hunk patches state that no messages changed, and only a small status region is live. Promote/Roll back controls remain separate from comparison, name the next-request effect and immutable history in confirmation, serialize writes, disable repeat mutation, accept the server-returned prior pointer as truth, never update optimistically, and require successful list plus selected-detail reload before claiming confirmation; any failed or ambiguous mutation/reload leaves a persistent shell alert and no stale actionable detail. The initial Sol/high audit returned BLOCK on build, stale-response, mutation, identity, focus, and rendering failures; the second pass found disabled-success controls and refresh/formatting issues; the final pass isolated swallowed post-write reload failures. Every finding received a correction and regression before final APPROVE. Focused dashboard passed 46/46; the full repository passed 845/845 tests in 66 files, lint across 174 files, all four builds, Compose configuration, and diff checks. GPT-5.6 Terra / high implemented the bounded screen; no provider call, secret read, live eval, external network, browser mutation, or persistent runtime-data mutation occurred. | Commit the verified Prompts substep, then implement Quality Drift |
| 2026-07-30 | Phase 7 step 4c Cost Explorer — added the second ordered screen as an authenticated cost-timeseries consumer with native model/API-key/feature grouping, one floored whole-second exclusive upper bound, and no fake or zero-filled data. Exact charges, estimated charges, the known subtotal, and unknown-pricing request counts remain separately visible in the nonanimated stacked chart, full-precision table, tooltips, stat, and prose; the cost-specific formatter preserves all six micro-USD decimal places so one micro-USD never displays as zero, while fractional Chart.js guide ticks use a safe approximate micro-USD formatter and cannot crash the view. Null features display as `Untagged`; literal names remain prefixed; backslashes and every Unicode `Cc`/`Cf` control or format code point are visibly encoded before escaped HTML insertion. Hardened the shared metric parser to reject unexpected fields, duplicate bucket/group points, descending buckets, invalid provenance, and overflow while accepting server-provided within-bucket Unicode order. Added the central hash router with one active-screen disposer, focus transfer on navigation, and honest temporary placeholders for the two not-yet-built routes; Overview and token behavior remain intact. Native radio controls and page chrome stay mounted through loading/success/error so selection/focus survive, selected state is visible, known totals cannot remain stale, and stale or disposed requests cannot own the view. The independent GPT-5.6 Sol / high pre-audit and first review identified cents-rounding, focus, route lifecycle, control-character, strict-parser, and a one-micro-USD fractional-axis crash; all were corrected, then final review returned APPROVE. Focused dashboard passed 35/35; the full repository passed 834/834 tests in 65 files, lint across 172 files, all four builds, Compose configuration, and diff checks. GPT-5.6 Terra / high implemented the bounded screen; no provider call, secret read, live eval, external network, browser mutation, or persistent runtime-data mutation occurred. | Commit the verified Cost Explorer substep, then implement Prompts |
| 2026-07-30 | Phase 7 step 4b Overview — built the first ordered screen as an authenticated live consumer of the approved metrics and keys APIs, with no fake data: stacked known spend by model; request count with p50/p95 latency; cache rate with known exact and estimated saved amounts; and sorted per-key month-to-date budget burn, including truthful `$0` hard-ceiling handling. Added explicit loading/error/empty states, a responsive shell/nav, escaped dynamic markup, nonanimated/reduced-motion-safe Chart.js configurations, formatted tooltips, explicit UTC labels, and keyboard-accessible data-table alternatives. Added strict DTO parsing for canonical hours, dimensions, units, nullability, exact/estimated/value equations, safe integers, ratios, key fields, and financial aggregate overflow. All six metrics calls share one floored whole-second exclusive upper bound; group-none bucket alignment fails closed; an early fan-out failure aborts siblings; refresh/dispose prevents stale render/listener/chart ownership. The initial independent GPT-5.6 Sol / high review returned BLOCK on malformed-response acceptance, mixed snapshots, sibling lifetime, missing-latency count, zero-budget wording, hidden spend provenance, timezone/tooltips, and then a same-second fractional-bound race. Corrected every item, added targeted regressions including the exact six-request `.000Z` fan-out, and received final APPROVE. Focused dashboard passed 25/25; the full repository passed 824/824 tests in 64 files, lint across 170 files, all four builds, Compose configuration, and diff checks. GPT-5.6 Terra / high implemented the screen; no provider call, secret read, live eval, external network, browser mutation, or persistent runtime-data mutation occurred. | Commit the verified Overview substep, then implement Cost Explorer |
| 2026-07-30 | Phase 7 step 4a — after the project owner approved Contract Amendment A, synchronized `IMPLEMENTATION_GUIDE.md` and `BUILD_PLAYBOOK.md` with the fixed metrics/prompt contracts, retained-raw-window rule, migration 006, and Quality Drift truthfulness limits. Added immutable saved-cost and estimated/exact provenance to cache entries and request rows; legacy misses backfill to exact zero while legacy hits remain unknown. Streaming and non-streaming cache writes freeze the source cost/provenance, and later hits copy it without repricing while actual spend remains zero. Added authenticated prompt detail/history with ordered current labels, parsed immutable message/variable arrays, 404 handling, and fail-closed corrupt-storage behavior. Added the authenticated metrics DAO/route for cost, request count, p50/p95 latency, cache rate, cache saved, and tokens with `none|model|key|feature` grouping; exact `[from,to)` RFC3339 fractional/offset semantics; canonical UTC-hour buckets; deterministic nearest-rank percentiles; explicit exact/estimated/unknown values; raw-row validation; null/NUL-safe groups; safe-integer overflow rejection; no zero fill; and constant-memory fail-closed timestamp preflight. No eval schema change was made. The initial independent GPT-5.6 Sol / high audit found fractional-bound, group-collision, row-validation, overflow, and unbounded-preflight issues; all were corrected with regressions, and the final audit returned APPROVE. After a clean four-package build supplied the ignored runtime artifacts expected by native-node tests, the full repository passed 823/823 tests in 64 files, lint across 170 files, all builds, Compose configuration, and diff checks. GPT-5.6 Terra / high implemented the bounded foundation; no provider call, secret read, live eval, browser action, or persistent runtime database mutation occurred. | Commit the verified Contract Amendment A backend/docs foundation, then commit the separately verified Overview screen and continue to Cost Explorer |
| 2026-07-30 | Phase 7 step 3 — after protected step-2 PR #10 passed two ordinary CI runs, its same-version live evaluation (48/50; pass rate 0.96; score average 0.9716666666666667 over 6 scored cases; zero score/pass-rate drops), cleanup, and aggregate `eval-gate`, merged as `97ef1e4`, synchronized protected `master` and created `codex/phase7-token-flow`. Added `src/api.ts` with one load-initialized module-memory admin token, no storage/cookie/URL/log path, normalized same-origin `/admin/api` enforcement, caller-header replacement, and one-retry 401 recovery coordinated by separate prompt/token generations so simultaneous stale or canceled refreshes cannot prompt-storm; canceled/empty replacement retains the prior token and allows a later attempt. The entry renders the existing shell before prompting. Twelve no-network token-flow tests cover exact token handling, one-time initialization, forced header, unsafe/traversal paths, request preservation, successful and repeated 401s, null/empty recovery, simultaneous accepted/canceled refreshes, non-401 responses, fetch failures, no persistence/logging, and render-before-prompt order; with the four scaffold tests, focused dashboard coverage passed 16/16. Full 802/802 in 61 files, lint across 164 files, dashboard and four-package builds, and diff checks passed. Browser-control smoke proved exactly one native prompt per fresh load, cancel and submit both returned to the intact accessible landmark structure, local/session storage and cookies stayed empty, only the document and local hashed assets loaded, the console stayed clean, and no viewport overflow appeared; the preview and tabs were removed. GPT-5.6 Terra / high implemented the bounded step; independent GPT-5.6 Sol / high returned APPROVE after security, concurrency, accessibility, encoded-path, focused, full-suite, build, lint, and diff review. No gateway/API/schema/dependency/CSS/screen/metrics change, provider call, secret read, or persistent runtime-data mutation occurred. | Commit and publish Phase 7 step 3 through protected review; after merge, request the owner-gated metrics-contract amendment before Phase 7 step 4 |
| 2026-07-30 | Phase 7 step 2 — added `@fastify/static` `^10.1.2` as a gateway production dependency; added absolute-only `DASHBOARD_DIST_PATH` with a source/workspace-dist-stable module-relative `packages/dashboard/dist` default; registered static `/` only after health, admin, and `/v1` APIs; copied the Vite bundle to `/app/dashboard` in the runtime image and set the explicit flattened-image path; synchronized `.env.example`; and added no-network coverage for index/hashed-style asset content types, health/admin/v1 precedence and authentication, default/override path behavior, ignored missing build output, dotfiles, percent-encoded traversal, and double-encoded traversal. The initial independent Sol/high review returned BLOCK after reproducing an unauthenticated `200 SECRET` for a dashboard-root dotfile under the plugin default and noted setup-failure temp-root leakage; corrected with explicit `dotfiles: "ignore"`, exact no-secret 404 regressions, and dashboard roots under the suite-owned cleanup directory. Focused server tests then passed 15/15 both normally and with the ignored dashboard `dist` removed; full 790/790 in 60 files, lint across 162 files, frozen-lockfile install, four-package build, Compose config, and diff checks passed; a fresh independent review returned APPROVE. The exact corrected image built successfully and a provider-free ephemeral container returned 200 for `/`, its referenced hashed JS, `/healthz`, and authenticated `/admin/api/keys`; returned the exact 401 error envelopes for unauthenticated admin and `/v1/models`; and returned 404 for encoded traversal. The temporary container and image were removed. GPT-5.6 Terra / high implemented the bounded step; no step 3 behavior, API/schema change, provider, secret, live eval, or persistent runtime-data action occurred. | Commit and publish Phase 7 step 2 through protected review; after merge, begin Phase 7 step 3 |
| 2026-07-30 | Phase 7 step 1 — after protected Remedy A PR #8 passed ordinary CI plus the 13m13s live evaluation and aggregate `eval-gate`, merged as `50abf9e`, and opened the Phase 7 gate, synchronized protected `master` and created `codex/phase7-dashboard`. Replaced the TypeScript dashboard stub with a Vite vanilla-TypeScript app; pinned local `chart.js` 4.5.1 and Vite 8.1.4; added `dev`, strict typechecked production `build`, and `preview` scripts; configured `/admin/api` to proxy to loopback gateway port 8787 and explicit emptied `dist` output; switched to strict browser/Bundler TypeScript; registered bundled Chart.js; and added an accessible responsive no-fake-data shell with local CSS and no CDN. Four no-network tests pin dependencies/scripts, proxy/output, local entry/Chart registration, and a write-free production bundle containing local hashed HTML/JS/CSS. Focused 4/4, full 785/785 in 60 files, lint across 162 files, frozen-lockfile install, strict dashboard typecheck, four-package build, and diff checks passed. Browser inspection found correct landmarks/headings, first-Tab skip navigation into main content, no console warning/error, no 320×568 overflow, and text contrast from 6.09:1 to 16.29:1. GPT-5.6 Terra / high implemented the bounded step; independent GPT-5.6 Sol / high returned APPROVE with no actionable findings. No API, gateway, provider, secret, live eval, or runtime-data work occurred. | Commit and publish Phase 7 step 1 through protected review; after merge, begin Phase 7 step 2 |
| 2026-07-30 | Post-Phase-6 D1 Remedy A implementation — added `--max-pass-rate-drop` with default 0.05; reused the scalar-EPS drop comparator so exact 0.05 passes; enforced larger drops for fresh distinct-version and historical baselines as quality exit 1; retained fresh same-version one-run semantics with both drops zero; required valid historical `cases_passed <= cases_total`; kept deterministic failures unscored; printed baseline/candidate pass rates, drop, and limit; left the live workflow command byte-unchanged; synchronized the orchestrator, playbook, guide, locked idea decision #11, Phase 6 evidence/status, and this record. Regression coverage includes both comparison modes at/over boundary, same-version, history validation, CLI, workflow defaults, and the exact D1 shape (50/50 baseline, 45/50 candidate, higher candidate score average, exit 1). Focused 77, full 781, lint 158 files, four-package build, Compose config, eval TypeScript, and diff checks passed; independent GPT-5.6 Sol / xhigh review returned APPROVE. The first D1 fixture was corrected after `safety@2` accidentally matched `contains safe`; no product defect. Logs: `docs/implementation-logs/phase-6/2026-07-30-codex-d1-pass-rate-drop.md` and `docs/implementation-logs/phase-6/2026-07-30-verifier-d1-pass-rate-drop.md`. No provider, secret, live eval, database, or Phase 7 action occurred. | Commit, push, open the protected correction PR, require `ci` and `eval-gate` green, merge, then begin Phase 7 |
| 2026-07-30 | Phase 6 completion and D1 Remedy A authorization — the project owner explicitly approved Phase 6 after reviewing the final 14/50 deterministic negative-control result and the independent Opus verdict; marked Phase 6 done with `docs/evidence/phase-6.md`; reconciled protected `master` from the stale pre-publication `2e82f25` record to evidence-PR merge `bcca560`; selected the 0.05 pass-rate drop band instead of zero-filling deterministic failures; created `codex/d1-pass-rate-drop`; and re-proved the clean pre-change baseline with all 768 tests in 59 files green. No provider call, live eval, secret read, Phase 7 work, or product-code edit occurred in this reconciliation step. | Implement, verify, independently review, and publish the bounded D1 remedy before Phase 7 |
| 2026-07-30 | Phase 6 evidence persistence (docs/evidence-only, Claude Code, on clean branch `codex/phase6-evidence` created from protected `2e82f25`) — persisted the owner-approved deterministic negative-control authority amendment into `BUILD_PLAYBOOK.md` Phase 6 (amendment paragraph plus Verify wording), `IMPLEMENTATION_GUIDE.md` §7.4, and `PromptGate_PROJECT_IDEA.md` decision #11 exactly as approved in disposable commit `28e4d6a` (the working diff was proven identical to that commit's authority-file diff); checked in the three deterministic-negative-control audit logs byte-exact from `28e4d6a`, preserving the original independent-verification BLOCK and the fresh correction APPROVE; created `docs/evidence/phase-6.md` and `docs/evidence/phase-6/` with four byte-identical JPEG 1920×992 screenshots under honest `.jpg` names and recomputed SHA-256 values; and recorded the final red-arm result — the single pre-registered live attempt, `eval-gate` run `30539658160` at head `28e4d6a`: detector job `90861169364` green with relevant/trusted true, `live-evaluation` job `90861217803` quality failure, aggregate job `90866805736` red, fresh baseline-first prod-v1/candidate-v3 pair, candidate 14/50 with 36 named failures, baseline score_avg 0.8966666666666666 over 6 scored cases, candidate 0.165 over 2, drop 0.7316666666666666, exact process exit 1, `ci` green at the same head (push run `30539655326` job `90861160384`; PR run `30539658159` job `90861169134` — lint, build, 771 tests), pricing/setup/seed/cleanup green; valid below the pre-registered <40 boundary (exact compliant offline prediction 16/50 with 34 deterministic label failures; live 14/50 because the two expected-`none` rubric cases also failed at 0.08 and 0.25), not a falsification, no rerun; PR #6 closed unmerged (`mergedAt` null) with local and remote `codex/phase6-degraded-proof` deleted after evidence capture. Run/PR/branch facts were re-confirmed with read-only GitHub queries only. The disposable v3 product/test files were deliberately not copied: `safety_screen.json`, `ci-seed.ts`, and `ci-seed.test.ts` have no diff from `master`, and `negative-control.test.ts` does not exist on this branch. Phase 6 status set to `awaiting approval` with evidence `docs/evidence/phase-6.md`; the Approved-by-human cell stays blank; Phase 7 untouched; product tests deliberately not rerun for this docs-only pass. All changes left uncommitted; this pass's implementation log is `docs/implementation-logs/phase-6/2026-07-30-claude-code-phase-6-evidence.md`; the protected evidence PR is not yet pushed or opened. | Fresh independent verification of this evidence pass, then the protected evidence PR to `master`, then the explicit human Phase 6 completion decision |
| 2026-07-30 | Phase 6 audit-record correction (pass 2, verifier-driven) — the fresh independent verification of the deterministic negative-control amendment left every offline product gate green but returned BLOCK because this record misattributed the Phase 6 green Verify arm: it named merged PR #4 as the green proof, omitted the actual disposable proof PR #5, and claimed PR #4's exact scores were not captured and unrecoverable even though its retained job log is retrievable. Corrected only `PROGRESS.md` (Position, Last session, Repo state, the green-arm blocker, this row) and `docs/implementation-logs/phase-6/2026-07-30-claude-code-deterministic-negative-control.md`. Recovered exact evidence now recorded: disposable PR #5 (head `4d80296f99c025d2340939236c000c6e378e9291`, run `30521540051`) is the prescribed green arm — same certified v1 for `prod`/`candidate` run once, drop 0, 49/50, both score_avg 0.9342857142857142 over 7 scored cases, single tolerated failure `policy_blood_review_01` (expected `review`, received `none`), exit 0, pricing seed/gateway/CI seed/evaluation/cleanup/aggregate green, closed unmerged with its remote branch deleted after evidence capture — while merged PR #4 (head `7e8c91d4b7120016817b4cc9b96177ba0efbc7c1`, run `30520586222`, merged as `2e82f2567afdebff30caced108b7de2867e0aa41`) is corroborating amendment evidence — 48/50, both score_avg 0.9714285714285715 over 7, drop 0, two tolerated failures `policy_fever_review_01` (immutable direct-diagnosis safety match) and `masked_stopped_heart_meds_withdrawal_review_01` (expected `review`, received `urgent`), exit 0, all stages green. The v3 amendment, D1 record, PR #6 preservation, pre-registration, and Phase 6 `verify pending` status are untouched; no product, fixture, test, workflow, authority-doc, or verifier-log file changed. Verification: `git diff --check` clean plus a scoped review of the two edited files; expensive product gates were deliberately not rerun for this docs-only pass. No commit, push, GitHub, provider, Docker runtime, `.env`, or runtime-data action occurred. | Fresh independent re-verification of the corrected record, then integrator commit/push of the v3 amendment to PR #6's branch and the single pre-registered live attempt (exit 1 via pass rate; compliant exactly 16/50; valid ≤39/50; falsified ≥40/50 → redesign, no rerun) |
| 2026-07-30 | Phase 6 deterministic negative-control amendment — implemented the owner-approved amendment on `codex/phase6-degraded-proof` (uncommitted, for integrator review) after the certified-degraded red arm failed as a control: PR #6 (candidate→certified degraded v2 at `3663220`) unexpectedly returned exit 0 with candidate 45/50 (0.90), five new deterministic failures, baseline score_avg 0.9720000000000001 over five scored cases, candidate 0.9879999999999999 over five, and drop −0.016, now recorded as defect D1 with its evidence preserved. Added the uncertified fixture v3 whose system message pins `{"risk_level":"none","guidance":"No action needed."}` and whose user message never references the still-declared `note` variable; pointed `candidate` at v3 with `prod` at certified v1; pinned `NEGATIVE_CONTROL_DIGEST` `b4191e04…` beside the two byte-unchanged certified digests (both re-verified); widened only fixture input validation to three versions (tuple, candidate union, `addSafetyVersion`, `ensureSafetyPrompt` bounds); updated the `ci-seed` digest-array/label expectations so ordinary `ci` stays green; and added the offline negative-control margin proof, which drives the real 50-case dataset through the real `evaluateCaseAssertions` with the pinned constant and a stub rubric — exactly 16 pass/34 fail, every failure detail matching `Expected risk_level urgent\|review; received none.`, pass rate 0.32 below threshold 0.8, exactly two stub rubric calls, and the constant-label lemma max 24 < 40. `eval-gate-workflow` and `safety-screening` tests passed unmodified. Synchronized playbook Phase 6 (amendment paragraph plus Verify wording), GUIDE §7.4, idea decision #11, and this record; pre-registered the single live attempt in Blockers before any push. Verification: focused 4-file/23-test run, lint across 159 files, all 771 tests in 60 files (one unrelated known gateway-lifecycle flake failed once, then its file passed focused and the full suite passed clean), the four-package build, `docker compose config --quiet`, and `git diff --check` all passed; no commit, push, GitHub, provider, Docker runtime, `.env`, or runtime-data action occurred. | Integrator review, then commit/push the v3 amendment to PR #6's branch and run the single pre-registered live attempt: exit 1 via pass rate, compliant exactly 16/50, valid ≤39/50, falsified ≥40/50 → redesign with no rerun |
| 2026-07-29 | Phase 6 same-version pairing amendment — implemented the owner-approved amendment on branch `codex/phase6-pricing-seed-fix` (PR #4, which also carries fresh pricing seed fix `02109b3`) after PR #4's live gate went red: setup, pricing seeding, CI seeding, and cleanup passed, but the pre-amendment same-version pair returned quality exit 1 with candidate 48/50, score 0.881666667 over six scored cases, one 0.42 rubric disagreement and the known over-triage case, while the baseline's exact score was never emitted and cannot be recovered from the discarded ephemeral database. The runner now freezes both refs and, when they resolve to one prompt id and version on the fresh paired path, runs and persists the candidate exactly once, applies the dataset threshold, defines the score drop as zero, and prints an explicit line naming both refs and the shared version; every fresh pair also prints baseline/candidate score averages, both scored counts, and the computed drop after the unchanged per-case table with named failures. Distinct versions keep baseline-first two-run persistence with the 0.05 band; history mode is untouched. Added offline regressions for same-version single-run call/persistence counts with a green result, same-version below-threshold red, distinct-version exact-boundary green and one-billionth-over red with baseline-first persistence order, unchanged same-version history comparison, the exact full-output comparison summary, and the single no-retry live workflow invocation; synchronized idea decision #11, GUIDE §7.2/§7.4/§13, playbook Phase 5 step 6 and Phase 6, and this record. The independent verifier passed 38 focused tests, lint across 158 files, all 768 tests in 59 files, the four-package build, Compose configuration, and diff checks. Its sole blocker was the stale broad sentence “Phase 6 remains unchanged”; the integrator narrowed it exactly to “The Phase 6 workflow command remains unchanged,” after which the focused suite and diff check re-passed and the verifier returned final `APPROVE`. Implementation and verification logs are checked in; no provider, Docker runtime, database, or persistent project-data mutation occurred during this amendment pass. | Commit and push the verified amendment to PR #4, require its live green gate, then run the separate degraded Verify PR |
| 2026-07-29 | Phase 6 step 4 completion — published deterministic reset commit `c39d661`; ordinary Linux CI run `30515425287` passed lint, all 762 tests, and build. Opened temporary docs-only draft PR #1 at `bbedf29`, outside the exact live allowlist. Both `ci` runs passed; `detect-eval-scope` passed; `live-evaluation` was explicitly skipped; and aggregate `eval-gate` passed. Commit check-run inspection confirmed exact required names `ci` and `eval-gate`, both from GitHub Actions app ID `15368`, with no provider job. The first protection request was rejected atomically with HTTP 422 because GitHub's current schema treats legacy `contexts` and app-bound `checks` as alternative forms; retrying with only the verified app-bound `checks` succeeded. A fresh protection read proved `strict=true`, exact `ci`/`eval-gate` app bindings, administrator enforcement, no pull-request review rule, no restrictions, force-push off, deletion off, and `master protected=true` at `c39d661`. PR #1 was then closed unmerged and its remote/local probe branch deleted. No provider call or persistent project data mutation occurred. | Phase 6 Verify — run one trusted relevant no-op PR for a live green pair, then one deliberately degraded fixture-label PR for a named red quality result; merge neither |
| 2026-07-29 | Phase 6 step 4 deterministic CI regression — pushed the `writableFinished` correction as `61ff4b1`; ordinary CI run `30515045421` again passed 761/762 tests but honestly persisted `ok` because Linux handed the entire cached response to the OS and emitted successful `finish` before the client received its first data callback. Kept the correct production semantics and replaced only the test's TCP-buffer assumption: a test-local `onSend` hook selected by a test-only header wraps the cache replay, yields one transport chunk, blocks all remaining frames, lets the client destroy the socket, awaits the server's `close`, then releases idempotently. The production close listener runs first, so both durable reconciliation and insert-failure debt retention finish before assertions; the fixed timeout and 2 MiB payload were removed. The exact regression, all 23 stream-handler tests, lint across 158 files, all 762 tests in 59 files, all four package builds, Compose configuration, and diff checks passed. Independent review recommended this no-production-change gate. No provider call, protection change, or persistent project data mutation occurred. | Commit/push the deterministic test, require ordinary `ci` green, then run the provider-inert check-context PR before enabling protection |
| 2026-07-29 | Phase 6 step 4 publication and CI correction — committed/pushed the approved stable-check policy as `7c21c74` while `master` remained intentionally unprotected. The resulting ordinary `ci` run `30514663366` ran no providers and passed 761/762 tests; its sole failure reproduced a pre-existing cache-replay reset race on Linux, where `writableEnded` had become true after `end()` was called even though the response had not successfully flushed, so the request row incorrectly retained `ok`. Replaced both cached-replay and live-stream close guards with Node's actual successful `writableFinished` signal and documented the transport distinction. The exact failed regression passed, the complete 23-test stream-handler file passed independently, and two independent semantic reviews returned `APPROVE`. After stopping one overlapping diagnostic process and rebuilding cleanly, lint checked 158 files, all 762 tests in 59 files passed, all four packages built, Compose configuration and diff checks passed. No provider call, protection change, or persistent project data mutation occurred. | Commit/push the stream-finish correction, require ordinary `ci` green, then run the provider-inert check-context PR before enabling protection |
| 2026-07-29 | Phase 6 step 4 policy preparation — applied the owner-approved resolution of the `main`/`master` and required-path-filter conflict. The workflow now runs on every pull request and uses a credential-free, full-history detector with immutable checkout pin, no persisted credentials, validated event SHAs, a merge-base-to-head NUL-safe diff, relevant-on-failure fallback, and the exact approved gateway/evals/shared/runtime/lockfile/two-workflow/custom-action allowlist. Trust requires the same repository plus non-Dependabot author and actor. Only trusted relevant work reaches the unchanged secret-bearing live evaluation; relevant untrusted work is provider-inert red, while irrelevant work is provider-inert green. An `always()` aggregate named `eval-gate` rejects detector failure, invalid outputs, and every unexpected job-result combination; ordinary CI is stably named `ci`. The guide/playbook now target existing `master`, strict up-to-date `ci` + `eval-gate` checks for administrators, and no added review requirement. The six-test workflow contract locks exact paths/trust/pins and executes a ten-case aggregate truth table. Final lint checked 158 files, all 762 tests in 59 files passed, all four packages built, Compose configuration and diff checks passed. Independent workflow and security reviews returned `APPROVE`; no provider call or persistent data mutation occurred. | Publish the amendment to unprotected `master`, verify both real check contexts on a same-repository irrelevant PR without provider traffic, then enable and re-read branch protection |
| 2026-07-29 | Phase 6 step 3 — added `.github/workflows/eval-gate.yml` with the approved `pull_request` path scope, read-only repository permission, PR-number concurrency, a 60-minute bound, and full immutable pins for checkout v6.1.0, pnpm action v6.0.9, and setup-node v6.5.0. A no-secret first step fails forks and Dependabot before any action or credential load. The image builds before provider credentials exist; the workflow then requires all four configured secrets, writes a mode-`0600` Compose `.env`, masks a per-run admin token, starts a fresh no-rebuild gateway/database, runs Recovery-A `seed-ci`, validates and masks the mode-`0600` handoff, and runs exactly the fresh `safety_screening` `candidate` versus `prod` DeepSeek-target/Terra-judge pair with 15-second pacing and no history. The summary preserves named cases and exact 0/1/2 meaning through `tee`; capture or summary failures become infrastructure 2. Bounded cleanup requires a successful graceful Compose shutdown before reporting green, never deletes SQLite while the gateway may be live, and removes the secret files. The new three-test offline workflow contract parses the YAML and locks trigger scope, permissions, action SHAs, build-before-secret ordering, fork policy, all four secret names, Recovery A handoff, exact paired command, no history/comment path, summary behavior, and fail-closed cleanup. Final lint checked 158 files, all 759 tests in 59 files passed, all four packages built, Compose configuration and shell syntax checks passed, and an exact Docker build completed as manifest list `sha256:89e1db4f6b0330748cc191695373c6c21a40ee5b8251d0557b8db71f54fc513a` before its temporary tag was removed. Independent implementation and security re-reviews both returned `APPROVE`; no provider call or persistent database mutation occurred. | Phase 6 step 4 — blocked pending owner resolution of `main` versus actual `master` and event path filters versus a stable required check |
| 2026-07-29 | Phase 6 step 2 — the project owner confirmed completion of the dedicated provider-side-bounded OpenAI, Anthropic, Gemini, and DeepSeek CI credential setup. A value-blind `gh secret list --app actions` check against `ASDFGHJKLZXC123/promptgate` verified all four required repository secret names: `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `GEMINI_API_KEY`, and `DEEPSEEK_API_KEY`; no secret value was read or printed. The full step gate remained green with lint across 157 files and all 756 tests in 58 files. Anthropic and Gemini remain configured-only for Phase 6 and must not be described as live adapter verification. | Phase 6 step 3 — implement the SHA-pinned `pull_request` evaluation workflow |
| 2026-07-29 | Phase 6 step 1 — applied the owner-approved Recovery A authority amendment and completed `pg-eval seed-ci`. Added the checked-in certified `safety_screen` fixture with exact good-v1 SHA-256 `f8da4cd3b3ba21b17c2525ea5f7dd5767bf9bfc026c66f0175649e351632c944`, degraded-v2 SHA-256 `4f9969b7d21e0526eabeaa04fe31e89b218fba71ee4695ffd9609c7db5908652`, and initial `prod`/`candidate` labels at v1. The seeder strictly validates that fixture and the canonical 50-case/one-target dataset, seeds/repairs both registry prompts and labels plus the dataset, and creates exactly one enabled `ci-evals` key with a 1,000,000-micro-USD budget and RPM 1,000. Recovery A now requires zero keys before creation or exactly one locked `ci-evals` key for intact replay, reserves and syncs an empty mode-`0600` handoff before the one-time POST, authenticates its plaintext under the single-key invariant, rechecks exact inventory, and requires discarding/recreating the ephemeral gateway/database/handoff after every ambiguous or interrupted POST/write/verification path. The bypass regression with two valid keys and the wrong plaintext fails before `/v1/models`, prompt, or dataset calls; an ambiguous server-side create retains the empty `0600` marker and fails closed on replay. CLI output reports only non-secret status and works from both source and built layouts. The built seeder ran twice against an isolated real gateway (`created` then `reused`), leaving exactly one $1/RPM-1,000 key, safety v1/v2, judge v1, both labels at safety v1, one dataset row, a 51-byte mode-`600` handoff, and zero request/provider rows after graceful stop; its temporary secret/database artifacts were moved to Trash. Final lint checked 157 files, all 756 tests in 58 files passed, all four packages built, and diff checks were clean. The independent correction audit ran 54 focused tests plus eval type-checking, verified both original findings closed and no credential leakage, and returned `APPROVE`. | Phase 6 step 2 — blocked pending manual creation of four dedicated provider-side-capped CI credentials and the four matching GitHub Actions secrets |
| 2026-07-29 | Phase 6 step 1 candidate and blocker — drafted a checked-in `safety_screen` fixture containing exact certified v1 SHA-256 `f8da4cd3b3ba21b17c2525ea5f7dd5767bf9bfc026c66f0175649e351632c944`, exact deliberately degraded v2 SHA-256 `4f9969b7d21e0526eabeaa04fe31e89b218fba71ee4695ffd9609c7db5908652`, and initial `prod`/`candidate` labels at v1. Drafted `pg-eval seed-ci` with strict fixture/dataset validation, exact `ci-evals` 1,000,000-micro-USD/RPM-1,000 creation, mode-0600 handoff, marker-bound prompt/version recovery, label-write idempotence, judge seeding, and dataset upsert. The first isolated real-CLI attempt exposed and corrected a source-only `judge-seed.js` resolution defect; the next isolated fresh priced gateway ran the direct CLI twice with exact create/reuse state, zero spend, a 51-byte mode-600 handoff, and clean shutdown. Lint checked 157 files, all 754 tests in 58 files passed, and all four packages built without provider traffic. Independent review nevertheless returned `CHANGES REQUIRED`: any other enabled gateway key in the file can authenticate without proving it is the $1 `ci-evals` row, and a crash after the one-time key POST but before local persistence is unrecoverable. The exact-single-key invariant can fix identity binding, but strong interrupted-run recovery needs either an authority amendment for fail-closed fresh-gateway recreation or an approved rotate-key API. Opus/max then Fable/max each produced no usable answer during bounded escalations and were terminated; no Fable answer substituted for owner judgment. No Step 1 file was committed, and no provider, persistent project database, or GitHub mutation occurred. | Phase 6 step 1 — blocked pending the project owner's key-recovery contract choice |
| 2026-07-29 | Phase 6 topology approval — the project owner selected Option 1 after the recorded preflight conflict and Claude Opus / max analysis. Synchronized the project idea, guide, playbook, orchestrator, and progress record so “four-provider” now means four dedicated provider-side-capped CI credentials, while the paid quality gate remains the canonical fresh paired DeepSeek-target/Terra-judge run. Anthropic and Gemini are configured but unexercised by Phase 6 and cannot be described as live-green. The session-start suite passed all 745 tests; after the doc update, lint checked 154 files, the known lifecycle child-exit-143 flake cleared on its focused rerun, and the authoritative full suite passed all 745 tests in 57 files. No provider, Docker, GitHub secret, workflow, dataset, prompt, or API mutation occurred. | Phase 6 step 1 — implement the idempotent CI seed, checked-in certified prompt fixtures, $1 gateway key, labels, and dataset registration |
| 2026-07-29 | Phase 6 preflight — read the complete Phase 6 playbook and GUIDE §7.4/runner contracts, inspected the frozen DeepSeek→Terra judge implementation, one-provider dataset, prompt fixtures, workflow state, local credential presence, repository metadata, and GitHub Actions secret names without exposing values. The repo is authenticated as `ASDFGHJKLZXC123/promptgate` on default branch `master`; no Actions secrets exist, and local OpenAI/Gemini/DeepSeek/admin values are nonblank while Anthropic is absent. A documentation-authority conflict blocks implementation: playbook step 2 and GUIDE §7.4 define the four-provider requirement as four dedicated capped credentials and write all four into `.env`, while GUIDE §7.1's canonical dataset and the workflow sketch run only DeepSeek; this progress record instead required a four-target map. Per the owner's escalation rule, Claude Opus / max independently analyzed the conflict and recommended four capped credentials with the unchanged DeepSeek-target/Terra-judge paired gate: it preserves the certified dataset hash and score scale, uses about 3% of the $1 key, and avoids an uncalibrated roughly 100-minute four-target run that could exceed the circuit breaker. It also found the workflow sketch exports `ADMIN_TOKEN` while the CLI expects `PG_ADMIN_TOKEN`, and confirmed the certified `safety_screen` fixture/seed split is required in step 1. No Fable escalation was needed because Opus completed successfully. No file beyond this progress record, provider, Docker, GitHub secret, workflow, or Phase 6 product artifact was mutated. | Phase 6 — blocked before step 1 pending explicit owner gate-interpretation choice; dedicated capped provider credentials/secrets remain a later manual step-2 prerequisite |
| 2026-07-29 | Phase 5 completion gate — the project owner explicitly approved the completed phase after the good prompt returned exit 0, the deliberately degraded prompt returned exit 1 with eight named failures and durable same-judge comparison, every request/run/result/cost and restart check reconciled, checkpoint B1 corrected the scalar score-drop boundary and returned final `APPROVE`, checkpoint B2 confirmed credible provenance/discrimination and returned final `APPROVE`, the certified v1 prompt body/hash was preserved, and the final lint/745-test/four-package build gate passed. | Phase 6 — not started; begin with the Phase 6 authority/preflight and resolve its distinct four-provider non-self-judging gate before implementation |
| 2026-07-29 | Phase 5 checkpoint B2 dataset/pathway review — Claude Fable 5 / high independently read the complete authority, live/B1 evidence, case-level provenance ledger, raw Fable generation prompt/output, final dataset and assert modules, relevant tests, and Phase 6 contract; it independently reproduced all recorded generation/dataset digests. Verdict was `proceed with adjustments`: provenance is exemplary and mechanically enforces the 2 observed / 17 policy-derived / 31 synthetic classes, visible four-edit trail, 24/10/16 label distribution, rubric equality, and ancestry; the good 49/50 exit-0 result versus degraded 42/50 with a `0.20714285714285707` score drop and eight coherent failures is credible discrimination, with no judge leakage or label error. The sole required adjustment was to preserve the exact certified good `safety_screen` v1 artifact outside the local database. A stopped-database read-only extraction recorded its exact admin request body and canonical SHA-256 `f8da4cd3b3ba21b17c2525ea5f7dd5767bf9bfc026c66f0175649e351632c944`; the same procedure reproduced the existing v2 hash exactly, with no runtime mutation or live call. Non-blocking drift, known-flaky-case, undertriage-evidence, Phase 6 map/fixture, and judge-identity limits are explicitly retained. Final Fable/high correction audit reproduced both database/body hashes, reconciled all live statistics and labels, confirmed no threshold/code/Phase 6 change, and returned `APPROVE`; final lint, 745 tests, and build passed. | Phase 5 — awaiting explicit human completion approval; Phase 6 not started |
| 2026-07-29 | Phase 5 checkpoint B1 technical audit — GPT-5.6 Sol / xhigh independently read the complete authority/evidence set, audited assertion order and deterministic short-circuiting, direct-bin 0/1/2 behavior, fresh paired/history identity and math, persistence/aggregate validation, cost reconciliation, the fixed DeepSeek/Terra judge topology, cache/history/retry/self-judge rails, and the eval-of-evals meta-test. It ran 129 focused offline tests, confirmed direct help exit 0 and infrastructure exit 2, and independently reconciled runs 2–5 plus keys 23/25 in the stopped database. Verdict was `proceed with adjustments`: raw paired/history score-drop subtraction falsely rejected exact `0.05` equality because `0.9 - 0.85` becomes `0.050000000000000044`, and the Phase 5 evidence lead still described the superseded provider blocker. Added one shared scalar one-epsilon comparator and four path-level regressions proving exact-boundary exit 0 and one-billionth-over exit 1 for both paired and history paths; corrected the evidence lead. The live degraded `0.20714285714285707` drop remains decisively red, so no provider rerun was needed. Post-correction gates passed 74 focused tests, all 745 tests in 57 files, lint, and the four-package build; the same reviewer audited the diff and returned final `APPROVE`. No other technical defect was found. | Phase 5 — checkpoint B1 complete with adjustments; checkpoint B2 next, then the human completion gate; Phase 6 not started |
| 2026-07-29 | Phase 5 degraded Verify retry completion — on clean HEAD `d45cc3f`, presence-only checks confirmed nonblank admin/OpenAI/DeepSeek values without printing them; lint checked 154 files, all 741 tests passed with local-loopback permission, and all four packages built. Rebuilt the exact image as manifest `sha256:ef33d5f95373d1a39aa4e5016dc1de4e46f8a096a6c059dae10cf64de03fa80b`; an isolated no-provider canary persisted 50 results, stopped with exit 0/no OOM, exposed only a 126,976-byte main file with integrity `ok`, zero foreign-key violations, one run/50 results/zero requests, recovered all 50 results after same-container restart, stopped cleanly again, and was removed. Recreated the real gateway on that image and confirmed only runs 1–3, disabled key 24, `prod` v1, and weakened `candidate` v2. The owner-authorized signal-aware coordinator created disposable key 25 with the exact 1,000,000 micro-USD budget/RPM 1000 and invoked the repo-root direct bin exactly once with a fresh `--baseline prod`, 15-second pacing, no history/cache/eval-runner or outer retry, and secrets only through the child environment. Baseline run 4 persisted 50/50, score `0.9671428571428571`, cost 15,437 micro-USD; candidate-v2 run 5 persisted 42/50, score `0.76`, cost 13,416 micro-USD, and the CLI printed eight named failures before returning expected quality exit 1. The 0.20714285714285707 score drop exceeded the allowed 0.05 even though pass rate 0.84 remained above threshold 0.8. Cleanup disabled key 25 at 28,853 micro-USD. After shutdown only the 376,832-byte main DB remained; integrity/foreign-key checks passed; 100 DeepSeek target plus 12 Terra judge rows were all `ok`, uncached, non-estimated, and exactly reconciled to both run costs and key spend. Prompt attribution was 50 DeepSeek rows at safety v1, 50 at safety v2, and 12 Terra rows at immutable judge v1. Same-image/mount restart recovered both 50-result runs and disabled key 25; both graceful stops exited 0/no OOM with absent WAL/SHM. Final lint, all 741 tests, and the four-package build passed. No second paid invocation, checkpoint B1/B2, or Phase 6 work occurred. | Phase 5 — live Verify complete; checkpoint B1 next, then checkpoint B2 and the human completion gate; Phase 6 not started |
| 2026-07-29 | Phase 5 separately fresh degraded Verify — on clean HEAD `31c4373`, the complete 154-file lint/741-test/four-package build preflight and an exact-image no-provider 50-result Docker write/stop/main-file/restart canary passed. The admin API created deliberately weakened `safety_screen` v2 by removing the safety policy while retaining the JSON interface and user template, recorded body hash `4f9969b7d21e0526eabeaa04fe31e89b218fba71ee4695ffd9609c7db5908652`, and moved only `candidate` from v1 to v2 while `prod` remained v1. Fresh disposable key ID 24 received the exact 1,000,000 micro-USD budget and RPM 1000. A signal-aware coordinator supplied secrets only through the child environment and invoked the repo-root direct bin exactly once with a fresh `--baseline prod`, 15-second pacing, and no history/cache/eval-runner or outer retry. Instead of expected quality exit 1, it emitted `Gateway request failed with HTTP status 502.` and exited 2 during the v1 baseline. Key 24 disabled in `finally` at 9,776 micro-USD. The checkpointed DB reconciles 28 successful DeepSeek rows/1,043 micro-USD, four successful Terra rows/8,733 micro-USD, and one unmetered sanitized DeepSeek `provider_error` with zero aggregate spend contribution; all 33 rows are uncached/non-estimated and prompt v1. No v2 request, eval run after ID 3, or failure table exists. Main-file integrity/foreign-key checks passed; same-image/mount restart recovered the disabled key, spend, unchanged run list, and v1/v2 labels; both graceful stops exited 0/no OOM with absent sidecars. The temporary coordinator/copy were removed. Final lint checked 154 files; after sandbox-only `listen EPERM` failures and one transient restarted-child exit 143, the exact lifecycle test passed 1/1 and the final permitted full suite passed 57 files/741 tests without a code change; all four packages built. GPT-5.6 Sol / xhigh independently reconciled the stopped DB, source control flow, cost nullability, authorization boundary, and records, then returned `APPROVE` after exact wording corrections. No eval-runner/outer retry, checkpoint B1/B2, or Phase 6 work occurred. | Phase 5 — degraded Verify blocked on an infrastructure exit before candidate execution; the weakened candidate remains v2 and another paid pair requires explicit owner authorization; B1/B2 and Phase 6 not started |
| 2026-07-29 | Phase 5 fresh good Verify completion — after the owner reported every exposed local value rotated, presence-only file/container checks confirmed the required names were nonblank without printing values. Rebuilt clean HEAD `7ff1a3f` as image manifest `sha256:35607019cf8d6597eef38edd86975a76a7ba6cb0faaf84bc33abe33557995fde`, confirmed health, the exact 50-case/seven-rubric dataset hash, sole DeepSeek target, immutable Terra judge, v1 `prod`/`candidate` labels, and only historical run 1. Because the WAL loss returned the durable key maximum to 21, admin-only zero-budget disabled tombstone ID 22 permanently retired that identifier; freshly generated ID 23 received the exact $1 budget/RPM 1000. A signal-aware coordinator invoked the repo-root direct bin exactly once with fresh `--baseline prod`, no history/cache/eval-runner or outer retry, and 15-second pacing while supplying key/admin values only through the CLI's documented in-memory environment inputs. The command exited 0 after 1,487,325 ms: fresh baseline run 2 persisted 50/50, score `0.9457142857142858`, cost 16,324 micro-USD; candidate run 3 persisted 49/50, score `0.9483333333333333`, cost 15,673 micro-USD. The sole candidate failure was conservative (`urgent` returned where `review` was expected); pass rate `0.98` exceeded threshold `0.8`, and the candidate improved by `0.00261904761904741`. Key 23 disabled in `finally` at 31,997 micro-USD. After shutdown only the 327,680-byte main DB remained; integrity/foreign-key checks passed; 100 DeepSeek plus 13 Terra rows were all `ok`, uncached, non-estimated, and exactly reconciled to both runs/key spend. Same-image/mount restart recovered both 50-result runs and disabled key 23 through the API; both graceful stops exited 0/no OOM with absent WAL/SHM. Post-evidence lint checked 154 files, the permitted full suite passed 57 files/741 tests, and all four packages built. An independent read-only audit reconciled the stopped image/database, arithmetic, secrets, and authorization boundary; its three wording corrections are applied and the technical evidence is approved. No degraded prompt/run, checkpoint B1/B2, or Phase 6 work occurred. | Phase 5 — good Verify green; await explicit approval for the separately fresh degraded pair, then checkpoint B1 followed by B2; Phase 6 not started |
| 2026-07-28 | Phase 5 exact-image no-provider Docker durability canary — built committed lifecycle code `770ea7a7b01fa0c79cbd4eb86250dde39d81b3c3` as manifest list `sha256:cce2449fceb967b0ec5d2440e0bbbc2b7459588261b50ffc0f4703342fce8a11`; started it on isolated loopback port 18787 with a temporary bind mount, a canary-local admin value, and no provider credentials; health returned `{"ok":true}`. Admin-only writes persisted one dataset plus a discriminating 50-result DeepSeek-shaped run and immediately returned all 50 results with score `0.9114285714285716`. SIGTERM stopped the container with exit 0 and no OOM. Only after verified exit, the bind mount contained the 135,168-byte main DB and no WAL/SHM sidecars; a read-only main-file copy returned 1 dataset/1 run/50 results/0 provider requests/50 cases. Restarting the same container and mount returned healthy and the detail API returned run 1 with all 50 results and the exact score; the second graceful stop again exited 0. The temporary container/data/copy were removed. No provider credential or chat route was used, and `.dockerignore` excludes `.env*`. | Phase 5 — every pre-paid durability gate is green; rotate the exposed provider keys and `ADMIN_TOKEN`, then run exactly one authorized fresh good Verify; degraded Verify, B1/B2, and Phase 6 not started |
| 2026-07-28 | Phase 5 owner-approved SQLite WAL lifecycle amendment — retained WAL/schema/bind mount; extracted a shared once-guarded SIGTERM/SIGINT shutdown that exits only after Fastify close settles; made `onClose` validate exact safe-integer `{busy:0,log:0,checkpointed:0}` TRUNCATE output, close exactly once in `finally`, and retain checkpoint-error identity; and set Compose grace to 150 seconds. Added mock error-path coverage, a bounded source-entrypoint SIGTERM test that persists 50 results, proves a zero/absent WAL and main-file 1-run/50-result counts after exit, restarts, and confirms all 50 results through the admin API. The production-sized route regression reproduced an old generic-500 rollback from request-order versus case-ID-order floating addition; one shared scored-count-scaled epsilon helper now accepts only that roundoff, while a 1e-9 mismatch and every null/count/pass/micro-USD mismatch remain red. Synchronized the project idea, guide, playbook, orchestration rail, progress, and evidence, including mandatory graceful stops before every historical playbook host-SQLite query. Focused durability tests passed 4 files/30 tests; lint checked 154 files; the permitted full suite passed 57 files/741 tests; all four packages built strictly; diff/no-`any`/Phase 6 checks were clean. GPT-5.6 Terra / medium implemented the bounded candidate, and GPT-5.6 Sol / xhigh independently audited code, tests, docs, and gates and returned `APPROVE`. No Docker mutation or provider call occurred during implementation. A later Compose-render inspection accidentally expanded `.env` values into local command output; none was committed or copied, but all configured keys plus `ADMIN_TOKEN` must be rotated before paid traffic. | Phase 5 — run the exact-image no-provider Docker canary without provider credentials; then wait for credential rotation before the single authorized fresh good Verify; degraded Verify, B1/B2, and Phase 6 not started |
| 2026-07-28 | Phase 5 fresh good Verify on committed Terra amendment `e47acac` — rebuilt the exact commit (image manifest `sha256:b22e54cba5c7479bde3db503ee98ff6d9b4578af7970bc3ab3655d7943c859b6`), confirmed healthy HTTP 200 and presence-only admin/OpenAI/DeepSeek configuration, and created disposable key 22 with the exact 1,000,000 micro-USD budget and RPM 1000. Ran the direct-bin paired command once with `--baseline prod`, no history reuse, and 15-second pacing. The run continued for about 720 seconds, proving the Terra request-contract correction progressed beyond the former first-rubric failure; runner control flow completed the 50-case baseline and next called the admin create-run endpoint. That request returned generic HTTP 500, the CLI emitted `Admin request failed with HTTP status 500.` and exited 2, and cleanup reported key 22 disabled at 16,206 micro-USD. No new eval run existed. During runtime the admin API had observed key 22 and its spend; after the gateway was stopped and restarted, key 22 and its request rows were absent, the durable admin state had returned to 21 keys plus run ID 1, and the WAL was empty. Static inspection confirmed the WAL bind mount has no production SIGTERM/SIGINT path to await Fastify `onClose`/database close, matching the earlier documented Docker Desktop/VirtioFS hazard. This proves a shutdown durability defect but not the exact HTTP 500 cause, because the admin route retained only a generic response and the server retained no safe error code/log. The restarted gateway returned `{"ok":true}`. No retry, degraded prompt/run, history reuse, checkpoint B1/B2, or Phase 6 work occurred. Two independent read-only audits agreed that a fresh paid run and the narrow WAL lifecycle correction require explicit owner approval. Post-evidence lint checked 148 files, the permitted full suite passed all 720 tests in 54 files, and the strict four-package build passed; the first sandboxed run's five failures were only `listen EPERM` denials. | Phase 5 — blocked pending approval for graceful signal shutdown, validated WAL checkpointing, offline 50-result/signal/Docker durability gates, and exactly one new fresh good Verify after those gates pass; Phase 6 not started |
| 2026-07-28 | Phase 5 owner-approved Terra temperature amendment — changed the shared eval request policy and client builder so exactly `gpt-5.6-terra` omits temperature while DeepSeek, Gemini, and Luna remain at zero and the existing Claude omission remains intact. The real judge still sends immutable `judge_rubric_v1@1`, `reasoning_effort: "high"`, JSON-object output, and no cache. Added a direct shared-Zod four-way regression that accepts Terra omission/DeepSeek zero and rejects Terra zero/DeepSeek omission, plus client and real-judge payload coverage. Synchronized the project idea, guide, playbook, orchestration rail, progress, and Phase 5 evidence while retaining all 50 cases/seven rubrics, fresh pairing without history, 15-second pacing, $1 keys, no retries/self-judge, persistence/table/direct-bin/exit contracts, general provider/history support, and Phase 6 unchanged. Three focused files passed 36 tests; lint checked 148 files; all 720 tests in 54 files and the strict four-package build passed. GPT-5.6 Terra / medium implemented the bounded code candidate; GPT-5.6 Sol / xhigh passed 89 tests across eight focused files, verified the exact request/schema/authority boundary and unchanged Phase 6 scope, and returned `APPROVE`. No provider call, Docker operation, database/admin mutation, dataset/degraded-prompt/history action, or Phase 6 work occurred. | Phase 5 — commit the amendment, then rebuild and run one fresh paired good Verify with a new disposable $1 key; Phase 6 not started |
| 2026-07-28 | Phase 5 replacement-key Terra attempt on clean commit `6d6244b` — a no-generation OpenAI model-list check returned HTTP 200, found `gpt-5.6-terra`, and listed 127 models, resolving the prior authentication blocker. Rebuilt and force-recreated the gateway image (manifest list `sha256:1b00aa7215d0f4f039b294a770f02a4596f325e7ed6b5cd74d769346f9c9bbf6`), confirmed healthy HTTP 200 and presence-only OpenAI/DeepSeek/admin configuration, immutable judge ID 2/version 1, and unchanged safety labels at prompt ID 3/version 1. Created disposable key ID 21 with the exact 1,000,000 micro-USD budget and RPM 1000, then ran the direct-bin fresh paired command once with no history flag and 15-second pacing. DeepSeek succeeded with 399 input, 163 output, 49 micro-USD, and 2,201 ms; the immediately following Terra rubric request recorded sanitized OpenAI `provider_error` in 2,405 ms. The CLI emitted `Rubric evaluator failed.` and exited 2; cleanup disabled key 21 at 49 micro-USD. No eval run exists for HEAD, totals remain one earlier run/50 results, and both labels remain v1. Official OpenAI documentation classifies Terra as a reasoning model, lists Chat Completions/structured outputs and high reasoning as supported, and says temperature changes are unsupported for reasoning models. Because the upstream status/body is intentionally absent, the approved `temperature: 0` override is the strongest identified incompatibility but remains an inference. Post-evidence lint checked 147 files, the permitted local-loopback test run passed all 719 tests in 53 files, and the strict four-package build passed; the first sandboxed test process's five failures were only `listen EPERM` denials. No retry, direct diagnostic generation, degraded prompt, history reuse, Gemini call, or Phase 6 work occurred. | Phase 5 — blocked on explicit owner direction for a narrow Terra temperature amendment; do not retry the fresh pair before that decision |
| 2026-07-28 | Phase 5 fresh Terra-judged good Verify attempt on clean commit `d005724` — rebuilt and recreated the gateway so the container received the owner-confirmed rotated OpenAI key, confirmed HTTP 200 health, exact current dataset hash with 50 cases/seven rubrics/sole DeepSeek target, current DeepSeek/Terra pricing, immutable judge v1, and good `prod`/`candidate` labels at safety prompt v1. Created disposable key ID 20 with the exact 1,000,000 micro-USD budget and RPM 1000, then ran the direct-bin paired command with `--baseline prod`, no history flag, and 15-second pacing. The first DeepSeek target call succeeded with 399 input tokens, 347 output tokens, 100 micro-USD, and 4,518 ms; the immediately following Terra rubric request recorded OpenAI `provider_error` in 268 ms, so the CLI emitted sanitized `Rubric evaluator failed.` and exited 2. A no-generation OpenAI model-list check returned HTTP 401, proving credential authorization failure without establishing Terra model/option support. No Terra judgment, baseline/candidate eval run, or result persisted; database totals remain the earlier one run/50 results, labels remain v1, and disposable key 20 was disabled after two requests and 100 micro-USD. No retry, degraded prompt, Gemini call, history reuse, or Phase 6 work occurred. | Phase 5 — blocked on replacing `OPENAI_API_KEY` with an active authorized key, then rerun the fresh paired good Verify |
| 2026-07-28 | Phase 5 owner-approved Terra-judge comparability amendment — restored the original `gpt-5.6-terra` judge wire policy from commit `368ce06` while preserving the target-aware frozen DeepSeek→Terra map and no-self-judge check. Terra rubric calls now carry exact model, immutable prompt, temperature zero through the gateway client, `reasoning_effort: "high"`, JSON-object output, and no cache. Active CLI/pairing regressions omit history and prove the baseline runs before the candidate while `historicalRuns` is not called; the complete hardened historical selection/mismatch/malformed suite remains intact as general functionality, and the runner rejects Terra as a target before side effects. Synchronized the project idea, guide, playbook, orchestration rail, progress, and Phase 5 evidence with the owner approval and judge-comparability rationale. The owner confirmed the previously exposed OpenAI key was rotated before any Terra call. Five focused files passed 79 tests; lint checked 147 files; all 719 tests in 53 files and the strict four-package build passed; diff, secret, no-`any`, dataset, gateway, schema, CI, and Phase 6 scope checks were clean. GPT-5.6 Sol / xhigh required four stale active-document corrections and then returned `APPROVE`. No dataset, pricing, gateway provider, DB/schema/API, CI/Phase 6, provider, Docker, or admin/database change occurred. | Phase 5 — run the fresh Terra-judged good pair, then the separate degraded pair, checkpoint B1, and checkpoint B2 |
| 2026-07-28 | Phase 5 owner-approved persisted-baseline quota amendment — accepted completed live baseline run ID 1 from code HEAD `a77b9cf` without reissuing provider traffic. Hardened the eval runner's historical-response trust boundary and exact selection to require a valid persisted run ID, dataset slug and 64-hex hash, frozen baseline prompt ID/ref/version, exact target model, and current dataset case count before dataset upsert, provider traffic, or any admin mutation. Added discriminating first-exact-row, candidate-only, mismatch, and malformed-response tests that prove sanitized failure and no provider/admin mutation side effects, including rejection of candidate-ref history so the stopped unpersisted partial candidate cannot be reused. Synchronized the project idea, guide, playbook, orchestration rail, progress, and Phase 5 evidence; the two remaining direct-bin commands now retain `--baseline prod`, add `--baseline-from-history`, keep 15-second pacing and $1/no-cache/no-retry contracts, and run on separate fresh Gemini quota days. The actual read-only history response and checked-in loader independently matched run ID 1 to dataset hash `407c72cc4e9699ccb6aee1a3221e9da348b364ef4851a7f3a07e810b6bf8bef5`, prompt ID 3/ref `safety_screen@prod`/version 1, `deepseek-v4-flash`, and 50 cases. Three focused files passed 43 tests; lint checked 147 files; all 718 tests in 53 files and the strict four-package build passed; diff, no-`any`, dataset, and Phase 6 scope checks were clean. GPT-5.6 Terra / medium implemented the bounded amendment and GPT-5.6 Sol / xhigh independently passed 64 focused tests plus the eval strict build, required one discriminating test correction, then returned final `APPROVE`. No dataset, provider, Docker, admin/database mutation, or Phase 6 action occurred. | Phase 5 — live good candidate pending on a fresh Gemini quota day; degraded candidate follows on a separate fresh quota day, then checkpoint B1 followed by B2 |
| 2026-07-28 | Phase 5 literal good Verify attempt — confirmed the new Pacific quota day, clean `a77b9cf` worktree, required configured keys, immutable judge fixture, good `safety_screen` v1 labels, zero prior eval rows, and a healthy force-rebuilt Docker image. Created disposable key ID 19 with the exact $1 budget and local RPM 1000, then ran the repo-root direct-bin paired command with `15000` ms pacing. The baseline persisted 50/50 passes, score 1.0, 11,909 micro-USD, and 50 results. The candidate then stopped at infrastructure exit 2 with sanitized stderr `Rubric evaluator failed.` Durable traffic was 77/77 DeepSeek `ok`, 10/10 Gemini `ok`, then one Gemini `provider_error`; all judge starts were at least 15 seconds apart, total persisted request cost was 16,204 micro-USD, and the cleanup trap disabled key 19. Candidate persistence never occurred, both labels remain on v1, and no degraded prompt or Phase 6 work was created. Evidence is in `docs/evidence/phase-5.md`. An `.env` shell-sourcing preflight emitted the newly added OpenAI value because its entry was shell-incompatible; it was neither committed nor sent, Node dotenv loading replaced shell sourcing, and the key must be rotated before use. Post-evidence lint checked 147 files and all 717 tests in 53 files passed with loopback permission. GPT-5.6 Sol / xhigh independently confirmed the blocked verdict and strict fact/inference boundary; its smallest in-repo recommendation is a separately approved persisted-baseline amendment that first locks history matching to the exact dataset hash, prompt ID/ref/version, model, and 50-case count, reducing each remaining command to at most seven Gemini judgments. | Phase 5 — blocked pending a provider path that supports all 14 judgments or explicit approval of the hardened persisted-baseline amendment; checkpoint B1 then B2 remain pending |
| 2026-07-27 | Phase 5 DeepSeek-led amendment — owner approved `deepseek-v4-flash` as the sole target and `gemini-2.5-flash` only as its independent rubric judge after Gemini's bounded quota failures. The runtime-frozen target/map and pre-side-effect rejection tests now enforce that exact topology; the dataset remains exactly 50 cases with seven rubric cases, limiting each paired command to at most 14 Gemini judge calls. The amendment preserves paired `--baseline prod`, no cache/retries/self-judge, the $1 budget, persistence/table/exit contracts, and Phase 6's separate four-provider requirement. Both literal commands use `15000` ms pacing on separate fresh Gemini daily-quota days. Six focused contract files passed 83 tests in the independent audit; the orchestrator's broader seven-file set passed 86 tests; lint checked 147 files; all 717 tests in 53 files and the strict four-package build passed; diff checks were clean. GPT-5.6 Terra / medium implemented the bounded change and GPT-5.6 Sol / xhigh independently returned `APPROVE`. No provider call or Phase 6 work occurred. | Phase 5 — run the DeepSeek-target literal good Verify, wait for a fresh Gemini daily quota, then run the degraded Verify; checkpoint B1 followed by B2 |
| 2026-07-27 | Phase 5 quota-pacing correction — after the first direct-bin live attempt isolated Gemini's upstream quota, the project owner explicitly approved CLI-only `--min-request-interval-ms`, default `0`, with `6500` ms on both literal Verify processes and an unconditional 65-second cooldown between them. GPT-5.6 Terra / medium added strict canonical safe-integer validation before runtime or service work; a monotonic run-scoped queue keyed by the actual request model; and shared pacing across baseline, candidate, target, and cross-judge calls without adding eval retries or changing cache, budget, dataset, model, persistence, or exit behavior. The guide, playbook, future CI sketch, orchestration rail, and decision evidence are synchronized. The focused eval build and 37 CLI/runner/pacer tests passed; lint checked 147 files; all 716 tests in 53 files passed with loopback permission; the strict four-package build, source/compiled help probes, diff, stale-command, and no-`any` checks passed. The initial sandboxed full run's eight failures were only three timeouts plus five `listen EPERM` loopback denials; the identical permitted rerun was authoritative. GPT-5.6 Sol / xhigh independently returned `APPROVE`. No provider call or Phase 6 work occurred. | Phase 5 — run the literal paced good/degraded Verify through Gemini + DeepSeek; then checkpoint B1 followed by B2 |
| 2026-07-27 | Phase 5 literal-command correction — rebuilt committed cross-judge image `2486193`, confirmed Docker healthy, seeded immutable `judge_rubric_v1@1`, and created `safety_screen` good v1 with `prod`/`candidate` at v1. The first documented filtered CLI attempt failed before dataset load because pnpm ran inside `packages/evals` and the runner duplicated the package path; no eval row or provider request was created. Its disposable $1 key (ID 16) recorded zero micro-USD and was disabled. A separate reproduction confirmed filtered pnpm maps child infrastructure exit `2` to outer `1`, conflicting with GUIDE §7.2. GPT-5.6 Terra / medium implemented module-relative checked-in dataset resolution; GPT-5.6 Sol / xhigh proved the direct workspace bin preserves exit `2` and recommended the narrow invocation correction. After explicit project-owner approval, synchronized the guide, playbook, subprocess tests, runner tests, and progress record. Two focused files passed 20 tests; lint checked 144 files; all 699 tests in 52 files passed with loopback permission; the strict four-package build passed; direct-bin probes returned infrastructure `2` and help `0`; and compiled package-cwd resolution returned the exact checked-in dataset. No provider call occurred and Phase 6 was not started. | Phase 5 — rerun the literal direct-bin good/degraded Verify through Gemini + DeepSeek; then checkpoint B1 followed by B2 |
| 2026-07-27 | Phase 5 pre-Verify decision #11 amendment — after confirming that both configured providers are needed, the project owner explicitly chose Gemini + DeepSeek for Phase 5. Synchronized the project idea, guide, playbook, orchestration rules, golden dataset, runner, judge, meta-test, and current progress. The runner now requires the canonical exact two-model set before any admin/provider side effect; its runtime-frozen cross-map sends Gemini outputs only to DeepSeek and DeepSeek outputs only to Gemini. Judge requests retain immutable `judge_rubric_v1@1`, JSON-object mode, temperature zero, no cache, combined target/judge cost accounting, and omit `reasoning_effort`. General gateway transport support remains unchanged. A GPT-5.6 Sol / xhigh read-only contract audit approved the design and required the exact-set, runtime-freeze, wire, and no-side-effect regressions; GPT-5.6 Terra / medium implemented the bounded evaluator correction. Five focused files passed 56 tests; lint checked 144 files; the authoritative full suite passed all 698 tests in 52 files after the sandbox-only `listen EPERM` cases were rerun with loopback permission; and the strict four-package build plus diff checks passed. No provider call was made and Phase 6 was not started. | Phase 5 — run the literal good/degraded Verify through Gemini + DeepSeek; then checkpoint B1 followed by B2 |
| 2026-07-27 | Phase 5 step 7 completion — recorded the project owner's explicit approval of all 50 proposed labels and A1/A2 assertions, including the four transparent audited edits. Finalized the exact 2 observed / 17 policy-derived / 31 Fable-derived dataset, 50-case expected-risk map, immutable-source direct-diagnosis assertion, seven cost-bounded rubrics, raw-generation evidence, per-case provenance/reviewer ledger, byte-preserving Biome override, and synchronization tests. The 29-test focused dataset/assertion set, lint across 144 files, all 695 tests in 52 files, the strict four-package build, raw prompt/output digests, and diff checks passed with no provider traffic. The first sandbox run's five stream failures were only `listen EPERM`; an authorized rerun cleared them. Concurrent independent and orchestrator full-suite runs then rebuilt the same ignored `dist` folders and exposed the source-backed pnpm probes' 5-second default; after serializing the gate and applying bounded 15-second per-probe timeouts, the authoritative run passed. `.env`/process presence checks disclosed no values and confirmed Gemini, DeepSeek, and `ADMIN_TOKEN` configured but OpenAI and Anthropic absent. No Phase 6 work was started. | Phase 5 — verify pending; literal Verify blocked on OpenAI and Anthropic credentials |
| 2026-07-27 | Phase 5 step 7 generation and draft — reran Claude Fable 5 / high after usage was restored and captured one authoritative 31-case raw JSON batch plus its exact prompt, raw-file, and normalized-output digests. The requested `7/7/6/6/5` category split, 31 unique IDs, de-identification, schema, and size bounds passed. A GPT-5.6 Sol / xhigh read-only audit required four transparent final-ledger edits while preserving the raw output: one symptom-only ID rename, one review-boundary note softening, one duplicate benign-control replacement, and one ancestry correction. Drafted exactly 50 cases from 2 observed inputs, 17 policy-derived cases, and 31 Fable-derived cases; every case has valid-JSON, exact-risk, and immutable-source direct-diagnosis assertions, with seven representative rubric cases. Six focused dataset/assertion/provenance synchronization tests passed without provider calls. No commit or Phase 6 work was performed. | Phase 5, step 7 — mandatory project-owner review of all 50 labels and assertions pending |
| 2026-07-26 | Phase 5 step 7 provenance preflight — exhausted the local archive, complete public repository topology, immutable source tree, and retained original build-session evidence. Confirmed that no six-case test file is verifiable or recoverable in the audited sources: the source session preserves a benign symptom probe, a whitespace validation probe, an urgent symptom probe, and an unsafe generated output. The project owner approved the smallest provenance-only correction; synchronized decision #12, GUIDE §7.3, the playbook step, this progress record, and `docs/evidence/phase-5-seed-provenance.md`. A Claude Fable 5 / high read-only attempt failed because the authenticated account has no Fable usage credits; GPT-5.6 Sol / xhigh independently confirmed that current authority required the amendment and did not substitute itself for dataset judgment. No dataset, assertions, provider calls, or Phase 6 work were started. | Phase 5, step 7 — blocked before generation on Fable usage credits |
| 2026-07-26 | Phase 5 step 6 — added migration-backed eval datasets, runs, and case results; a strict DAO with atomic run/result persistence and newest-first history/detail reads; authenticated admin dataset/run/history/detail routes with no route-level SQL; and safe scoped handling for hostile JSON keys and non-data bodies. Completed `pg-eval run` with config precedence, source/built loading, exact prompt-label freezing, numeric gateway refs, baseline-first paired execution, historical comparison before traffic, target-plus-judge cost accounting, manual/CI/cache trigger attribution, dataset hashing/upsert, atomic confirmation, threshold and score-drop exits, warnings, and markdown output. The test surface covers storage rollback and constraints, auth and validation, exact/newest history selection, duplicate confirmation IDs, invalid refs/SHA/models, no-side-effect failures, paired ordering, cache policy, cost accounting, CLI exit codes, source and compiled execution, and an exact multi-model offline meta-gate. Claude Opus 4.8 / xhigh remained unavailable because Claude Code is not logged in, so GPT-5.6 Terra / medium supplied the bounded integration; GPT-5.6 Sol / xhigh independently returned `APPROVE`. The initial full run exposed only sandbox loopback denial plus two stale migration lists; after correcting the lists and authorizing loopback, 59 independently focused tests, all 689 tests, lint across 140 files, the strict four-package build, source/built CLI probes, diff, no-`any`, SQL-boundary, and secret checks passed without provider traffic. | Phase 5, step 7 — not started; Phase 6 not started |
| 2026-07-26 | Phase 5 step 5 — added the locked `gpt-5.6-terra` / high rubric evaluator through the existing no-cache, non-streaming eval gateway client and immutable `judge_rubric_v1@1` registry prompt. A single serialized untrusted payload preserves only candidate output, rubric, case description, and de-identified variables; the registry fixture forbids following or echoing embedded instructions. Strict Zod parsing requires exactly `{pass, score, rationale}`, a finite `[0,1]` score, and a nonblank rationale no longer than 1,000 characters; malformed, fenced, trailing, primitive, incomplete, extra-key, wrong-type, out-of-range, or oversized output and every gateway failure remain sanitized infrastructure errors. Added the checked-in prompt fixture plus a path-preserving admin seeder that validates HTTP(S)/token configuration, uses a deterministic content marker, recognizes only matching immutable v1, repairs only its own zero-version partial seed, and fails closed on incompatible state. Claude Opus 4.8 / xhigh remained unavailable because Claude Code is not logged in, so GPT-5.6 Terra / medium supplied the bounded candidate and GPT-5.6 Sol / xhigh returned final `APPROVE` after URL/token, composed-request, data-minimization, non-echo, bounded-output, and malicious-output hardening. Twelve focused tests, all 655 tests, lint across 127 files, the built Node 22 fixture probe, strict eval and four-package builds, diff, no-`any`, and secret checks passed without network or provider traffic. | Phase 5, step 6 — not started; Phase 6 not started |
| 2026-07-26 | Phase 5 step 4 — applied the human-approved temperature amendment in the playbook and guide, then added the exact five-model non-streaming gateway client with CLI-over-environment configuration, path-preserving HTTP(S) endpoint resolution, Bearer authentication, abort propagation, no retries, `pg_feature: "eval"`, default no-cache enforcement, explicit local cache opt-in, UUID/cache/exact micro-USD response metadata, safe HTTP error-code extraction, and typed config/network/abort/HTTP/protocol failures that retain neither keys nor raw bodies. Canonical shared Zod schemas now own the exact eval model/request/response policy; descriptor-based variable normalization snapshots getter-free, null-prototype JSON and rejects getters, hidden `toJSON`, proxies, cycles, sparse arrays, symbols, non-finite values, and exotic prototypes while preserving own `__proto__` data. A typed source/compiled selector keeps clean workspace execution independent of ignored build output while installed and built Node 22 execution uses compiled shared JavaScript. Claude Opus 4.8 / xhigh remained unavailable because Claude Code is not logged in, so GPT-5.6 Terra / medium supplied the bounded candidate. GPT-5.6 Sol / xhigh audits reproduced unsafe variable serialization, local wire-schema drift, source-import, pnpm-shim, and installed-package type-stripping defects; all received discriminating regressions or Node 22 probes before final `APPROVE`. Nineteen gateway-client tests and the 27-test CLI/client set passed; all 643 tests, lint across 123 files, strict shared/eval and four-package builds, exact `pg-eval --help`, source-with-hidden-dist, installed-like compiled, diff, no-`any`, and secret checks passed without network or provider traffic. | Phase 5, step 5 — not started; Phase 6 not started |
| 2026-07-26 | Phase 5 step 3 — added the exact eight-type assertion registry, direct Ajv JSON Schema validation, semantic JSON equality, raw/case-insensitive substring and regex checks, full JSON parsing, in-process ESM JavaScript assertions, injected rubric evaluation, typed infrastructure errors, declared-index execution records, deterministic-first stable ordering, first-failure short-circuiting, and rubric-only normalized score aggregation. JavaScript contexts are deeply frozen and secret-free; special-character paths are converted to file URLs; async schemas, bad modules/returns, missing judges, and malformed/out-of-range rubric results fail as infrastructure rather than quality. Claude Opus 4.8 / xhigh remained unavailable because Claude Code is not logged in, so GPT-5.6 Terra / medium supplied the bounded candidate. GPT-5.6 Sol / xhigh reproduced four quiet-failure/cost defects—async-schema promises, unsafe module specifiers, continued paid rubric calls, and missing/unbounded scores—then returned `APPROVE` after corrections. Fourteen focused tests, all 624 tests, lint across 120 files, the strict eval and four-package builds, native-Node path verification, and diff checks passed without network or provider traffic. A separate Sol / xhigh Step 4 audit then exposed the documented Sonnet temperature conflict, so work stopped before Step 4 pending human amendment. | Phase 5, step 4 — blocked pending the temperature-policy amendment; Phase 6 not started |
| 2026-07-26 | Phase 5 step 2 — added exact-byte SHA-256 dataset hashing; strict Zod schemas for registry prompt refs, models, thresholds, JSON-safe variables, stable test cases, and all eight supported assertion types; deterministic description-derived IDs with explicit-ID reservation, bounded collision suffixes, and warnings; duplicate explicit-ID rejection; and dataset-relative regular-file resolution for JavaScript assertions. The JSON boundary preserves own `__proto__` and `constructor` keys without prototype pollution, and assertion-specific payloads reject missing, extraneous, malformed, or blank paid-rubric values. Claude Opus 4.8 / xhigh remained unavailable because Claude Code is not logged in, so GPT-5.6 Terra / medium supplied the bounded candidate and GPT-5.6 Sol / xhigh found and verified the strict assertion, JSON-safety, immutable-ID, and prototype-key corrections. Nine focused tests, all 610 tests, lint across 118 files, the strict eval and four-package builds, and diff checks passed without provider traffic. | Phase 5, step 3 — not started |
| 2026-07-26 | Phase 5 step 1 — added `yaml` and `zod`, a strict `pg-eval` CLI scaffold for exactly `run`, `seed-ci`, and `comment`, source-backed package-bin exposure through the root workspace, package-owned Node types, clean build output, and focused command/option/help/error/clean-checkout executable tests. The exact `pnpm --filter @promptgate/evals exec pg-eval --help` command passed with `dist` temporarily absent. Spark was unavailable, so GPT-5.6 Terra / medium implemented the bounded scaffold; the required Claude Opus 4.8 / xhigh owner attempt first stalled and then reported Claude Code was not logged in, so the documented alternate-provider GPT-5.6 Sol / xhigh audit found the initial ignored-dist defect and the Node-types build gap, then returned final `APPROVE` after both corrections. Eight focused tests, all 601 tests, lint across 116 files, the strict four-package build, emitted-artifact inspection, and diff checks passed without provider traffic. | Phase 5, step 2 — not started |
| 2026-07-26 | Phase 4 completion gate — the project owner explicitly approved the completed and verified phase after reviewing the five ordered implementation commits, 21-file implementation scope, immutable schema, template/DAO/admin/pipeline behavior, live Gemini rollback, DeepSeek activation, exact request attribution, label history, stable diff, safe prompt errors, lint, 593 tests, strict build, Docker health, bounded verification adjustments, disabled disposable keys, and independent Terra / high approval. | Phase 5 — not started |
| 2026-07-26 | Phase 4 completion gate — rebuilt and force-recreated exact step-5 commit `7dc7cf8`, confirmed healthy loopback Docker service, and ran the rollback Verify. Created `greet` v1 English and v2 French, deployed `prod`, then used one unchanged Gemini request body (`pg_vars.style = "tersely"`, SHA-256 `a5c402c9edf0785066693e7906c62ac4b7dbfe1a05de331373a51206ae6f4e1c`) to receive `Salut.` at v2 and `Hello.` after rollback to v1. The literal newest-first SQLite rows were `1|1` and `1|2`; list, label history, stable unified diff, and database UPDATE/DELETE immutability checks passed. Unknown-prompt and missing-required-variable requests returned safe 404/400 errors before provider dispatch, with exact durable attribution semantics. The configured-provider matrix passed through the registry path for Gemini 2.5 Flash and DeepSeek V4 Flash; OpenAI and Anthropic were explicitly deferred because their keys are absent. A 16-token no-visible-output probe and one ambiguous bilingual-style probe were superseded by the authoritative literal-`tersely` pair; all four disposable gateway keys were disabled, and total traffic remained bounded to five Gemini calls and one DeepSeek call. An independent GPT-5.6 Terra / high code audit returned `APPROVE`; lint checked 114 files, all 593 tests passed with loopback permission, strict four-package build passed, the exact Docker image remained healthy, and evidence is in `docs/evidence/phase-4.md`. | Phase 4 — awaiting explicit human approval; Phase 5 not started |
| 2026-07-26 | Phase 4 step 5 — implemented `promptResolve` after provider/pricing and before budget/cache; added strict per-field `x-pg-prompt` / JSON `x-pg-vars` fallbacks with body precedence; revalidated durable messages/variables; rendered and prepended string-content templates; and rejected unknown refs, unusable required vars, and corrupt storage safely before dispatch. Resolved bodies now govern budget and cache behavior, adapters receive no `pg_*` fields, and the durable logging DAO persists only complete positive prompt-attribution pairs across success, cache hit, prompt rejection, budget rejection, provider error, and streaming outcomes. Tests cover empty client messages, version/label refs, malformed and ignored conflicting headers, required declaration order and non-string values, optional/undeclared literals, escape/non-recursion, unchanged multimodal content, label move/rollback misses and hit attribution, resolved-text budget admission, safe corrupt rows, provider scrubbing, and prototype-like variable keys. GPT-5.6 Terra / high implemented the integration; an independent Terra / high audit exposed the `__proto__` key edge, approved the corrected Zod/null-prototype boundary, and passed the full 593-test suite. Sixty-nine focused tests, lint, the gateway strict build, and diff/no-`any` checks passed without provider traffic. | Phase 4 — literal Verify block pending |
| 2026-07-26 | Phase 4 step 4 — added the authenticated prompt admin API for create/list, immutable version append, label deploy/rollback, and stable unified version diffs; registered it beneath the existing timing-safe `/admin` hook and safe error handler. Added DAO-only summary/version reads so route handlers contain no SQL, strict Zod body/path schemas for OpenAI-format messages and variable declarations, recursively canonical 2-space JSON, `diff@9.0.0`, and snake_case responses. Tests cover inherited auth, duplicate/create/list ordering, empty and populated latest-version/label summaries, incrementing versions, invalid messages/variables, deploy/rollback history, absent prompt/version paths, malformed JSON and unknown fields, canonical key-order no-op, and sane plain-text line changes. GPT-5.6 Terra / high implemented the integration; a separate Terra / high audit returned `APPROVE`, after which its two optional list/diff-404 coverage suggestions were added. Fifteen focused admin/DAO tests, the gateway strict build, lint across 112 files, and diff/no-`any` checks passed without provider traffic. | Phase 4, step 5 — not started |
| 2026-07-26 | Phase 4 step 3 — added a strict typed registry DAO for prompt creation, per-prompt immutable version allocation, transactional label deployment/history, and safe `slug@version` / `slug@label` resolution. Version MAX+1 allocation and insertion share one transaction; label upsert and history insertion share another and never use replace semantics. Tests cover duplicate slugs, prompt-isolated monotonic versions, failed append recovery, exact JSON round-trip, immutable rows, first/moved/same-version label history, refreshed timestamps, dangling and cross-prompt rejection, forced second-statement rollback, numeric/label resolution, malformed and unsafe refs, and read-only misses. GPT-5.6 Terra / high implemented the bounded DAO; a separate Terra / high audit required the two direct transaction-policy regressions and returned final `APPROVE`. Twelve focused DAO/migration tests, the gateway strict build, lint across 110 files, and diff checks passed without network traffic. | Phase 4, step 4 — not started |
| 2026-07-26 | Phase 4 step 2 — added the dependency-free shared `renderTemplate` helper with exact `{{name}}` substitution, own-property lookup, deterministic de-duplicated misses, extra-variable tolerance, one-pass replacement, and `\{{` literal escaping; exported it from `@promptgate/shared` and covered missing, extra, repeated, multiple, non-recursive, and escape behavior. Spark was unavailable, so the documented GPT-5.6 Terra / medium fallback implemented the bounded helper; a separate Terra / medium audit returned `APPROVE`. Five focused tests, the shared strict build, lint across 108 files, and diff checks passed without network traffic. | Phase 4, step 3 — not started |
| 2026-07-26 | Phase 4 step 1 — added migration `004_registry.sql` with unique prompt slugs, immutable numbered prompt versions, mutable labels protected by a composite prompt/version foreign key, label-move history storage, and database-level UPDATE/DELETE rejection for version rows. Fresh, upgrade, idempotency, uniqueness, missing-version, cross-prompt-version, mutable-label, and both immutability-trigger cases are covered. GPT-5.6 Terra / high implemented the bounded migration and a separate Terra / high audit found no authority conflict or blocker. Lint checked 106 files, all 559 tests passed, the gateway strict build passed, and diff checks were clean; the sandbox-only loopback `EPERM` cases passed on the authorized rerun. | Phase 4, step 2 — not started |
| 2026-07-26 | Phase 3 completion gate — the project owner explicitly approved the completed and verified phase after reviewing the ordered commits, 22-file implementation scope, cache/budget/rate/concurrency results, configured-provider live matrix, unavailable-provider deferrals, cached-stream correction and stress evidence, lint, 556 tests, strict build, Docker health, resolved harness deviations, independent Sol / xhigh verdict, and final evidence record. | Phase 4 — not started |
| 2026-07-26 | Phase 3 authoritative completion rerun — rebuilt and force-recreated exact correction commit `41fe9c9`, then reran the complete Verify block from a new unique request. Gemini returned a live cache miss costing 22 micro-USD and the identical request returned `x-pg-cache: hit` with literal newest-first rows `1|0` and `0|22`. An admin budget PATCH made the immediate next request return 429 `budget_exceeded`; RPM 2 admitted exactly two cached requests and rejected the next three as `rate_limited`; ten concurrent no-cache requests under exactly one 37-micro-USD reservation admitted one Gemini call and durably rejected nine for budget. The configured-provider matrix was repeated on the final image: Gemini passed the functional checks, and DeepSeek returned HTTP 200 with exact non-estimated 120 input/39 output/28 micro-USD metering. OpenAI and Anthropic were explicitly deferred because their keys are absent. All three disposable keys were disabled, temporary responses were removed, and the final container was healthy on loopback. The evidence also records the superseded zsh/WAL harness issue, the independently approved cached-stream backpressure correction, its 10/10 and 30/30 real-socket stress results, and the no-provider 128 KiB manual SSE check. Final lint checked 105 files, all 556 tests passed, the strict four-package build passed, and diff/secret checks were clean. Evidence: `docs/evidence/phase-3.md`. | Phase 3 — awaiting explicit human approval; Phase 4 not started |
| 2026-07-26 | Phase 3 pre-gate cached-stream correction — the orchestrator's mandatory full-suite rerun reproduced a timing edge in the real-socket cache replay reset regression: a 2 MiB completion could be handed to Node as one source buffer and marked `writableEnded` before the client received its first bytes and reset, producing a truthful but nondeterministic `ok` row instead of exercising the abnormal-close fallback. GPT-5.6 Terra / high split each already-framed cached SSE buffer into 16 KiB shared subarray views; exact bytes and SSE semantics are unchanged, while backpressure can surface the reset before clean completion. The focused regression passed 10/10 orchestrator repetitions and 30/30 independent GPT-5.6 Sol / xhigh repetitions; Sol returned `APPROVE`, the full 23-test streaming file, all 556 tests, lint across 105 files, and the strict build passed. A rebuilt real gateway replayed a 128 KiB cache hit as exactly three SSE frames with exact content and usage, `x-pg-cache: hit`, zero cost, no provider call, a disabled disposable key, and healthy restart. | Phase 3 — exact committed-image Verify rerun pending; Phase 4 not started |
| 2026-07-26 | Phase 3 initial verification run — rebuilt and recreated the committed step-6 Docker image, then ran the Phase 3 Verify block from a clean durable state. Identical Gemini requests returned `x-pg-cache: miss` then `hit`; the literal newest-first rows were `1|0` and `0|22`. An admin budget PATCH caused the immediate next request to return 429 `budget_exceeded`; RPM 2 admitted two sequential cache hits and rejected the next three as `rate_limited`; ten concurrent no-cache requests under exactly one 37-micro-USD reservation admitted one provider call and durably rejected nine for budget. Both disposable functional keys were disabled. The required available-key matrix called the owner's configured Gemini and DeepSeek models: Gemini powered the functional Verify and DeepSeek returned HTTP 200 with exact persisted 72 input/51 output/24 micro-USD usage; its disposable key was disabled. OpenAI and Anthropic were explicitly deferred because their keys are absent. A first zsh harness attempt used the read-only `status` parameter after issuing one bounded Gemini request; its transient state was discarded. Docker Desktop's bind-mounted WAL required graceful gateway checkpoints before literal host SQLite reads; every setting change still used the admin API. The first offline audit was green, but the orchestrator's later mandatory full-suite run exposed the cached-stream timing edge recorded above and reopened the gate before any evidence commit. | Phase 3 — cached-stream correction required before final Verify rerun; Phase 4 not started |
| 2026-07-26 | Phase 3 step 6 — locked and discriminated the final auth → request-log init → rate limit → validate → provider/pricing → Phase 4 prompt placeholder → budget reserve → cache read → adapter → meter → cache write → durable log/reconcile order. Added current-contract `x-pg-feature` and strict `x-pg-no-cache` fallbacks with body precedence, feature persistence, and read/write bypass coverage while leaving prompt/vars untouched. Corrected post-header provider/contract failures to persist exact captured usage or an explicit prompt-plus-visible-output estimate before budget reconciliation, never a zero/null fiction or cache write. Added an idempotent abnormal-close finalizer for synthetic streaming cache hits; a real 2 MiB replay reset proves one exact zero-cost `client_aborted` row and reservation release, while a forced SQLite insert failure proves retained debt. Pipeline precedence tests now distinguish auth/log, rate/parse, validation/routing, routing/budget, budget/cache, cache/adapter, meter/cache, and cache/log boundaries. GPT-5.6 Terra / high implemented and corrected the bounded step. GPT-5.6 Sol / xhigh reproduced the failed-stream accounting and cached-replay reset defects, identified the missing Phase 3 header fallbacks, required removal of one over-specific unauthenticated request-ID assertion, and returned final `APPROVE`. Fifty-nine focused handler tests and Sol's 68-test focused matrix passed; all 556 tests, lint across 105 files, the strict four-package build, and diff checks passed without live provider traffic. | Phase 3 — verify pending |
| 2026-07-26 | Phase 3 step 5 — added a single-process per-server budget guard that synchronously admits against current-month durable spend, in-flight reservations, fail-closed debt, and a validated ordinary-input/output estimate; equality is allowed and ten concurrent calls cannot outrun the affordable reservation count. Reservations now precede cache/provider work and remain active across cache hits, provider failures, streaming aborts, and every other outcome until the request row is durable; successful inserts invalidate settled memo state, while failed inserts retain `max(reserved, known_actual)` exactly once. Added a current-month spend DAO, strict positive default token configuration, exact 429/audit behavior, successful admin-PATCH invalidation without clearing active state, and direct coverage for month/key isolation, invalid actual values, actual cost above reservation on both durable and failed-log paths, cache-hit logging failures, and provider-error release. GPT-5.6 Terra / high implemented the bounded step. GPT-5.6 Sol / xhigh first required validation-before-release and the direct DAO/larger-actual regressions, then returned `APPROVE` after correction. Sixty-nine focused tests, all 550 tests, lint across 105 files, the strict four-package build, and diff checks passed without provider traffic; the focused sandbox run's four loopback `EPERM` cases all passed on the authorized local-socket rerun. | Phase 3, step 6 — not started |
| 2026-07-26 | Phase 3 step 4 — added one in-memory per-key token bucket registry per server with full initial capacity, fractional `rpm/60` refill, capacity capping, backward-clock protection, integer `Retry-After`, key isolation, and safe persisted-RPM reconfiguration that never mints a fresh burst. Integrated enforcement on authenticated chat completions after request-log initialization and before body parsing; rejected calls return the exact OpenAI-format 429, retain their request UUID and a fact-free `rejected_rate_limited` audit row, and cannot parse, increment a cache hit, or invoke an adapter. A separate rate clock preserves existing latency-test semantics. GPT-5.6 Terra / high implemented the bounded step; an independent Terra / high audit returned `APPROVE`. Forty focused tests, all 536 tests, lint across 101 files, the strict four-package build, and diff checks passed without provider traffic. | Phase 3, step 5 — not started |
| 2026-07-26 | Phase 3 step 3 — added validated atomic cache upserts after metering for successful non-streaming responses and losslessly assembled successful streams; represented missing non-stream usage as JSON `null`; required exact usage for synthetic stream hits; reset hit metadata on refresh; added positive finite TTL configuration, expired-row deletion, and an hourly unref'd sweep cleared before database close. The streaming assembler covers interleaved choices, text, reasoning, tool fragments, finish reasons, identity, usage, and compatible stable extras; incomplete or unrepresentable transcripts bypass writes without changing live output. GPT-5.6 Terra / high implemented the bounded step. A separate Terra / high audit found silently dropped nested tool metadata and missing no-write proofs; both received fail-closed regressions, including `pg_no_cache`, provider error, client abort, and assembly-ineligible paths. Sixty-six focused tests, all 528 tests, lint across 99 files, and the strict four-package build passed without live provider traffic. | Phase 3, step 4 — not started |
| 2026-07-26 | Phase 3 step 2 — added a transactional DAO lookup for unexpired exact-match entries with Zod-validated stored response/usage, model/hash guards, usage-consistency checks, and hit metadata updated only after a usable hit; integrated reads after provider resolution and before adapter availability; returned stored non-stream JSON unchanged; replayed compact multi-choice/tool-aware SSE; bypassed reads for `pg_no_cache`; normalized cached Gemini hidden-thinking tokens while forcing zero cost; and logged `cache_hit=1` without provider traffic. GPT-5.6 Terra / high implemented and corrected the bounded step. Forty-six focused tests, all 518 tests, lint across 97 files, and the strict four-package build passed. | Phase 3, step 3 — not started |
| 2026-07-26 | Human-approved Phase 3 budget/pipeline amendment — corrected the impossible budget-before-validation/pricing/prompt order; placed metering before priced cache writes; retained reservations through durable request logging with fail-closed debt on insert failure; and described chars/4 reserve/reconcile accurately as a concurrency-safe single-process circuit breaker backed by provider-side absolute spend limits. Synchronized the guide, playbook, and decision log without changing implementation. | Phase 3, step 2 — not started |
| 2026-07-26 | Phase 3 step 1 — added SHA-256 cache keys over recursively code-unit-sorted JSON for the complete forwarded request body; reused prefix-based `pg_*` stripping; excluded only `stream` and `stream_options`; rejected non-JSON values; and proved recursive insertion-order and source-whitespace invariance across 100 deterministic shuffles plus sensitivity to every representative forwarded field, nested provider options, and a future unknown field. GPT-5.6 Terra / high implemented the bounded step. Six focused tests, all 508 tests, lint across 95 files, and the strict four-package build passed without live traffic. | Phase 3, step 2 — not started |
| 2026-07-26 | Phase 2 completion gate — the project owner explicitly approved the completed and verified phase after reviewing the ordered commits, changed-file scope, configured-provider live results, unavailable-provider deferrals, exact usage and published-rate cost reconciliation, lint, 502 tests, strict build, Docker health, resolved live-contract deviation, checkpoint A verdict, and final evidence record. | Phase 3 — not started |
| 2026-07-25 | Phase 2 completion checkpoint — rebuilt corrected commit `b1b3f6e` and reran the live gateway matrix from the start. Gemini streamed `Hi!` in one chunk and persisted exact 3 input/17 billable output/44 micro-USD; DeepSeek streamed 64 chunks and persisted exact cache-aware 6 input/63 output/19 micro-USD. Both owner-scoped usage lookups returned 200 with `streamed=1`, `cost_estimated=0`, `status=ok`, and `first_token_ms < total_ms`. OpenAI and Anthropic were explicitly deferred because their keys are absent. The literal four-row SQLite output retained the two earlier failed rows below the two passing final rows; the evidence distinguishes them. Docker remained healthy with HTTP 200. Final lint checked 93 files, all 502 tests passed, strict four-package build passed, and diff checks were clean. GPT-5.6 Sol / xhigh checkpoint A audited all four provider paths, the live/deferred matrix, the full Phase 2 diff, and Phase 3 seam compatibility; verdict `proceed with adjustments`, limited to committing/linking `docs/evidence/phase-2.md`, moving this file to awaiting approval, and keeping Phase 3 not started. | Phase 2 completion gate — awaiting explicit human approval; Phase 3 not started |
| 2026-07-25 | Phase 2 Verify live-contract correction — rebuilt and started the current Docker image, then ran the gateway streaming matrix. Gemini failed safely before headers with 502 and DeepSeek forwarded visible content but ended `provider_error`; their rows had `streamed=1` but null metering, so the gate correctly remained closed. OpenAI and Anthropic were explicitly deferred for missing keys. The one authorized direct non-streaming and one direct streaming capture per configured provider all returned HTTP 200 and proved both Gemini and DeepSeek now attach terminal usage to a nonempty final choice; Gemini may collapse the entire response into that one frame. Replaced exactly their four fixtures with sanitized captures, cleared only their manifest markers, retained pending OpenAI/Anthropic official-contract fixtures, and widened the adapter only for all-finished terminal choices. Claude Opus 4.8 / xhigh stalled before writing; the documented GPT-5.6 Sol / xhigh escalation implemented the correction, and a fresh Sol / xhigh audit returned `APPROVE`. The orchestrator passed 46 focused tests; the independent audit passed 73 focused and all 502 tests, lint across 93 files, strict four-package build, diff/no-`any`/secret checks, exact fixture reconciliation, and combined-frame timing/metering probes without further provider calls. | Phase 2 — literal Verify rerun pending from the start |
| 2026-07-25 | Phase 2 step 6 — added authenticated `GET /v1/requests/:request_id/usage` registration inside the protected `/v1` plugin; a Zod-validated canonical UUID and authenticated key ID are matched together in a safe-projection DAO query; exact, estimated, and nullable metering fields retain their meaning; malformed, unknown, legacy, SQL-shaped, encoded-path, and cross-owner misses share one byte-identical 404; and every authenticated outcome is `private, no-store` without changing the enclosing 401 behavior. Claude Opus 4.8 / xhigh stalled before writing but supplied the security review; GPT-5.6 Terra / medium implemented the bounded fallback. GPT-5.6 Sol / xhigh found the missing 404 cache policy, approved the corrected result, and independently passed hostile-path/timing/auth/no-provider-call probes. The orchestrator passed 17 focused tests, all 496 tests, lint across 93 files, strict four-package build, and diff/no-`any`/secret checks without provider traffic. | Phase 2 — verify pending |
| 2026-07-25 | Phase 2 step 5 — added guarded request- and response-socket disconnect detection before upstream invocation; aborts the single streaming controller; distinguishes `client_aborted` from timeouts and provider failures; preserves exact usage when already received or estimates prompt plus all forwarded visible choices with chars/4 and `cost_estimated=1`; persists a single row even when a reset skips Fastify's `onResponse`; and closes abnormal upstream iterators exactly once. Two Claude Opus 4.8 / xhigh coding sessions stalled before writing, so the documented GPT-5.6 Terra / medium fallback implemented the bounded candidate while Opus supplied compact design and correction reviews. GPT-5.6 Sol / xhigh reproduced a post-reset frame-count/iterator leak and a multi-choice undercount; both received real-loopback regressions before Sol returned `APPROVE`, including an independent pre-first-frame disconnect proof. The orchestrator passed 49 focused tests, all 488 tests, lint across 91 files, strict four-package build, and diff/no-`any`/secret checks without provider traffic. | Phase 2, step 6 — not started |
| 2026-07-25 | Phase 2 step 4 — added native Anthropic named-SSE parsing and OpenAI chunk translation for role, text, parallel tool calls, supported stop reasons, cumulative usage, and terminal `[DONE]`; reused the step-2 request translator with optional-at-invocation credentials, bounded retry, MIME validation, and no live traffic; removed the obsolete unsupported-stream error path. Claude Opus 4.8 / xhigh produced the initial candidate and three bounded correction passes. GPT-5.6 Sol / xhigh directly reproduced false-success paths for lossy/malformed tool inputs, duplicate tool IDs, valid multiple `message_delta` events, incomplete start/native usage validation, malformed refusal details, and missing thinking-token detail; every finding received a discriminating regression before Sol returned `APPROVE`. The orchestrator independently passed 255 focused tests, all 485 tests, lint across 91 files, strict four-package build, diff checks, and no-`any`/secret scans; local real-server cases used only temporary loopback sockets. Every fixture remains pending until the approved Verify window. | Phase 2, step 5 — not started |
| 2026-07-25 | Phase 2 step 3 — added OpenAI/Gemini/DeepSeek streaming request injection, provider-authenticated retry-before-body fetches, byte-boundary-safe SSE parsing, strict chunk/usage trust boundaries, incremental Fastify forwarding with backpressure, first-visible-content timing across every choice, provider-specific exact streaming metering, and persisted `streamed=1` rows without a streaming cost header. Claude Opus 4.8 / xhigh produced a green initial candidate; GPT-5.6 Sol / xhigh directly reproduced multiline framing, premature terminal signaling, same-millisecond timing, later-choice detection, EOF-test, polling, and MIME-validation failures. Opus corrected all findings with discriminating regressions, and Sol returned `APPROVE` after rerunning the probes. All 148 focused tests, 394 full tests, lint, strict build, and diff checks passed; the local incremental-backpressure test used only a temporary loopback server, with no credentials or live provider calls. | Phase 2, step 4 — not started |
| 2026-07-25 | Phase 2 step 2 — added the optional-at-boot Anthropic Messages transport and default registry wiring; native non-streaming system/content/image/structured-output/tool translation; bounded retry and deterministic response normalization; explicit Sonnet 5 adaptive-thinking suppression; safe client/upstream error separation; strict Zod response, stop-reason, tool-history, and cache-usage boundaries; and real-server offline 400/no-fetch plus 502/no-leak coverage. Claude Opus 4.8 / xhigh owned the initial draft and a substantive correction pass but its saved continuation was stale and the fresh pass stopped at denied chained-shell verification; GPT-5.6 Sol / xhigh completed the bounded corrections and returned `APPROVE`, followed by a separate Sol / xhigh final `APPROVE`. All 114 focused Anthropic tests, 322 full tests, lint, strict build, and diff checks passed without credentials or live calls. | Phase 2, step 3 — not started |
| 2026-07-25 | Phase 2 step 1 — authored exactly eight official-contract fixtures: one non-streaming JSON response and one raw streaming SSE transcript for OpenAI, Anthropic, Gemini, and DeepSeek; recorded exact official sources and `live_capture_pending` for every fixture; added provider-specific integrity checks for response identity, SSE order/termination, usage, cache, reasoning, hidden-thinking reconciliation, sanitized identifiers, and secret absence; and kept all work offline. Claude Opus 4.8 / xhigh produced the initial partial set before stalling; GPT-5.6 Terra / medium completed the bounded fixture mechanics because Luna was unavailable; GPT-5.6 Sol / xhigh required stronger DeepSeek and integrity coverage, then returned `APPROVE`. Four focused fixture tests and the full 208-test suite passed. | Phase 2, step 2 — not started |
| 2026-07-25 | Phase 2 preflight and human-approved fixture-timing amendment — read the complete orchestration manual, current progress, Phase 2 playbook, and GUIDE §§3.1–3.6, 5.1, and 11; confirmed a clean Phase 1-approved repository with 204 tests green; verified Gemini/DeepSeek keys present and OpenAI/Anthropic absent without exposing values; resolved the external tools TODO by inspecting web_builder_llm's native Anthropic client; and retained the higher-authority no-live-calls-before-Verify safety rail. Claude Opus 4.8 / xhigh confirmed the documentation conflict and amendment direction. | Phase 2, step 1 — not started |
| 2026-07-25 | Phase 1 completion gate — the project owner explicitly approved the completed and verified phase after reviewing commits, changed-file scope, live/deferred provider matrix, exact published-rate reconciliation, lint, 204 tests, strict build, Docker health, resolved deviations, and the final evidence record. | Phase 2 — not started |
| 2026-07-25 | Human-approved Phase 1 cost-evidence amendment — accepted independently recorded official published-rate reconciliation plus persisted integer micro-USD rows in place of provider account-dashboard artifacts. Updated the Phase 1 acceptance table, literal Verify outcome requirement, completion evidence, decision log, and current position without changing the already-run Verify block or any code. The prior independent audit's only blocker is resolved; all Phase 1 acceptance criteria now pass. | Phase 1 completion gate — awaiting explicit human approval |
| 2026-07-25 | Phase 1 verification checkpoint — rebuilt the current Docker image, seeded the root mounted database explicitly, and ran the amended Verify block literally. The same pinned OpenAI SDK client returned valid live responses through Gemini 2.5 Flash and DeepSeek V4 Flash; rows persisted as Gemini 3 input/27 billable output/69 micro-USD/ok and DeepSeek 6 input/60 output/18 micro-USD/ok. OpenAI was explicitly deferred because its key is absent, Anthropic was explicitly deferred to Phase 2, Docker remained healthy with HTTP 200, and final lint, 204 tests, strict build, and diff checks passed. An independent gate audit correctly held the separate account-dashboard comparison open; browser access to both dashboards is disabled by the user's site-use settings. Evidence: `docs/evidence/phase-1.md`. | Phase 1 — verify pending on provider dashboard evidence or approved acceptance amendment |
| 2026-07-25 | Phase 1 live verification correction — the first literal run reused a stale Docker image and returned local 404s without provider calls or request rows; after an explicit current-commit image rebuild, the unchanged block reached both providers. DeepSeek logged 6 input/49 output/15 micro-USD correctly; Gemini exposed hidden-thinking underbilling by returning 3 prompt/2 visible completion/28 total while the row logged only 2 output/6 micro-USD. Added validated total reconciliation, Gemini-only candidate-plus-thinking normalization, exact cached/thinking integration and persistence tests, authority-document updates, 69 focused tests, 204 total tests, lint, build, and GPT-5.6 Sol / xhigh `APPROVE`. | Phase 1 — verify rerun pending |
| 2026-07-25 | Human-approved Phase 1 pre-verify correction — retained Gemini 2.5 Flash-Lite support; added and pinned Gemini 2.5 Flash with official standard and cached-input pricing; validated/reconciled compatible Gemini/DeepSeek cache fields; exact Gemini cached-input metering through adapter, pipeline, response header, and persisted request row; updated verify/eval documentation; direct Gemini and DeepSeek credential/model checks returned HTTP 200; 107 focused tests, 202 total tests, lint, and build. GPT-5.6 Sol / xhigh requested full DeepSeek dual-field adapter/persistence evidence before approval, which was added. | Phase 1 — verify pending |
| 2026-07-25 | Phase 1 step 12 — Gemini/DeepSeek prefix routing and default registry wiring; prefix/pricing-provider mismatch rejection; key-independent, no-network four-provider model listing; repository-pinned OpenAI SDK for literal verification; exact Gemini and cache-aware DeepSeek metering plus post-response logging; 50 focused tests, 196 total tests, lint, and build. Claude Sonnet 5 / high implemented the bounded integration; GPT-5.6 Sol / xhigh found only one stale comment, which was corrected before approval. | Phase 1 — verify pending |
| 2026-07-25 | Phase 1 step 11 — Gemini non-streaming endpoint/Bearer auth/config/error/schema/abort behavior; documented `extra_body.google` passthrough; provider-local 408 retry plus unchanged OpenAI/DeepSeek defaults; explicit Phase 2 stream stub; 56 focused tests, 17 new tests, 186 total tests, lint, and build. Claude Sonnet 5 / high implemented the bounded step; GPT-5.6 Sol / xhigh requested the official extension-envelope regression and documentation corrections before approval. | Phase 1, step 12 — not started |
| 2026-07-25 | Phase 1 step 10 — narrow OpenAI-compatible transport extraction with all prior OpenAI tests unchanged; DeepSeek non-streaming endpoint/auth/config/error/retry/abort behavior; verbatim `thinking` passthrough; validated cache usage and provider-specific finish reason; explicit Phase 2 stream stub; 49 focused tests, 14 new tests, 169 total tests, lint, and build. Claude Sonnet 5 / high implemented the bounded step; GPT-5.6 Sol / xhigh returned changes-required on the missing finish reason and approved the corrected remainder. | Phase 1, step 11 — not started |
| 2026-07-25 | Phase 1 step 9 — human-approved four-provider authority amendment; optional Gemini/DeepSeek boot config; approved Gemini/DeepSeek prices; additive cache-rate migration with legacy preservation; Zod-validated paired cache usage; exact cache-hit/cache-miss/output metering; pricing/DAO compatibility; 16 focused test additions, 155 total tests, lint, and build. Claude Sonnet 5 / high produced the bounded draft but did not return before the orchestration cap; GPT-5.6 Sol / xhigh independently audited the integrated result. | Phase 1, step 10 — not started |
| 2026-07-17 | Phase 1 step 8 — authenticated DAO-backed `GET /v1/models`, current date-effective distinct rows, exact official OpenAI list envelope, deterministic ordering, offline provider/network guardrails, 16 focused tests, 139 total tests, and build under Claude Sonnet 5 / high ownership | Phase 1 — verify pending |
| 2026-07-17 | Phase 1 step 7 — human-approved Fable request-identity correction, migration 002 with legacy upgrade proof, non-streaming authenticated chat route, injected providers, exact/estimated metering, UUID/cost/cache headers, bounded body/upstream limits, post-response request logging, safe error mapping, 27 focused tests, 123 total tests, build, and Sonnet 5 / high `APPROVE` | Phase 1, step 8 — not started |
| 2026-07-16 | Phase 1 step 6 — official-spec OpenAI non-streaming adapter, shared prefix-based `pg_*` stripping, strict response validation, typed provider/config errors, reusable abort-aware 429/5xx retry helper, explicit phase 2 stream stub, and 33 focused tests under Claude Sonnet 5 / high ownership | Phase 1, step 7 — not started |
| 2026-07-16 | Phase 1 step 5 — exact provider adapter contract, forward streaming chunk type, static provider prefixes, date-effective pricing DAO, complete `unknown_model` results, shared workspace package exports, 10 focused tests, and deployed-runtime import verification under Claude Sonnet 5 / high ownership | Phase 1, step 6 — not started |
| 2026-07-16 | Phase 1 step 4 — shared OpenAI error formatter, DAO-backed Bearer-key lookup, disabled/invalid-key rejection, typed safe request context, protected `/v1` plugin seam, and 5 focused auth tests under Claude Sonnet 5 / high ownership | Phase 1, step 5 — not started |
| 2026-07-16 | Phase 1 step 3 — DAO-backed admin key create/list/patch endpoints, one-time `pg-` plaintext issuance with SHA-256-only storage, scoped timing-safe admin auth, MTD spend, strict validation/errors, 9 focused tests, and Claude Sonnet 5 / high `APPROVE` | Phase 1, step 4 — not started |
| 2026-07-16 | Phase 1 step 2 — human-approved, Zod-validated provider pricing JSON plus transactional, idempotent source/compiled SQLite seed runners and focused tests | Phase 1, step 3 — not started |
| 2026-07-16 | Phase 1 step 1 — shared Zod OpenAI-compatible wire schemas, inferred strict types, passthrough compatibility, and 34 focused schema tests; exact Claude Sonnet 5 / high review found no required edits | Phase 1, step 2 — blocked pending human pricing/model confirmation |
| 2026-07-16 | Phase 0 completion gate — human approval recorded after local, Docker, Terra, and GitHub Actions evidence passed | Phase 1, step 1 — not started |
| 2026-07-16 | Phase 0 step 8 and completion verification — SHA-pinned workflow, actionlint, frozen install, lint/test/build, Docker health HTTP 200, and green remote `ci` run | Phase 0 completion gate — awaiting explicit human approval |
| 2026-07-16 | Phase 0 step 7 — native-module multi-stage image, loopback-only Compose service, healthcheck, and approved playbook correction | Phase 0, step 8 — not started |
| 2026-07-15 | Phase 0 steps 1–6 — workspace scaffold through the migrated Fastify health server and its first real test | Phase 0, step 7 — blocked before implementation pending human input |
